# Project Status — Faiz E Aam School Platform

## Authoritative recovery checkpoint — 8 September 2026

**Overall state: IN PROGRESS.** The active execution order is exclusively the opening UI-first recovery section of plan.md. All status/verification claims below this checkpoint are historical and must not override it, even where old headings say "Current", "Next phase", or "VERIFIED". No current remote ledger, provider readiness, or deployment is asserted by this checkpoint.

**V15 supersedes V14 (owner decision, 8 September 2026).** The canonical design prototype is `V15 Faiz E Aam School Platform.html` (repository root; V14 remains as the historical reference). Approved rules: (1) V15 is the visual source of truth across code, tests, and documentation; (2) where V15's own responsive/accessibility behavior is weak, the application fixes beyond the prototype; (3) `/environment` and `/staff/facility/*` stay deferred outside UI acceptance; (4) `/administrator/guardians` was a planned-workspace placeholder until 16 September 2026, when the owner asked for the real activation workspace (see the 16 September evidence block); (5) copy V15 layout only — never its illustrative data, counts, actor names, or client-only authorization behavior. The V15 prototype itself contains a known illustrative admission-detail record mismatch and queue-count inconsistencies; its data is not a specification.

Read-only baseline: HEAD `fecfdcf`; 264 tracked paths changed at inspection, plus untracked prototype, UI and migration files. Existing changes span presentation, services, database repairs, tests and documentation. V15 fragments already present in the tree: the About two-column layout, admission key-dates ledger, notice-detail sidebar, `q-head5`/`q-row5` CSS, facts-ledger and record-card primitives. Most V15 composition, shells, wizard, queue, and evidence-panel changes remain unimplemented. These static observations do not establish visual acceptance. There are 78 actual `page.tsx` files; prototype screen keys are not necessarily application URLs.

### Current environment evidence — 16 September 2026 (supabase mode, live project `jxegiamjcawdywqyutdz`)

Owner-directed remediation pass following a code-based end-to-end audit. Commits `addf4d3`, `20ae18f`, `0d6873a`, `31cc647`, `b8f7c57`, `34be1f7`, `454d889` (not pushed). Migrations `000121–000124` applied to the live project through the Supabase MCP server with ledger rows repaired to file versions.

- **Staff invitations (`000121`):** invitation emails are generated with Supabase `generateLink` and delivered from the school Resend domain (no longer the rate-limited default Auth mailer); Administrators can resend (fresh link, expiry extended, idempotency key `staff-invite:<ref>:<attempt>`) or revoke pending/expired invitations from `/administrator/users`; `/auth/complete` consumes implicit-flow links without flashing the expired-link copy and explains `otp_expired`; `/sign-in/invite` checks for a session first; `same_password`/`weak_password` handled. Live: probe invite → email via stub → callback → acceptance → Active; resend and revoke verified against the DB (auth user deleted on revoke).
- **Guardian activation (`000122`, `/administrator/guardians`):** record an email contact, send/resend/revoke activation; `/sign-in/activate` previews the claim (children with class labels) and accepts through `POST /api/auth/guardian-claim-accept`; `email.guardian_welcome` now resolves recipients. Live: Ghulam Nabi probe activated end-to-end and landed on `/portal`; welcome email delivered via stub; probe account suspended and contact revoked afterwards. Email is the only channel until an SMS provider is registered.
- **Shell and UI:** desktop sidebar minimise (persisted), container-query breakpoints for in-shell pages (`.page` is an inline-size container; grids collapse at 767px content, workflow rows at 719px), themed always-visible scrollbars on horizontal scrollers, long-token wrapping; documents workspace rebuilt as a filterable ledger with a compact public-register panel; Administrator and Principal home dashboards rebuilt from a new aggregation service (KPI tiles, approval/drafting queues, 14-day audit activity, admissions funnel, fee collection rail, per-class results progress, support/content/guardian access charts). Overflow audit: 0 findings across all staff routes at 1024/1280/1440, both profiles, sidebar expanded and collapsed.
- **School configuration (`000123`, `/administrator/school` + `/principal/school`, action `academics.configure`):** academic years with lifecycle, classes (standard Nursery–Class 10 catalog helper), sections (planned → active → archived, copy from a previous year), subjects, exam terms with per-section assessment components (locked once marks exist). CSV imports accept class labels (`Class 8-A`, `8A`, `Nursery A`). Planned sections are excluded from timetables, teaching assignments and imports until activated. Live: catalog added (grades 5 → 13), Class 6-A/6-B created, Hindi added, `unit test 1` created for 6-A and 8-A, a draft entry sheet created on 8-A.
- **Delivery operations (`000124`, `/administrator/deliveries`, action `deliveries.manage`):** every successful domain write schedules a bounded worker batch after the response (`scheduleOutboxKick`, 5 s debounce; adapter gateway + pre-account/public routes), so notifications no longer wait for the daily cron; the workspace shows events, masked recipients, worker freshness, "Run worker now", and reason-gated retry/requeue. Live: `security.account_reactivated` delivered by the kick within ~2 s (`outbox.kick` log line), a permanent failure retried and delivered. The 34 sandbox-era permanent failures remain visible and retryable by design.
- **Two-step verification:** the demo auto-elevation route no longer deletes real authenticator factors; `TotpForm` ignores "Dev auto-elevation" factors and purges them (`POST /api/auth/mfa/purge-dev-factors`) when TOTP is required again; `scripts/purge-dev-mfa-factors.mjs` (dry-run default) is available for bulk cleanup before removing `FASS_DEMO_NO_TOTP`.
- **Gates (16 Sep):** `npm run typecheck`, `npm run lint`, `npm test -w apps/web` (151 files · 1285 tests), `npm run build`, `npm run check:icons` (69), `sh scripts/validate-db-local.sh` through `000124` including new school-configuration and delivery-operations DB suites.
- **Known limitations:** Supabase Auth recovery emails still use the default Auth mailer; `vercel.json` cron stays daily as the safety net (Hobby plan); the local dev server shares the live Supabase project, so local invitations carry the local `APP_URL`; `security.account_*` outbox keys fire once per account (dedupe by event key).

### Environment evidence — 11–12 September 2026 (supabase mode unless noted)

This evidence block is superseded by the 16 September block above wherever they disagree, and supersedes the older verification claims below. Finance remains parked by owner instruction, and the `/environment` and `/staff/facility/*` demonstrators remain excluded from acceptance.

- **Local gates PASSED (12 Sep):** `npm run typecheck`, `npm run lint`, `npm test` (web 947 passed · contracts 73 passed), `npm run build`, `npm run check:icons` (69 source icons), and `sh scripts/validate-db-local.sh` — the last including the full RPC suite against migrations `000001–000104` plus the seed's admission document requirements.
- **Remote migration ledger:** `000102_service_role_record_audit.sql`, `000103_results_sheet_native_withdraw.sql`, and `000104_school_documents_upload.sql` (school-document upload boundary for the public register) applied 11–12 September 2026 through the guarded linked CLI; remote now carries `000001–000104`. The 000103 header note "intentionally NOT applied to staging" is historical only.
- **Browser suites PASSED (demo adapter, :3002):** `scripts/critical-journeys.cjs` → ALL JOURNEYS PASSED (18 journeys); `scripts/local-feature-journeys/index.cjs` → 104 passed, 0 failed. Journey selectors were updated for the results correction-request maker/checker UI and the 000102/000103-era labels.
- **Live staging route walkthrough PASSED (supabase, :3000):** 59 routes scanned, 0 flagged, with both Administrator and Principal sign-ins landing on their canonical workspaces; `/api/health` ready after an outbox run.
- **Live staging workflows verified this session:** exports request → release → signed download → cancel; settings v2 approval and effective-from; results publish → report release → guardian visibility; guardian link request request→approve; CSV imports (students, guardians, links) with accept/fix/skip and dependency-ordered commit; timetable v5 publication; job applications email-only submission (no status route); admissions window `ADMW-2026-717998` and application `APP-2026-33BA4E` advanced submitted → under_review → assessment → offered; second-administrator maker/checker approvals.
- **Live module verification (12 Sep):** sheet-native results withdrawal end-to-end (`RES-2026-61FB4B` → `PUB-2026-A1A742` withdrawn, entry sheet v9, release supersession, audit row, family portal withdrawal); report-card PDF downloaded by the family (`%PDF`, 2321 bytes, versioned `document_generation_records`); careers scorecard → shortlist → interview → offer chain with maker/checker denial and a real Resend-sandbox delivery (`JOB-2026-D80F2C`, `JOB-2026-336A8C`); admissions document upload → ready → Principal reviewer visibility, changes-requested → re-upload loop with retained old versions, and cross-family document isolation (404/`InvalidSignature`); signed Resend webhook (delivered projection, `email_suppressions` on permanent bounce, invalid signature 401); staff invitation `INV-2026-C035FD` accepted end-to-end after the hash-token fix, with the principal profile active and TOTP enrollment reached.
- **Public document register verified end-to-end (12 Sep):** a content-publishing Administrator uploaded a fictional school PDF from the staff Documents page (`DOC-2026-3E97D2`), the approval control refused it while scan-pending, the scan callback flipped it to ready, approval put it in the anonymous public register on `/notices` with a working anonymous download (452-byte PDF, `%PDF`), and withdrawal removed it from public view while the private row and both `Document public visibility changed` audit events remained on file. Finalize now skips the association-link RPC for `school_document` (no association table exists), and the staff upload panel is gated by `content.publish`.
- **Five-guardian results matrix verified end-to-end (15 Sep, supabase staging):** five synthetic guardian accounts (`aamina.wani@`, `bashir.dar@`, `chasme.shah@`, `dilawar.bhat@`, `erum.mir@faizaam.example`) each linked to one child in a different class (8-A, 8-B, 9-C, 9-D, 10-A) with distinct student details. The Principal created midterm batches per class and entered distinct marks; an independent Administrator approved, published, and assembled report releases. Guardian outcomes matched exactly: Hiba (8-A Mathematics 88/100, release `RPR-2026-A3BD47`), Aryan (8-B English entered, not published → honest "No published report"), Zoya (9-C published then withdrawn → no report), Imad (9-D English 91/100 visible while correction v2 sheet `RES-2026-D36B93` is open), Noor (10-A English 22/100 failing, shown honestly). Each portal listed exactly its own child, and a cross-child release URL returned the honest not-found state. Class/student alignment was verified from the database (marks map to the right roster, class, and subject) and demonstrated with the Aside browser skill.
- **Autonomous improvement loops (15 Sep, overnight):** four parallel agent passes each ran plan → browser verification → fix → re-verification across the guardian portal, results/timetable, content/support/documents, admissions/applicant, plus a dedicated security pass, a public job-intake redesign, and a 4k-scale audit. Verified outcomes: guardians get honest fees/results/receipt 404 copy, no invented concessions, portal-scoped 404 shell, 0 axe violations across 22 portal routes; the New batch panel now lists component-less terms with an actionable note instead of a dead disabled select, the entry matrix is student-labelled without duplicate React keys, and mobile refs no longer wrap; the public register refuses every non-school document at both the app and SQL boundaries (high-severity leak closed); the family notice placeholder was scoped to published, unexpired rows; archive now confirms before the terminal action. Applicant request-changes editing now restores the submitted snapshot and attachments (a resubmit previously erased answers and forced re-uploads) and stale `?edit=` links redirect to tracking. Hot-path fixes: admissions decision flows read one row instead of the whole queue, invoice/payment registers are paged with exact totals, public content reads only the current version, link-request lists are cached and invalidated, and CSV commit gained group indexes.
- **Identity/access improvement loop (15 Sep, supabase staging :3000, browser + live SQL):** staff-only `/administrator/users` (guardian/applicant accounts were listed as "LEGACY ACCESS — REVIEW REQUIRED" staff; revoked invitations displayed as Active; failed invitations had no honest label; the Change profile control always failed for legacy accounts — now routed to the existing `staff_profile_adopt` command), settings saved-by showed a raw account UUID (now resolved through `app.audit_account_reference`; an unconfigured grading scheme no longer masquerades as "Letter grades"; a save now refreshes the approval panel instead of hiding the new draft; self-approval is disabled with the maker/checker reason), audit action filter was a stale fixed list and the register stopped silently at 100 rows (options now derive from loaded events; cursor pagination with "Load older events" uses the existing `app.audit_list_page`), guardian-link rows with empty display names rendered blank (now composed from given/family or "Unnamed guardian/student"), suspend→reactivate copy now states that grants stay revoked and points to Change profile, and the notification unread chip contrast was raised to 6.6:1 (axe: 0 violations on eight identity routes). Browser evidence: profile-change round trip on the second administrator, suspend/reactivate/reactivate-recovery on `test.principal`, legacy adoption on `Dev Principal`, guardian link request→administrator rejection (`LINK-2026-E708EE`) + restrict/restore and module toggle, settings save→independent approval (`SET` v4 effective), audit paging 50→100 and filtering by an action absent from the old list, invite provider-failure path, recovery/wrong-password/TOTP/invite/access-denied copy, 1440×900 and 390×844 no page overflow. Gates: `npm run typecheck`, `npm run lint`, `npm test` (1068 web tests · 125 files; 73 contract tests), `npm run build`, `git diff --check` all pass. **Blocking DB defect (needs 000109):** `public.in_app_notifications` carries a `notifications_touch` BEFORE UPDATE trigger calling `app.touch_updated_at()` but has no `updated_at` column, so `app.notifications_mark_read`/`notifications_mark_all` always fail (`record "new" has no field "updated_at"`) and the bell can never clear unread state; the UI already shows the honest inline error with retry. No migrations or DB changes were made in this pass.
- **Migrations applied 15 Sep (MCP, ledger repaired to file versions):** `000105_jobs_public_intake` (ownerless applications with contact identity, one optional profile photo, service-role-only submit + photo-intent commands, accountless outbox delivery), `000106_document_public_visibility_owner_guard` (public approval restricted to school documents in the RPC, register projection, and public-read policies), `000107_anonymous_rpc_grant_repairs` (default-PUBLIC EXECUTE revoked from internal import/export helpers; the anonymous app-function surface drops to the three deliberate projections), `000108_scale_hot_paths_and_register_retention` (import group index, `has_role` STABLE, guardian-contact index, register retention gate). Remote ledger now `000001–000108`; local `validate-db-local.sh` passes through `000108`.
- **Public job intake (owner requirement, 15 Sep):** `/apply/job/[slug]` is now a public three-step form with no sign-in, no document uploads, and one optional profile photo (JPEG/PNG/WebP ≤ 2 MiB, server byte verification). The intake route enforces same-origin, IP and per-email rate limits, honeypot, strict schemas, typed 4xx errors, and duplicate suppression; rejection emails carry the recorded decision reason. Verified live: anonymous browser flow without login, curl security probes (403/400/honeypot), a real shortlist/not-selected decision chain with reason, and confirmation/outcome emails delivered through the sandbox recipient.
- **Wave 4-5 loops (15 Sep):** results read model rebuilt (`app.results_entry_sheet_get` reads one sheet; the queue projects counts instead of full rosters/marks: list payload 11.76 MB → 46.5 KB for 80 sheets, detail explain 1,029 ms/119k buffers → 20 ms/1.5k buffers in a 4k-student scratch model); `jobsListStaffQueue` embeds only the latest version snapshot; public job intake verified live end-to-end (anonymous UI + Aside submission, immutable version/event/retention/audit/outbox, confirmation delivered through the accountless recipient path, shortlist/not-selected with a required reason, rejection email carrying the reason, honeypot/duplicate/cross-origin/anon-RPC probes refused, and a fixed gap so HR can see the linked profile photo). Identity/access pass fixed staff/non-staff list mixing, revoked-invitation labels, settings saved-by UUIDs, unset grading scheme masquerade, post-save draft visibility, maker self-approval, audit action filter/pagination, legacy profile adoption and WCAG chip contrast; imports/exports pass fixed CSV MIME detection and the scan-before-link race that stranded batches, paged outcomes and export requests, chunked parse storage, and confirmed commit idempotency. Public pass fixed window-driven hero copy, a silently failing concern form (now a retryable panel), hours/story consistency, policy metadata, canonical/noindex basics, 0 axe violations across 16 routes, and the CSS-module print selector that broke the production build. Portal finance pass fixed stale sibling-child money rows after a failed reload, print scoping, live receipt labelling, child-menu keyboard behavior and route titles.
- **Final verification sweep (15 Sep, overnight close):** with a single clean dev server, `000116`/`000117` were exercised live — a queue-failed export was released with "Recover generation" (one `generation_recovered` event, one recover-keyed job, idempotent replay refused) and then completed to a downloadable 115-row CSV; restricted-scope equivalence probes showed 0 mismatches between the legacy scope helpers and the new boxed predicates across admissions (parent + five child tables) and finance, with UI counts matching SQL for both staff profiles; anonymous REST probes stay 401/42501 and guardians see only their own child's records. New references carry 10-hex suffixes (`JOB-2026-D2CA9093FA`, `TTO-2026-D85B8C1327`) while legacy 6-character references still resolve. The manual walk covered 65 screenshots across the three portals with 0 console errors, and the three synchronization claims held: 8-A Mathematics 88/100 for Hiba matches the guardian view; exam date sheet v2 and the substitute override appear on the guardian timetable; anonymous job contact details and photo are visible only to staff.
- **Migrations applied 15 Sep (MCP, ledger repaired):** `000109_results_read_model_scale`, `000110_hot_path_repairs` (notification `updated_at` column restoring mark-read/mark-all, export keyset pagination repair, import commit skipped-count), `000111_support_intake_uuid_fix` (public concern intake could never insert because a uuid column received a text expression). Remote ledger now `000001–000111`; local `validate-db-local.sh` passes through `000111`; browser gates re-verified after journey updates (critical journeys ALL PASSED, local features 107/107, live route sweep 59/59 with 0 flagged).
- **Wave 6-7 loops (15 Sep, overnight):** exam date-sheet authoring added to the Principal timetable manager end-to-end (staged rows, validation with focus, publish v1 `ESV-2026-392807` then correction v2 `ESV-2026-85B90C`, guardian portal shows v2 and the substitute override); imports UI unblocked and paged (24 batches / 36 issues live, no overlap/drops, paging exercised with synthetic fixtures); export failure reasons render with timestamps and a new "Recover generation" action releases a queue-failed `generating` export; guardian link queues and the staff document projection are paged (document page payload 4.69 MiB → 30 KB on the 8k-row model) and the single-reference read is O(1); results/admissions/finance projections resolve staff scope once per statement (admissions queue 3k rows 362 ms → 15 ms, finance page 1.35 s → 7 ms on the scratch model; membership proven row-for-row); `app.is_staff_aal2` now fails closed for an `aal`-less claim; `app.new_ref` suffixes widened to 40 bits, removing the ~23% collision risk on a 3,000-row bulk insert. Accessibility/responsive sweep: 74 routes × axe 0 violations, 284 route×viewport checks 0 overflow, keyboard/focus 26/26, link crawl 259 pages 0 failures; fixes for `--faint` contrast regressions, focusable scroll regions, and job-form error focus. A closed notification panel had been intercepting pointer events across every staff/guardian page (a class display rule defeated the `hidden` attribute); fixed with `.panel[hidden] { display: none }`, which cleared five separate click-timeout journey failures. The demo vacancy deadlines were moved forward so demo applications remain submittable (one vacancy stays deliberately closed).
- **Migrations applied 15 Sep (MCP, ledger repaired):** `000112_import_export_read_repairs`, `000113_timetable_override_teaching_assignment`, `000114_remaining_hot_paths`, `000115_timetable_override_legacy_column`, `000116_export_recovery_and_scope_boxes`, `000117_reference_entropy`. Remote ledger now `000001–000117`; local `validate-db-local.sh` passes through `000117`; critical journeys ALL PASSED, local features 107/107, live route sweep 59/59 (0 flagged) on a clean single-dev-server run.
- **Password recovery for MFA accounts fixed (15 Sep):** a recovery link signs the staff account in at AAL1, and GoTrue refuses a password change with `insufficient_aal` when the account holds an authenticator factor. The reset form mislabelled that as an expired link. It now detects the MFA requirement, tells the user two-step verification is required, and routes through the existing TOTP challenge with `next=/sign-in/reset-password`; after the code the reset form completes. Verified live: the branch redirects honestly, a non-MFA recovery completed to `/sign-in?reset=complete`, the new password authenticated, and the test account was restored (`Fass@313`, factor removed, `FASS_TOTP_REQUIRED=false`). Unit tests cover both the MFA detour and the genuinely-expired-link copy.
- **Provider-ready document scanning (15 Sep):** `DOCUMENT_SCANNER_PROVIDER` selects `manual` (default; callback unchanged), `http` (raw-byte POST with bounded JSON status), or `clamav` (clamd INSTREAM over TCP). Unreachable providers keep documents `pending_scan` with a retried transient error; no document becomes ready without an explicit clean result. Unit tests include a live in-process clamd fake over real sockets; operator requirements are documented in `docs/VERCEL-READINESS.md` and `.env.example`. No provider credentials are configured, so staging remains on `manual`.
- **Resend domain `poshsaaz.live` added by owner:** sending enabled, verification pending the DNS records (DKIM TXT at `resend._domainkey`, SPF CNAMEs `rsend` and `send`, receiving MX at the root). Once DNS verifies, `EMAIL_FROM` moves from the sandbox `onboarding@resend.dev` to the school sender and real recipients stop failing with provider 403.
- **Staging cleanup (15 Sep):** verification fixtures removed where tables allow mutation: 19 export requests with their events/jobs/documents, 20 import batches with 682 rows, 25 issues, synthetic guardians/students/contacts/enrollments/links, 4 link fixtures, 3 synthetic notifications, 6 timetable overrides, and the three pre-autosave duplicate application drafts. Jobs, documents and support tickets that carry append-only events cannot be deleted by design (`app.block_mutation` protects `job_events`, `job_application_versions`, `document_processing_events`, `support_messages`, `support_events`); those fixtures and 34 orphaned storage objects remain and are listed in the cleanup report. All legitimate demo data (five guardian/child pairs, results, releases, timetable) is intact and `audit_events` was never touched.
- **Defects fixed during verification (12 Sep):** applicant autosave race created multiple drafts per journey (now serialized, one draft, live-checked `APP-2026-FA370F`); job draft resume failed on the to-one embed and timeline events were mislabelled (both fixed with tests); family `/portal/documents` served report cards tied to superseded releases (now withheld for families, still served to staff/audit); business-rule rejection mapped to a non-retryable conflict; staff invitation hash-token session handling (new `AuthHashHandler`, callback `next` preservation); reviewer document labels now use configured requirement labels.
- **Public job intake rebuilt to the owner requirement (15 Sep, supabase staging):** `/apply/job/[slug]` is now a public, account-free application with no document uploads and one optional profile photo. The `/apply` layout no longer gates the job route (the student journey keeps its own guard in `/apply/student/layout.tsx`), the client form is a three-step public flow (details · experience/qualification · review, photo and consent) with a honeypot and honest email-only success state, and `POST /api/careers/apply` + `photo-intent`/`photo-finalize` enforce same-origin, IP + contact rate limits, strict schemas, honeypot rejection, byte-level MIME/size verification, and typed SQL error mapping through the service-role-only `app.jobs_public_submit_application`/`app.jobs_public_photo_intent` commands in forward migration `000105_jobs_public_intake.sql`. Live evidence: an anonymous Chromium context reached the form (HTTP 200, no sign-in copy, no document fields, one optional photo input, 11/11 checks); cross-origin POSTs returned 403 and malformed/oversized photo declarations 400; **HR decision with reason verified end-to-end** — Principal shortlisted `JOB-2026-336A8C` (maker), Administrator recorded not-selected with a required reason (short reasons refused in the UI), and the outbox delivered both status emails to the Resend sandbox's single verified recipient on the first attempt with the exact recorded reason in the body (provider message IDs `1e93aea3…`/`a936e68b…`). Migration `000105` is **not applied remotely** (remote ledger ends at `000104`); it was validated locally by applying migrations `000001–000107` to a throwaway PostgreSQL 17 cluster and exercising the commands (anon refused, service-role submit creates application + immutable version + event + retention + outbox + audit exactly once, duplicate/version/closed guards raise typed errors, photo intent → finalize → link works, one-photo and non-image rules enforced, anonymous photo stays private/pending/null-uploader). Until the central process applies `000105`, live anonymous submission returns the honest retryable unavailable state and the worker falls back to the account-bound recipient projection.
- **Defects fixed during public-intake verification (15 Sep):** the `/apply` layout was silently forcing sign-in for the public job route (now scoped to `/apply/student`); a revoked role grant left the account's stored active-workspace preference pointing at the dead grant, so the staff context resolver denied the whole portal (now falls back to the first active grant with a regression test); and the outbox worker read the not-yet-applied `job_applications.applicant_email` column, which failed every account-bound job email on staging (now degrades to the legacy projection until the migration lands, with tests). Staff shortlist/interview note copy now states the note is included in the applicant's status email rather than "applicants only see the stage".
- **Signed-in copy/a11y cleanup (15 Sep, presentation only):** sentence-punctuation em dashes replaced with the V15 `·` separator across the Guardian portal, Principal/Administrator workspaces, shared shells, sign-in, and applicant copy (labels, help text, dialogs, toasts, validation/error copy); lone table placeholders, en-dash ranges, comments, and regex classes untouched. Route titles added for `/portal/support`, `/portal/documents`, and `/portal/notices` (`Support · Portal`, `Documents · Portal`, `Notices · Portal` layout metadata matching Profile/Security). Mobile axe repairs: `role="region"`/`aria-label`/`tabIndex` on the scrollable timetable, payment-register, reconciliation, and content tables, plus an accessible name on the staff brand link. Tests updated where they pinned copy (application form, document preview, results corrections) and a new `portal-titles` test guards the three route titles. Browser evidence (production build, demo mode): 28 routes × 1440/390/320 → 0 axe violations, 0 console errors, titles as expected; `scripts/accessibility-check.cjs` → 53 routes, no violations. Gates: `npm run typecheck`, `npm run lint`, `npm test` (web 1093 · 127 files; contracts 73), `npm run build`, `git diff --check` pass. Known leftovers: demo/fixture data still contains em dashes (notice titles, vacancy titles, recorded notes) and lone `—` table placeholders remain by design; two pre-existing 320px overflows on `/portal/profile` and `/portal/security` are outside this pass (390px is clean).
- **Staff-surfaces daily-user pass (15 Sep, supabase staging :3000, browser + live SQL):** verified the Principal timetable workspace (draft → validate → publish v6–v8, append-only change log, override record → stale-version conflict → recovery → revocation with retained history, mobile 390 clean), Administrator staff Documents (upload → scan-pending refusal with explanation → scan callback ready → public-register approval → anonymous `%PDF` download → withdrawal), the careers review chain (queue, detail, scorecard, shortlist → independent Administrator not-selected with a required reason and confirm dialog, reviewer-name resolution, 0 console errors), and Principal teaching records (create/assign/end with reasons and validation); the Administrator is correctly denied `/administrator/timetables` and `/administrator/academics/teachers` with named-action reasons. Fixed: the staff careers projection and detail never carried an anonymous applicant's contact identity (`jobsListStaffQueue` now selects `applicant_email`/`applicant_phone`, `mapServerJob` maps them, and the review panel renders Email/Phone), the live queue's "Reviewer" column showed the last recorded actor instead of the assignment state (now "Assigned reviewer"/—), a substitute override with a mistyped subject silently recorded the effective subject (now refused, with configuration datalists on the override fields), an override recorded with no period produced "No configured period starts at on …" (now a clear instruction), and a stale-version revoke failure left the form stuck (now reloads the list and rebinds the target so the retry succeeds). **Data/config gaps (report only):** every live substitute override fails with `substitute teacher assignment is outside the section/subject scope` because `app.timetable_save_override` validates `p_substitute_teacher_assignment_id` against the legacy `public.staff_assignments` table (0 rows), while the client sends the canonical `teaching_assignments` id; `public.exam_schedule_versions`/`exam_schedule_entries` are empty and the Exam date sheet panel offers no authoring control, so the disabled publish button is honest but no date sheet can be authored (the service already saves+publishes given entries); and the 8-A published timetable carries only Monday periods. Gates: `npm run typecheck`, `npm run lint`, `npm test` (web 1102 · 127 files; contracts 73), `git diff --check` pass.
- **Notification dismissal and bounded bell (15 Sep, local):** the shared bell (`NotificationBell`) gains per-row clear and a "Clear all" action with optimistic removal and rollback, a six-row visible cap with a "Show all N" expand control, and a bounded panel (header and expand control fixed, list scrolls) so a long account list can no longer run past the viewport. Forward migration `000118_notification_dismiss` adds `dismissed_at`, the partial active-recipient index, and the account-scoped `app.notifications_dismiss(uuid,int)` / `app.notifications_dismiss_all()` commands (recipient-scoped, version-guarded, read-implied, replay-idempotent, marks rather than deletes so the projection's idempotency key survives a replay); the list/unread projections exclude dismissed rows and the adapter/service expose `notifications.dismiss`/`notifications.dismissAll` with demo session state and Supabase RPC paths. Verified: `npm run typecheck`, `npm run lint`, `npm test` (web 1129 · 128 files; contracts 73), `npm run build`, `npm run check:icons`, `git diff --check`, and `sh scripts/validate-db-local.sh` through `000118` including new RPC-suite assertions (C10: version guard, replay idempotency, cross-account denial, dismiss-all counts, no row deletion). Browser evidence (demo adapter :3002, fresh Chromium): 4-row list, per-row dismiss 4→3, Clear all → empty state and 0-unread bell, bounded panel inside 1440×900 and 390×844, 0 console errors after reload; session-derived bell state now renders only after mount so SSR and the first client render agree (a reload-after-clear hydration mismatch found during verification is fixed). **Applied to staging 15 Sep (owner-authorized, MCP):** `000118_notification_dismiss`; remote checks confirm the `dismissed_at` column, the partial active-recipient index, both `app.notifications_dismiss*` functions, authenticated execute grants with anon denied, and the ledger row repaired to the file version (remote ledger now `000001–000118`; advisors surface only the existing account-scoped SECURITY DEFINER warning class). Also fixed a pre-existing uncommitted type-narrowing error in `StaffHomeWorkspace.tsx` that blocked `npm run typecheck`.
- **Document scanner provider abstraction (15 Sep, local):** the scan boundary selects `DOCUMENT_SCANNER_PROVIDER` (`manual` default · `http` · `clamav`) with per-provider readiness in `/api/health` and `/administrator/data/imports`. `manual` keeps the shared-secret callback contract (`POST /api/documents/[ref]/scan`, constant-time compare) and the optional metadata-only trigger stub; `http` POSTs the storage-attested bytes with metadata headers and maps a bounded JSON `{status}` response; `clamav` streams INSTREAM frames to a `clamd` TCP endpoint (`OK` → ready, `<signature> FOUND` → quarantined). Uploads still stop at `pending_scan`; an unreachable provider records a transient outbox error and retries, an oversized/malformed response is refused, and no path marks a document `ready` without an explicit provider result. Local evidence: live fake `clamd` over TCP (framing, OK, FOUND, timeout, oversized, refused), live local trigger stub, real callback route with the real secret, and the real route + supabase-js over a PostgREST-shaped HTTP fake; live probes on the running staging-configured dev server returned 401/401/400/404 with no mutation. Gates: `npm run typecheck`, `npm run lint`, `npm test` (web 1166 · 132 files; contracts 73), `git diff --check`. Honest limitation: no http or clamav provider has run against staging; the deployed environment still has no scanner secret and the manual callback stub remains the only provider in use.
- **Live staging limitations:** admission acceptance → enrollment conversion and duplicate-identity review are blocked by the draft fee schedule (finance parked; no approved schedule to settle an admission invoice); the public register works end-to-end but production needs the school's real non-sensitive documents uploaded through the new panel (the staging entry is a clearly fictional demo PDF); Resend sandbox delivers only to the Resend account owner's address (redacted) so other recipients fail provider-side; document scanning uses the local manual callback stub; local `/api/health` reports `degraded` until `/api/outbox` runs within five minutes (the Vercel cron `* * * * *` covers deployed environments); migrations `000105–000107` await central validation and application, so the anonymous-submit and anonymous-photo paths are proven locally (scratch PostgreSQL + route tests) but not yet live.

| Area | V15 UI | Local persistence | Cross-module integration | Manual journey | Providers |
|---|---|---|---|---|---|
| Tokens, controls, shared shells | IN PROGRESS | N/A | Context regression checks required | Fresh V15 comparison pending | N/A |
| Public pages and policies | IN PROGRESS | Content reads require verification | Publishing propagation pending | Fresh V15 pass pending | Not reverified |
| Identity, invitations, contexts | IN PROGRESS | Prior tests only | Role/link revocation pending | Real Auth distinct from demo | Not reverified |
| Admission and job applicants | IN PROGRESS | Prior tests only | Ownership, drafts, decisions pending | Fresh V15 pass pending | Upload/Auth not reverified |
| Guardian fees/results/timetable/documents/support | IN PROGRESS | Prior tests only | Exact child data parity pending | Fresh V15 pass pending | Not reverified |
| Principal operations and teaching records | IN PROGRESS | Prior tests only | Teaching names and result propagation need recheck | Fresh V15 pass pending | Not reverified |
| Administrator operations, access and configuration | IN PROGRESS | Prior tests only | Approval/context propagation pending | Fresh V15 pass pending | Not reverified |
| Imports and exports | IN PROGRESS | Partial implementation | Mapping/filter/artifact gaps require verification | Full file journeys pending | Not reverified |

### Route-level V15 acceptance checklist

Every unchecked row needs visual comparison against V15, actual interaction, recovery-state review, and narrow/wide viewport evidence. Shared staff suffixes apply under each authorized `/administrator` or `/principal` profile; a rewrite alone never grants access. Do not manufacture a Teacher/Student portal or turn prototype keys into new routes. `/system` in the prototype is design reference only — the application's internal review route remains `/ui-states`.

- [ ] Shared tokens, Button, StatusBadge, fields, tables, callouts and dialogs (V15 panel zero-outer-padding, single-dot badges, eight-point emblem).
- [x] PublicHeader/PublicFooter; AuthFrame; ApplicantShell; PortalShell; StaffShell; notification and context controls — UI-2 local acceptance 8 Sep: self-hosted 68-icon subset, WebKit + Chromium at 1280/1024/390/320, 1920–320 header/footer matrix, all drawers/popovers/focus/overflow checks; route-specific content acceptance remains in later rows.
- [x] `/` (V15 hero/service-rail/story/stages/life/dates/CTA composition) — UI-3 local acceptance 8 Sep: hero reveal d1–d5, V15 record card + StarOrn + stamp, Material-icon service rail, V15 facts ledger/story, V15 stages sec-head, no TrustRibbon, 1440/1024/390/320 zero overflow.
- [x] `/about` (V15 g34 school-record sidebar), `/academics`, `/school-life` — structural V15 pass 8 Sep: PageIntro→PageHero (kicker/title/lede, no folio/ornament), all 12 public pages verified at 390px; page-specific V15 composition detail continues in UI-4.
- [x] `/admissions`, `/admissions/apply` (V15 key-dates facts-ledger) — structural pass; detail continues.
- [x] `/notices`, `/notices/[slug]` (V15 notice-board sidebar + office panel) — structural pass; ledger inner scroll verified intentional; detail continues.
- [x] `/careers`, `/careers/[vacancySlug]` — structural pass; detail continues.
- [x] `/contact`, `/disclosure` — structural pass (ornament-rules removed); detail continues.
- [x] `/policies/privacy`, `/policies/accessibility`, `/policies/fees-and-refunds`, `/policies/terms` — structural pass; detail continues.
- [ ] `/sign-in`, `/sign-in/verify`, `/sign-in/recovery` (V15 how-recovery-works), `/sign-in/reset-password`.
- [ ] `/sign-in/staff`, `/sign-in/totp`, `/sign-in/invite`, `/register/applicant`.
- [ ] `/access-denied`, `/session-expired`, `/ui-states` (expanded to V15's 33 committed states).
- [ ] `/apply/student` (V15 wizard rail; all eight steps).
- [ ] `/apply/student/[applicationRef]`, `/apply/student/[applicationRef]/status` (changes, offers, payment, readiness).
- [ ] `/apply/job/[slug]`, `/apply/job/[slug]/status` (V15 wizard; all steps and withdrawal).
- [ ] `/portal` (V15 overview incl. result preview; all-child overview and context switching).
- [ ] `/portal/fees` (incl. V15 payment history + fee desk), `/portal/fees/[invoiceRef]`, `/portal/receipts/[receiptRef]`.
- [ ] `/portal/results`, `/portal/results/[publicationRef]`.
- [ ] `/portal/timetable`, `/portal/notices`, `/portal/documents`.
- [ ] `/portal/profile`, `/portal/security`, `/portal/support`, `/portal/link-child`.
- [ ] Staff home under both canonical profiles (profile-specific V15 heads, no user-facing "Staff" wording).
- [ ] Staff `/admissions`, `/admissions/[applicationRef]` under both profiles (V15 review detail incl. verification checklist/timeline).
- [ ] Staff `/careers`, `/careers/[applicationRef]` under both profiles (V15 scorecard detail).
- [ ] Staff `/finance`, `/finance/invoices`, `/finance/payments`, `/finance/reconciliation` under both profiles (V15 adjustment rules evidence).
- [ ] Staff `/results` (V15 `q-head5`/`q-row5` queue), `/results/[resultBatchRef]` (V15 entry progress); Principal `/results/[resultBatchRef]/entry`; Administrator entry denial.
- [ ] Principal `/timetables`, `/academics/teachers` (V15 assignment coverage).
- [ ] Staff `/notices`, `/content` (V15 publishing checklists/live pages), `/documents` under both profiles.
- [ ] Principal `/support`.
- [ ] Administrator `/users`, `/link-requests` (V15 how-verification-works), `/settings` (V15 change process), `/audit` (V15 domain filter).
- [ ] Administrator `/data/imports` (V15 recent imports), `/data/exports`, `/guardians` (honest placeholder).

Deferred: `/environment` and facility pages remain isolated demonstrators and are excluded from this acceptance gate. Guardian activation/campaign behavior behind the `/administrator/guardians` placeholder belongs to the integration backlog; mockups do not establish implementation.

### Feature-by-feature verification (8 September 2026, supabase mode, real data)

**Guardian portal (Dev Parent, 2 linked children):**
- **Fees** — VERIFIED: fee summary (Balance ₹0, Next due None, Concessions), ledger table (1 paid invoice INV-2026-F55369, ₹22,000 admission fee), All/Unpaid/Paid URL-navigable filter tabs, receipt detail page (₹ amount, reference, print), statement preview opens/closes, 0px overflow at 390px.
- **Timetable** — VERIFIED: 8-period day view (Morning assembly, Mathematics, Science, English, Break etc. with rooms), week view (8 rows × 16 cells), exam date sheet view (renders empty — no published date sheet in DB, data gap not a bug), 0px overflow at 390px.
- **Results** — VERIFIED: page renders with honest empty state (no published results in DB — data gap). Feature will display results once Principal enters and Administrator publishes.
- **Documents** — VERIFIED: page renders with empty state (no documents for this child — data gap). Preview infrastructure present.
- **Notices** — VERIFIED: expandable notice panels using `<details>` elements that open/close correctly, 0px overflow at 390px.
- **Child switcher** — VERIFIED: dropdown shows both children (Integration Child msi00dms, Class 8-A · 2026-27 · STU-2026-D63E94 and Integration Child mshzzlab), switching works, "Link another child" option available.

**Staff workspace (Dev Administrator, legacy multi-role):**
- **Users** — VERIFIED: 164 real staff accounts in table (name, role, email, status, MFA, last active), Invite staff button opens inline form with profile selection, Manage buttons per row. Mobile overflow FIXED (added `grid-template-columns: minmax(0, 1fr)` — table now scrolls in wrapper, 0px page overflow at 390px).
- **Guardian links** — VERIFIED: 18 claims with Approve/Reject buttons per claim, 0px overflow at 390px.
- **Settings** — VERIFIED: 7 configuration panels (access profile, academic year, admission window from/to, partial payments toggle), editable inputs, Save changes button, 0px overflow at 390px.
- **Audit** — VERIFIED: 100 real audit entries (timestamped, action-typed, actor-attributed: Login, MFA verified etc.), date-range filter tabs (Today/7 days/30 days). Mobile overflow FIXED (added `grid-template-columns: minmax(0, 1fr)` — table scrolls in wrapper, 0px page overflow at 390px).
- **Data imports** — VERIFIED: Create batch button present, upload/validate/commit step UI, 0px overflow at 390px.
- **Data exports** — VERIFIED: Request export button present, 0px overflow at 390px.

**Route authorization** — VERIFIED: Administrator correctly denied from Principal-only surfaces (Admissions, Careers, Finance, Results, Notices, Content) with role-switch options. Principal correctly denied from Administrator-only surfaces (Users, Settings, Audit). Both profiles have access to their own workspace surfaces and Documents.

**Fixed during testing:**
- Users page: 696px → 0px overflow at 390px (grid min-track fix)
- Audit page: 646px → 0px overflow at 390px (grid min-track fix)

Latest recovery run (after UI-0 through UI-4 + demo-marker removal, 8 September 2026): `npm run typecheck` and `npm run lint` passed; `npm test` passed 514 web tests in 63 files; `npm run build` generated 82 pages (down from 84 after removing `/ui-states` and `/environment` demo routes); `npm run check:icons` confirms the self-hosted 95KB Material Symbols subset matches all 69 source icons; `git diff --check` passed. Anti-slop rules (30 items) codified in AGENTS.md and UX-BLUEPRINT.md. Homepage fully V15. All public inner pages converted to V15 PageHero. **Demo-marker removal complete:** `.demo-badge`, `.demo-strip`, `.demo-notice-text`, `p:has(> .demo-badge)` all hidden via CSS kill-switch; `DemoNotice` and `ConceptNote` components return null; footer "Design prototype" line removed; About "Illustrative" chip removed; policy-page "concept copy"/"fictional demo data" copy replaced with neutral text; `/ui-states` and `/environment` routes removed and scripts updated. All 16 public pages verified CLEAN of 20 demo/concept markers at 1440px in supabase mode. Playwright WebKit parity confirmed. Route-specific V15 composition detail continues in UI-5..UI-9.

UI polish pass (10 September 2026, presentation only — no database, domain-service, or backend-logic change): unified scroll/hero motion to the V15 rise curve, removed the dead `ken-burns` photography keyframe, added a quiet editorial skeleton pulse (opacity only), added missing 0.15s ease transitions to queue rows/segments/tabs/icon buttons/upload/drawer links/footer links/disclosure summaries, smoothed button-press and input-focus transitions, added scrim/child-popover fades, fixed the missing active-route nav underline, and fixed 320px fit (header brand ellipsis, full-width toolbar search, stacked page-head actions, tighter ctx-bar) plus 44px touch targets for segments/icon buttons/child switcher. Gates on this host: `npm run typecheck` passed, `npm run lint` passed, `npm test` passed (514 web tests, 63 files), `npm run build` passed, `git diff --check` passed. (Host Node is 24.5.0; the Node 22 gate remains unverified.) V15 visual-acceptance rows above stay unchecked pending rendered narrow/wide comparison.

Responsive device-range audit (10 September 2026, presentation only): verified every `repeat()` grid and fixed-width pattern in module CSS has a 1023/719/479 fallback; fixed the invoice `.totals` 3-column band stacking to one column at ≤719px, the staff application-review detail rows (`10rem 1fr`) stacking at ≤719px, the OTP row fitting 320px (tighter gap + `11.5vw` clamp), applicant-header wordmark ellipsis safety at ≤479px, staff-home band/quick-link `minmax(0,1fr)` hardening, and notification-bell quiet transitions plus popover fade. Breakpoint coverage confirmed: 320/390 phones, 768–1023 tablets (drawer shells, collapsing grids), 1024–1440 laptop/desktop (max-width wraps), ≥1920 wide (1320px wrap, 17px base). Gates re-run: `npm run typecheck` passed, `npm run lint` passed, `npm test` passed (514 web tests, 63 files), `npm run build` passed, `git diff --check` passed. No live-device screenshot evidence taken in this environment; route-level rendered comparison at each viewport remains the open acceptance step.

S1 states/honesty/logic slice (10 September 2026, UI + domain-light logic, both portals): new shared `AsyncStates` kit (skeleton/empty/error) + `RetryButton`; `/administrator/guardians` honest placeholder route created (`app/staff/guardians`, guard `links.verify`, nav entry) so the agreed URL resolves instead of 404ing; staff payments receipt refs no longer jump to the guardian portal (staff receipt detail deferred to S3); reconciliation evidence without a method renders Unknown instead of fabricated Challan; invoice print copy corrected to describe browser print/Save-as-PDF; support send/reopen failures now surface with draft preserved + retry; link-requests load/mutation failures surface with retry (no more silent emptying); finance workspace ledger refresh failures surface stale-data warning + retry; notices/audit/result-batch server pages return error panels with retry instead of throwing; staff home wires a real results-moderation count, renders Open links instead of fabricated zeros, removes the dead payments field, and shows a retryable error banner; content pages/notices tables gain empty states. Gates: `npm run typecheck`, `npm run lint`, `npm test` (514/63), `npm run build`, `git diff --check`, `npm run check:icons` (69 icons) all passed. (Host Node 24.5.0; Node 22 gate unverified.) Users-page empty state was already present — audit misreported it, no change made there.

S2 responsive slice (10 September 2026, presentation-only, four parallel agents + central review): every breakpoint-less staff bundle now has 1023/719/479 handling — finance ×4, results ×3, notices/link-requests/imports/exports/audit/documents pages, staff home queues, teaching-record inline forms (stacked full-row editors at ≤719), timetable editor/manager reflow, imports wizard stepper scroll; dense tables (marks 720px, results 1000px, careers 880px, content 780px) keep floors and scroll inside `.table--scroll` with sticky identity columns (paper ground, divider, hover-tint parity, `overflow-wrap:anywhere` refs); 44px touch targets and full-width primaries at mobile; `minmax(0,1fr)` hardening throughout. Central review unified the sticky-column divider across both results tables. Full loop re-green: `typecheck`, `lint`, `test` (514/63), `build`, `diff --check`, `check:icons` (69). Rendered-device measurement still open (no browser in this environment).

S3 workflow-depth slice (10 September 2026, UI-only, five parallel agents + central review): admissions/careers review depth — decision ConfirmAction modals (object + transition + consequences + required reason, focus trap, Escape, return focus), version-compare panel from loaded timeline events, applicant-visible vs staff-internal separation, interview-and-panel display from loaded record fields (honest omissions stated in UI where the service retains no snapshots/outcomes); results depth — publish confirmation naming ref/exam/class/version/audience with success link to the immutable version, marks draft autosave state indicator (Saved/Unsaved from local save results only, no fabricated timestamps); content draft preview dialog in public typography (read-only, focus-returning); documents readiness/category filter chips from exposed states only; users grant-detail from loaded grant fields (grantor/scope/dates honestly omitted — not exposed by listUsers); exports XLSX visibly disabled as CSV-only + fixed-columns honesty note + signed-URL download retry; settings dead inputs converted to policy-locked with named reasons + aria linkage. New tests: async-states (7), retry-button (3), guardians-canonical-urls (4). Central loop green: `typecheck` (both invocations), `lint`, `test` (528 tests, 66 files), `build`, `diff --check`, `check:icons` (69). Agent-reported tsc errors in ApplicationReview.tsx did not reproduce centrally — both tsc invocations pass on the final tree.

S4 logic-hardening slice (10 September 2026, domain services + tests, four parallel agents + central review): admissions/careers — optimistic-concurrency versioning on all staff transitions (stale writes fail with actor/time), maker/checker self-denial enforced server-side (review→offer/waitlist/decline, shortlist→offer, scorecard→decision), applicant-timeline leak negatives, single-invoice double-accept + idempotent conversion guards; finance — create idempotency keys, webhook idempotent posting with stale-overpay revalidation, server-side self-approval denials (approve/post/refund), paise validation, guardian-vs-staff ledger parity, refresh-never-charges; academics/timetable — no self-publish chain (submitter≠approver≠publisher), immutable publication clones, correction-creates-version, guardian snapshot negatives, timetable publish blocked on fresh hard clashes, override/supersession history, no teacher accounts/grants; platform — REAL LEAK FIXED: requester support projection now strips private notes/assignee (support.ts + server-loaders `mine` scope mapped via mapRequesterSupportRow), content expiry validated/stored/enforced, users cross-profile + legacy-teacher + suspended grants refused with last-admin changeProfile guard, settings versioned effective-dating (future versions inert), audit/outbox/links behaviors locked with tests. Owner decisions flagged, NOT changed: requester-post prohibition (contradicts finance-actions post tests), adjustment idempotencyKey param (signature change), concurrent-convert server lock (needs DB transaction), revoke/suspend demo last-admin (contradicts seed expectations; server enforces). Central loop green: `typecheck`, `lint`, `test` (624 tests, 70 files — up 96), `build`, `diff-check`, `check:icons` (69). No migrations created or edited; remote remains frozen.

Owner-decision completion (10 September 2026, four flagged items implemented): (1) requester-post boundary documented on `postAdjustment`/`postRefund` and locked by a positive test — the bar is on self-approval, not execution of an approved decision; (2) demo adjustment idempotency added (session key `adjustment:<invoice>:l<ledger-state>:<hash>`) so an overlapping double-submit collapses to one request, a pending identical request replays, and a settled re-request still creates a new one; (3) concurrent enrollment conversion serialized in-process via a per-application lock so overlapping submits observe the first stored result (the Supabase path still needs the DB transaction — forward-migration item from `000061`); (4) last-administrator protection extended to demo `revokeRole` and `suspendAccount`, mirroring migration `000056` §5 (which already serializes `staff_profile_change` + `accounts_suspend`), with the shared guard now used by `changeProfile` too; eight revocation/suspension tests now seed a cover administrator first, matching production requirements. Gates: `typecheck`, `lint`, `test` (632 tests, 70 files — up 8), `build`, `diff --check`, `check:icons` (69).

Signed-in Supabase verification pass (10 September 2026, read-only against staging through the `:3005` dev server; demo test personas via `/api/auth/dev-sign-in` + dev MFA auto-elevation, no remote mutation, no migration, no credential use). **Route matrix:** 66 probes across Administrator, Principal, Guardian, student applicant, job applicant, public, and anonymous. Every protected route returned 200 with a session; anonymous `/portal` correctly 307s to `/sign-in?next=%2Fportal`; every dev-persona cross-profile route returned the honest `StaffRouteGuard` denial with a switch option (legacy multi-role accounts; the active-workspace cookie decides, and profile accounts redirect to their canonical prefix). **Read-projection sweep:** 43 adapter reads across all personas — users 164, pending links 18, active links 11, documents 2, export catalog 7, audit rows, timetable versions 5, teaching records 2, content 2, result batches 9, admissions queue 26, finance invoices 21 / attempts 21 / adjustments 23 / refunds 6 / reconciliation runs 4, guardian invoices 2 / receipts 2; applicant and support queues honestly empty. **Defect fixed:** `jobsListStaffQueue` selected `job_review_assignments.assigned_at` and `job_scorecards.created_by_account_id`, columns that do not exist in the canonical schema (PostgREST `42703`), so the entire careers staff queue failed with a 503 for every role; the select and `mapServerJob` now use `created_at` / `reviewer_account_id` and the queue returns 200 with an honest empty list. **Defect fixed:** public `/careers` rendered an empty "Open positions" heading with no message when no vacancies are published; it now renders the shared `EmptyState` with a next step. **Honest limitations:** dev personas (`ad@`/`pr@`) are legacy multi-role accounts, so profile-bundle and canonical-prefix behavior could not be exercised locally (`TEST_ACCOUNT_PASSWORD` absent from `.env.local`); client-hydrated surfaces were verified through the same read-only adapter operations the pages call, not through a rendered browser DOM; `/api/health` remains `degraded` because `CRON_SECRET`/`RESEND_WEBHOOK_SECRET` are unset and `DOCUMENT_SCANNER_URL/SECRET` are absent, and the outbox worker has never run in this environment — all provider-gate items for S6. Gates on this host: `typecheck`, `lint`, `test` (633 web tests, 70 files; 73 contract tests), `build`, `git diff --check` all passed. No file under `supabase/migrations` was created or edited; remote remains frozen.

Multi-agent plan-vs-implementation audit and defect-fix slice (10 September 2026, presentation + domain-service + one forward migration). Five read-only explorer audits (plan/status truth, V15 UI gaps, cross-module contracts, DB readiness, dead controls) found the following real defects, all fixed and regression-tested: (1) **offer acceptance was broken in Supabase mode** — `respondToOffer` read `serverAdmissionIds` before `serverApplicationByRef` populated it, so the first response in a fresh session always threw "Application offer not found"; resolution order now matches `requestChange`/`withdraw` with a cold-session regression test; (2) **public `/careers`** empty state (prior entry); (3) **guardian support send failed silently** (`SupportForm` had `try/finally` with no `catch`) — now surfaces an error panel, preserves the draft, and retries the same intent, with a component test; the consent copy is mode-aware instead of claiming "demo form" in Supabase mode; (4) **guardian link-child page contradicted its own backend** (copy said "claim reference", form collected name/DOB, adapter resolves a student reference) — the form now collects the office-issued student reference + relation, removes the hardcoded fictional guardian name, and surfaces failures with retry; (5) **users roster** load failure fabricated "No staff accounts" — now an error panel with retry and a stale-data note; (6) **settings** load failure left "Loading settings…" forever and the demo "Reset demo data" button was a no-op — now an error panel with retry, and the dead control is removed; (7) **staff home** rendered zero counts on first-load failure — counts now read unavailable with honest links; (8) **guardian results/publication** conflated fetch errors with "no report/not found" and the aggregate was mislabelled "Weighted aggregate · policy-controlled" while computed client-side — now error+retry, and the copy states it is the sum of published subject marks with no rank; (9) **ResultTable** hardcoded every correction as "the Urdu grade" — now a generic corrected-release note; (10) **timetable writes resolved legacy `staff_assignments`** — the adapter now resolves `teaching_assignments` first with legacy fallback so Principal-created assignments can be saved and published; (11) **import Resolve always failed** because the UI fabricated `rowId: "row-<n>"` while the adapter requires a uuid and `data_import_list_issues` returned no row identity — forward migration `000069_import_issue_row_identity.sql` redefines the projection to return `row_id`, the UI uses it, and batch-level issues are visibly disabled with a reason; the mapping step's stub is now honestly gated (template required; custom mapping unavailable); (12) **anti-slop** — every `✓` glyph replaced with the approved Material Symbols `check` icon (six components). Presentation: the staff results queue now uses the V15 `q-head5`/`q-row5` composition (verified rendered on `/principal/results` with 9 staging batches); publish/withdraw success copy no longer claims the guardian portal updates when no release exists, and the publish confirmation states that each student's release is assembled separately. **Local database integration evidence (plan.md step 3):** `sh scripts/validate-db-local.sh` passed end to end on native PostgreSQL 17 — migrations `000001–000069` applied from scratch with the RLS, RPC, consolidation, results/timetable-release, slice5, slice6, and phase11-local-repairs suites all green; 000066–000068 were preserved unedited. Gates after the slice: `typecheck`, `lint`, `test` (635 web tests, 71 files; 73 contract tests), `build`, `git diff --check`, `check:icons` (69) all passed. **Known gaps recorded, not yet built (superseded below where fixed):** guardian claim-based activation has no public flow; policy pages still read demo copy; UI-state gallery removed while `robots.ts` and the checklist still reference `/ui-states`; rendered-browser narrow/wide measurement still pending; import custom mapping, export field picker, and audit cursor pagination remain.

Workflow-completion slice II (10 September 2026, forward migration `000070` + services + UI + local SQL suites). (1) **Report release assembly shipped, closing the results dead-end.** Publishing a sheet writes per-subject snapshots, but families read `result_report_releases`; nothing assembled them. Migration `000070_report_release_assembly.sql` adds `app.results_report_release_candidates(sheet)` (per-roster-student JSON: latest non-withdrawn publication per subject for the sheet's academic year and term, plus the current release) and `app.results_report_release_publish_batch(sheet, student_ids, key)` (delegates per student to the existing immutable-release command, captures per-student failures, composes the idempotency key from student + publication-set hash, and inherits audit/outbox/supersession). Domain `resultsListReportReleaseCandidates`/`resultsPublishReportReleaseBatch`, registry ops `results.releaseCandidates`/`results.publishReleaseBatch`, service `listReleaseCandidates`/`publishReportReleases`, and a staff `ResultReleasePanel` on the batch detail page (per-student and bulk release, release state badges, skipped/failed reporting, role-gated) complete the workflow from publication to guardian visibility. The local results/timetable suite now asserts candidates, reviewer denial, batch release, idempotent replay, and published rows; the phase11 suite asserts privilege boundaries and SECURITY DEFINER. (2) **Notification deep links are audience-aware**: staff viewers never receive `/portal` links (finance targets go to the staff registers), family viewers never receive `/administrator` links, and enrollment updates carry no link for staff rather than a wrong one; tests updated. (3) **Staff receipt detail route** `/staff/finance/receipts/[receiptRef]` added with `finance.listAllReceipts` (RLS `staff_read_receipts`, the same projection the family ledger reads), `loadServerStaffReceipts`, `ReceiptPanel` class now optional, and payments-register receipt refs linked in live mode; verified live with real staging receipt `RCPT-2026-7FADEA` matching the guardian ledger. (4) **Public content honesty**: `/admissions` uses a published notice (or an honest "closed right now" panel) in live mode instead of a demo fixture, and the sitemap no longer hardcodes a fixture notice. **Honest limitation:** `000070` is local-only and unapplied to staging while remote mutations are frozen, so the live release panel correctly reports the RPC as unavailable until the migration is applied at the provider gate; the full release workflow is proven on native scratch PostgreSQL instead. Gates: `typecheck`, `lint`, `test` (638 web tests, 72 files; 73 contract tests), `build`, `git diff --check`, `check:icons` (69), and `sh scripts/validate-db-local.sh` (migrations `000001–000070` + all suites) all passed.

Feature-completion pass: admissions → enrollment → guardian visibility (10 September 2026, four read-only workflow audits + forward migrations `000071–000073`, local-only and unapplied remotely by owner instruction). The audits traced the applicant, staff, offer→invoice→payment→conversion→guardian, and test-coverage sides of the feature and found four defects that made the live path fail outright, plus several robustness gaps. **Fixed (SQL):** `000071_finance_payment_integrity.sql` — the sandbox post RPC rejected the application-created attempt (`finance_create_attempt_v2` then post always raised 'attempt exists but has no payment'), so no admission fee could ever post; the function now locks the invoice, denies a NULL owner, checks authorization before idempotent returns, scopes provider-txn replay to the invoice, verifies attempt invoice/status/amount, and adopts the succeeded attempt. `000072_guardian_link_capability_defaults.sql` — link approval, conversion, and claim activation never seeded `guardian_link_capabilities`, so a converted child appeared in the portal while every module denied access; a trigger seeds the five standard capabilities whenever a link becomes active, with a backfill. `000073_admissions_transition_integrity.sql` — `admissions_review_advance`/`request_changes` had no row lock or version check, the transition matrix rejected UI-legal actions (start review from changes-requested; decline from changes-requested/waitlisted), the applicant-visible offer/waitlist copy omitted the typed reason, and the offered fee was never persisted (applicant saw ₹0.00); the `_v2` transitions lock and version-check, the matrix matches the UI, event copies carry the reason, and the offer records the approved schedule total in conditions. **Fixed (app):** all six staff transitions now send the version the reviewer is looking at (optimistic concurrency actually enforced), the staff detail loader reads one non-draft row instead of the whole in-scope cohort, the applicant projection no longer selects `decided_by_account_id`, duplicate-review rows are flagged in the queue, applicants are routed to `/apply/student` after OTP verification instead of a denied `/portal`, the enrollment result branches honestly on `manual_review_required`/`rejected` instead of claiming success, document uploads expose a per-file scan-status check with real errors, first-run autosave waits until session/class/student/guardian exist instead of guaranteed failure, and a cold `?edit=REF` save always updates that application instead of creating a duplicate. **Evidence:** `sh scripts/validate-db-local.sh` passed with migrations `000001–000073` plus new assertions (attempt adoption created→processing→succeeded; five capabilities seeded by conversion; stale review-advance denied and versioned advance accepted; offer event carries the reason); `typecheck`, `lint`, `test` (639 web tests, 72 files; 73 contract tests), `build`, `git diff --check` all passed. **Still open in this feature (next passes):** staff review still renders fictional document rows instead of the real `admission_documents` projection; duplicate-review resolution has no adapter op/UI; version-compare uses timeline replay rather than stored snapshots; private staff notes are not captured; applicants cannot list/resume past applications; offer-response conflict has no reload-and-retry path; pre-conversion applicant receipt links still target the guardian portal; draft expiry/multi-tab versioning; demo adapter does not mirror the server separate-reviewer rule or staff audit/outbox; `finance_issue_admission_invoice` still ignores `fee_required`; refunds do not block conversion; admissions RLS negatives are thin. Remote remains frozen and none of `000061–000073` is applied.

Admissions feature pass II — staff review truth (10 September 2026, presentation + projection, no SQL). (1) The staff review rendered hard-coded fictional document rows in Supabase mode; `admissionListMine`/`admissionGetStaffApplication` now embed the real `admission_documents → documents` projection (requirement code, filename, category, scan status, MIME, size, finalized date) and the review renders the actual rows with a staff-readable scan state and an "Open file" link that goes through the audited `/api/documents/[ref]` delivery route (only when finalized and clean/ready; otherwise the state names why it is not downloadable). Demo mode keeps its fixture list for the demo. (2) Duplicate-identity review was a stuck state: `admissions_duplicate_review_resolve` existed in SQL but had no adapter op, service method, or UI, and duplicate rows showed as plain "Under review". Added `admissionResolveDuplicateReview` domain wrapper, `admissions.resolveDuplicateReview` adapter op, `resolveDuplicateReview` service method, real candidate/review projection (`admission_duplicate_reviews` + candidate student), a flagged queue row, and an approver-only review panel that records a reason and returns the application to review or declines it. Live read-only checks: `admissions.staffByRef` and the applicant `admissions.listMine` embeds answer 200 against staging; the staff queue returns 26 rows. Regression test added for the resolution payload (candidate/version/evidence); `typecheck`, `lint`, `test` (640 web tests, 72 files; 73 contract tests), `build`, `git diff --check` all passed.

Admissions feature pass III — snapshot compare and staff-private notes (10 September 2026, projection + service + UI, no SQL). (1) **Version compare reads the immutable snapshots.** `admission_application_versions(snapshot)` was already selected but discarded, so the compare panel replayed timeline events and told reviewers "prior field values are omitted". `mapServerApplication` now maps the snapshots oldest-first, and the panel renders a field-level compare between the earliest and latest submission: changed fields are marked "Changed" with the previous value struck through and the new value beside it, unchanged fields show the current value, and the fallback copy stays for demo or single-submission records. (2) **Private staff notes are captured and displayed.** All six staff transitions (`requestChange`, start review, move to assessment, offer, waitlist, decline) now accept an optional staff-only note and send it as `p_private_note` (previously always `null`); the review projection carries `action`, `visible_reason`, and `private_note`, and the staff-internal panel lists each recorded step with its applicant-visible reason and private note (or an honest "no private note recorded with this step"). Demo parity stores the note on the record's `staffReviews` and never in the applicant timeline. (3) Live read-only check: the staff review page rendered real review rows from staging (`reviewed · Applicant-visible reason: Documents verified · No private note recorded with this step`). New tests: mapper carries versions and staff reviews; demo private note stays on the record and out of the timeline. Gates: `typecheck`, `lint`, `test` (642 web tests, 72 files; 73 contract tests), `build`, `git diff --check` all passed. **Still open in this feature:** the reviewer row still shows the raw account UUID (no name join is readable under staff RLS); applicants cannot list/resume past applications; offer-response conflict has no reload-and-retry path; pre-conversion applicant receipt links still target the guardian portal; draft expiry/multi-tab versioning; demo does not mirror the server separate-reviewer rule; `finance_issue_admission_invoice` ignores `fee_required`; refunds do not block conversion; admissions RLS negatives are thin.

Admissions feature pass IV — applicant list/resume and conflict recovery (10 September 2026, service + UI, no SQL). (1) **Applicants can now find their work.** `admissionsService.listMyApplications()` was added (applicant-scope `admissions.listMine`; demo returns the session's own records), and `getApplication` no longer falls back to the role-gated staff queue. `/apply/student` now shows a chooser for signed-in applicants with existing records: draft and changes-requested applications offer "Continue application" (sets `?edit=REF` and mounts the form fresh), submitted-and-later applications link to their status page, and "Start a new application" clears the edit param; the chooser has loading, empty (straight to the form), and error-with-retry states. `?edit=REF` still opens directly. Demo mode keeps the single-form behavior. (2) **Public resume copy is mode-aware.** `/admissions/apply` only renders the browser-session resume card in demo mode; live mode says drafts are saved to the account and links to sign-in. (3) **Conflict recovery.** Applicant offer/withdraw failures now surface the server message and silently re-read the authoritative record so stale versions or competing responses cannot leave the panel out of date; staff decision conflicts close the confirm dialog, reload the record, and say the record is being refreshed before any retry. New tests: demo `listMyApplications`; Supabase contract asserts the applicant list uses `admissions.listMine` and never the staff queue. Gates: `typecheck`, `lint`, `test` (644 web tests, 72 files; 73 contract tests), `build`, `git diff --check` all passed; live checks confirm the public apply copy and the `/apply/student` chooser render. **Still open in this feature:** reviewer name resolution; pre-conversion applicant receipt links; draft expiry/multi-tab versioning; demo separate-reviewer parity; `fee_required` invoice semantics; refunds do not block conversion; admissions RLS negatives are thin.

Admissions feature pass V — reviewer names and applicant receipt links (10 September 2026, two parallel agents + central review; forward migration `000074`). (1) **Staff never see a raw account UUID.** Migration `000074_admission_reviewer_directory.sql` adds `app.admission_reviewer_directory(application_id)` returning `accountId`/`displayName` per reviewing officer, SECURITY DEFINER with the admissions staff-scope guard and anon/authenticated/`prosecdef` assertions in the local repairs suite. Domain, adapter (`admissions.reviewerDirectory`, applicationRef resolution), and service (`reviewerDirectory`, `{}` fallback) support it, and the review page fetches names after every record load and renders "Latest review · <action> · <name> · <time>" plus "Recorded by <name>" per staff note, falling back honestly to "Admissions office". The server page header's raw-UUID reviewer meta was replaced with the application version, and the live page now renders zero UUIDs in visible text (the earlier panel showed one). (2) **Applicants are never linked into the guardian portal before a guardian link exists.** `PayFlow` gained optional `receiptHref`/`feesHref`/`portalPending` props with defaults that preserve the guardian portal behavior exactly; `AdmissionFeeStep` passes `receiptHref={null}` and a status-page destination before conversion, renders an honest "receipt is recorded on the fee ledger" note instead of a portal receipt link, and switches to the guardian defaults only when `converted?.portalAvailable` is true. New tests: reviewer-directory contract test and `pay-flow-success-links.test.tsx` (guardian defaults, applicant variant has no `/portal` links and offers the status page, custom/empty hrefs respected). Evidence: `validate-db-local.sh` passed with migrations through `000074`; `typecheck`, `lint`, `test` (648 web tests, 73 files; 73 contract tests), `build`, `git diff --check` all passed. **Still open in this feature:** no applicant-facing receipt detail view (the receipt ref is recorded but only linkable after the portal opens); draft expiry/multi-tab versioning; demo separate-reviewer parity; `fee_required` invoice semantics; refunds do not block conversion; admissions RLS negatives are thin.

Admissions feature pass VI — robustness and parity, no new surfaces (10 September 2026, two parallel agents + central verification). (1) **Local SQL negatives and exactly-once contracts (tests only).** `scripts/validate-rpcs.sql` now proves a non-owner cannot respond to an offer, withdraw, save a draft, or convert an application; an unpaid accepted application cannot convert; a duplicate-review resolution requires the admissions approver role and a matching candidate id; and the sandbox payment retries leave exactly one payment, allocation, payment ledger entry, receipt, `Payment posted` audit row, and `email.receipt:` outbox event. `scripts/validate-rls.sql` now proves anon lacks SELECT on `admission_applications`/`admission_documents` and that support-officer and finance-officer sessions read zero admission applications, events, and documents. No product defect surfaced; nothing was weakened or renumbered. (2) **Demo/server maker-checker parity.** The demo service refused only the same-account self-approval; an account could offer an application that no separate reviewer had touched. `requireDifferentApprover` now requires a recorded reviewer from a different account before offer, waitlist, or decline, mirroring `admissions_decide_v2`; affected tests now record the maker step with one account and decide with another, and the old "offer without an actor" expectation asserts the reviewer-required rejection instead. (3) **Staff queue freshness.** In Supabase mode the queue keeps the SSR first paint and then silently re-reads once on mount, so a decision made in another tab appears; a failed refresh keeps the current rows and shows no banner. (4) **No false "not found" on a failed review refresh.** The review mount fetch now catches failures: with no initial record the existing load-error/retry UI appears; with a loaded record the review stays on screen. Evidence: `validate-db-local.sh` passed with all new assertions; `typecheck`, `lint`, `test` (649 web tests, 73 files; 73 contract tests), `build`, `git diff --check` all passed. No migration, route, or component was added; remote remains untouched. **Still open:** applicant-facing receipt detail view; draft expiry/multi-tab versioning (needs an owner retention/purge decision); `fee_required` invoice semantics (needs the no-fee enrollment UI decision); refunds do not block conversion (owner policy); reviewer names require migration `000074` to be applied before they resolve live (the UI falls back to "Admissions office" until then).

Student-data integration pass (10 September 2026, three read-only audits + fixes across existing surfaces; no new routes, screens, or migrations). The audits confirmed there is **no student register or staff student-detail screen in the blueprint, UX catalog, or V15 prototype**, so none was manufactured (AGENTS: do not create surfaces from prototype keys). Instead the real integration defects on existing admin/guardian surfaces were fixed. (1) **Guardian-visible bugs:** the results header rendered `[object Object]` because the grade-section object was interpolated directly; it now uses `gradeSectionLabel` (`ResultsPageClient.tsx`). The documents page's "School records" panel claimed an "Enrolment certificate" and "Student reference" were Ready/On file with no backing records; it now shows the two references in a facts ledger with an honest note that certificates appear in the register once published. (2) **Staff/guardian identity parity:** the staff link-requests queue rendered the internal student UUID as the child reference (`childAdmissionRef`); the link projection now carries the public `students.reference` and display name, and the queue renders the public reference (or omits the field), with the child shown by name. Live checks show zero UUIDs in visible text on `/portal/link-child`, `/portal/profile`, `/portal/results`, and `/staff/link-requests`. (3) **Security/robustness:** `context.family` now rejects non-guardian actors instead of exposing a family's children and enrollment projections; `results.listPublications` gained the same active-child guard as `results.listReleases`; family-context now maps the real `person_id` (was the student row id) and maps an inactive student to the contract's `withdrawn` status instead of hardcoding `active`; staff/result fallbacks that printed a raw student id now show "Student". (4) **Guardian pending requests:** a new `PendingLinkRequests` panel on `/portal/link-child` lists the signed-in guardian's own pending link requests (child name, public reference when present, relationship, requested date, Pending verification badge) with skeleton, honest empty, and error-with-retry states, scoped by the family context account id; four tests cover rendering, the no-empty-reference rule, the empty note, and retry recovery. Evidence: `typecheck`, `lint`, `test` (653 web tests, 74 files; 73 contract tests), `build`, `git diff --check` all passed; live read-only checks on the guardian and staff surfaces above. **Still open:** the applicant's post-enrollment student/enrollment references are not re-read after reload (needs a conversion-reference projection, migration `000075`+); link capability changes/restriction have services but no UI; two active-child stores (`account_context_preferences` vs `guardian_preferences`) remain; admission snapshot DOB/gender are not copied onto the permanent record; enrollment status/effective dates and `student_support_records` are persisted but unsurfaced.

Student-data pass II — applicant conversion references (10 September 2026, forward migration `000075` + service/loader enrichment). The applicant's status page showed the permanent student and enrollment references only in the live conversion result; after a reload the projection carried none (the conversion row is staff-only under RLS), so an enrolled applicant lost the references. Migration `000075_admission_enrollment_reference.sql` adds `app.admission_enrollment_reference(application_id)`: SECURITY DEFINER, gated to the owning applicant or authorized admissions staff, returning only the public `studentRef`, `enrollmentRef`, `linkRef`, and `matchedExisting` (null before conversion, never internal ids). Domain wrapper, adapter op (`admissions.enrollmentReference`, applicationRef resolution), and enrichment in both the client read (`getApplication`, only for Enrolled records missing refs) and the SSR loader mean the references now survive reload and never block a read on failure. Tests: the Supabase contract proves the enrichment fires for an enrolled record and does not fire for a non-enrolled one; the local RPC suite proves the owner reads the exact student/enrollment/link references after conversion and a non-owner is denied; the repairs suite asserts anon/authenticated privileges and `prosecdef`. Evidence: `validate-db-local.sh` passed with migrations through `000075`; `typecheck`, `lint`, `test` (655 web tests, 74 files; 73 contract tests), `build`, `git diff --check` all passed. Live enrichment requires applying `000075`; until then the reference block stays hidden exactly as before. **Still open:** link capability change/restriction UI; two active-child stores; snapshot DOB/gender not copied to the permanent record; enrollment status/dates and support records unsurfaced.

Guardian-link module controls pass (10 September 2026, UI + tests, no SQL). Per-link module capabilities (academics, finance, documents, notices, profile) were enforced server-side on every guardian read but had no staff UI, so the only staff option was full revoke. (1) **Staff editor:** each active link on `/staff/link-requests` now shows a "Modules: all / N of 5" summary and a Modules editor (Administrator `links.verify` only) listing the five modules with scope hints; saving calls the existing `changeLinkCapabilities` service and folds the returned link into the row without a reload; failures keep the selection and offer Try again; Escape closes and restores focus. The **profile capability is required and non-toggleable** because the family context loads the child record through it, removing it would break every module; the editor warns when no optional module is selected rather than blocking, and states that server authorization checks the set on every read. (2) **Guardian view:** `/portal/profile` now shows a "Portal modules" facts ledger for the active child (Sharing enabled / Not enabled per module) with an honest note that the school office sets it. (3) Tests: `link-capabilities-editor.test.tsx` (5 cases: summary, save payload + local update, failed save with retry, required-profile behavior, Escape focus return) and `profile-modules.test.tsx` (narrowed set renders enabled/disabled correctly). Evidence: `typecheck`, `lint`, `test` (661 web tests, 76 files; 73 contract tests), `build`, `git diff --check` all passed; live adapter read confirms `links.listActive` returns each link with its capability array and `/portal/profile` renders the module panel. **Live data note:** links created before migration `000072` have zero capability rows in staging (the trigger/backfill is unapplied), so their guardians' context cannot load until either `000072` is applied or staff set modules through the new editor, which now provides that manual repair path. **Still open:** restrict/un-restrict needs a restore path before UI; two active-child stores; snapshot DOB/gender copy; enrollment status/dates and support records unsurfaced.

Active-child store consolidation pass (10 September 2026, forward migration `000076` + one domain read). Two stores held the guardian's active child: `account_context_preferences` (written by `context_family_select`, also used for the staff workspace grant) and `guardian_preferences` (written by `guardian_switch_active_child`, audited and hardened by `000066`); the family portal read the first and ignored the second, so they could disagree. Migration `000076_active_child_store_consolidation.sql` makes `guardian_preferences` the single family store: `context_family_select` keeps its signature, active-link validation, optimistic version, and return shape but upserts the guardian-keyed row, and every existing selection is backfilled when the link is still active so no family's active child changes. `resolveFamilyContext` now reads `guardian_preferences` by the resolved guardian id (the HttpOnly selection cookie remains only a cache hint, revalidated against active links on every request); the staff workspace preference keeps using `account_context_preferences` for its role grant. Tests: the local RPC suite proves the family selection writes the guardian store and bumps its version through the command; `validate-db-local.sh` passed with migrations through `000076`; `typecheck`, `lint`, `test` (661 web tests, 76 files; 73 contract tests), `build`, `git diff --check` all passed; live read-only checks show `/portal` and `/portal/profile` still resolve child context against staging. **Still open:** restrict/un-restrict needs a restore command before UI; admission snapshot DOB/gender are not copied to the permanent record; enrollment status/effective dates and `student_support_records` remain unsurfaced.

Student-data pass III + link access completion (10 September 2026, forward migrations `000077–000078`, UI, tests). (1) **Admission snapshot identity onto the permanent record:** `000077_student_identity_snapshot.sql` adds `students.date_of_birth`/`gender` and an AFTER INSERT trigger on `enrollment_conversions` that copies the latest submitted snapshot's DOB/gender onto the student on both conversion branches, filling only null fields so the school record stays authoritative; a malformed DOB never aborts a conversion; existing converted students are backfilled. The local RPC suite now submits `"dob"/"gender"` in the snapshot and asserts the converted student carries them; `validate-db-local.sh` passed through `000077`. (2) **Enrollment facts surfaced:** the guardian profile's enrolment panel now shows the enrolment status and the enrolled-from date (with an end date when present) from the already-loaded family context. `student_support_records` remains unsurfaced deliberately: no canonical role policy or staff student-record surface exists to host sensitive support data, so building a UI would create an unowned permission surface. (3) **Guardian-link restrict/restore:** `000078_guardian_link_restore.sql` adds `app.links_restore` (restricted → active, reason required, versioned, audited, outbox) and aligns the whole link-decision family with the application matrix: `links_restrict`, `links_revoke`, and `links_capabilities_set` now require `system_administrator` instead of the legacy `support_officer` allowance, closing a real server-broader-than-UI authorization gap. Domain/registry/service wiring and the staff page gained a Restrict action on active links and a Restricted links section with Restore; restricted-queue load failures cannot blank the active queue; tests cover restrict, restore, denial, Escape, and retry. **Suite lesson:** a `set_config` inside an exception subtransaction rolls back with it, so the role claim is now set outside the block. Evidence: `validate-db-local.sh` passed with migrations through `000078`; `typecheck`, `lint`, `test` (666 web tests, 77 files; 73 contract tests), `build`, `git diff --check` all passed. **Still open:** `student_support_records` needs an owner policy decision and a staff student-record surface; revoke directly from a restricted row needs a version fallback in `revokeLink`; admission DOB/gender are stored but have no staff register to display them (guardian-facing display remains a future decision).

Content/notifications plan slices C1 and C4 (10 September 2026, `CONTENT-NOTICES-ALERTS-PLAN.md`; forward migration `000079`). **C1 public notice reads (blocker):** migration `000079_public_notice_audiences.sql` makes `content_save_draft_v2` and `content_publish_version_v2` write `notice_audiences` (public, or role/guardian for the family choice, never both; write-once, idempotent), backfills published/scheduled notices lacking an audience, and adds anon SELECT policies for published public notice items and their versions. `contentListPublic` now uses an anonymous-safe projection (no audience embed, since anon has no privilege on recipient definitions) and `loadServerPublicContent` filters to notices then to published public rows. Public `/notices` is now a server component that server-loads the snapshot and renders the existing interactive UI through `NoticesPageClient` (no authenticated adapter call for anonymous visitors); the detail page renders the shared error panel instead of throwing. `NoticePublisher` gained a Public/Family audience select threaded through create/edit (default public). Tests: SQL audience/backfill/anon read+deny assertions, service audience mapping, and a notices page render/empty/error test. **C4 notification depth:** `notificationsList` is paginated (default 25, max 50) and `notifications.unreadCount` returns an exact unread head count; the bell shows the true count, and a failed load or mark-read now renders an inline error with Try again (optimistic state restored on failure) instead of a silent catch; unknown notification targets map to a neutral `Update` kind instead of "Security". Owner decisions recorded: no per-kind notification preferences and no in-app fan-out for public notices. Evidence: `validate-db-local.sh` passed through `000079`; `typecheck`, `lint`, `test` (676 web tests, 78 files; 73 contract tests), `build`, `git diff --check` all passed. **Next in the plan:** C2 scheduled publish/expiry producer and worker, C5 alert strip, C3 downloads register, C6 managed landing content, C7 CRUD polish.

Content/notices/notifications/alerts plan execution C2–C7 (10 September 2026, `CONTENT-NOTICES-ALERTS-PLAN.md`; forward migrations `000080–000083`). **C2 scheduling/expiry:** `000080` recreates the latest publish command to enqueue delayed `content.publish` and `content.expire` events (idempotent keys, `next_attempt_at` held to the due time) and restores `content_expire_due` audit plus a `content.expired` event; the worker dispatches `content.publish` and now `content.expire`, keeps failures retryable, and the scheduler-only `content.expire` event is excluded from in-app projection. **C5 alerts:** new `AlertStrip` plus a pure `selectUrgentNotice` (newest urgent, published, unexpired) renders on the public home (above the hero, with `NoticeLine` excluding the same notice) and on the portal home (family notices); no acknowledgement, no fabricated alert, facility alerts stay an isolated demonstrator. **C3 downloads register:** `000081` widens the anon public-document policy to the real `clean`/`ready` scan states, adds the authenticated mirror, and exposes `visibility` on the staff projection; `contentListPublicDownloads` returns safe metadata only, the public notices page lists real approved documents (honest empty, badge only when rows exist), the audited `/api/documents/[ref]` route now serves anonymous public downloads under a single 404 for everything else, and staff can approve/withdraw public visibility (UI gated on `content.publish`, readiness refused with a reason, audited). **C6 managed public content:** home hero/services/story/life read published content pages (`home-hero`, `home-services`, `home-story`, `home-life`) per field with the current institutional copy as fallback; `UpcomingDates` now derives from published notices with an honest empty state (no fixture calendar); policy pages (`policy-privacy`, `policy-terms`, `policy-accessibility`, `policy-fees-refunds`) and `/disclosure` render managed bodies when present and keep current text otherwise, with no fabricated verification statuses. **C7 CRUD completion:** `000082` adds `notices.pinned`, writes pinned/review-due from version metadata, and adds `content_unpublish_v3` with a true idempotency key; the composer gains pin and review-due controls, the content page gains page scheduling with an inline datetime, and notices/content lists gained loading, honest empty, and error-with-retry states. A C7 follow-up closed the dead scheduled-page control: `000083` stores `content_items.scheduled_at`, sweeps due pages in `content_publish_due` (version append, audit, one event, replay no-op), and maps `scheduledForIso` to the staff row. Owner decisions recorded: no per-kind notification preferences, no in-app fan-out for public notices, urgent notice equals the sitewide alert strip with no acknowledgement, no role/section audience pickers yet, public documents require `public_approved` plus a clean/ready scan. Evidence: `validate-db-local.sh` passed with migrations through `000083`; `typecheck`, `lint`, `test` (736 web tests, 84 files; 73 contract tests), `build`, `git diff --check` all passed. **Still open:** role/section audience pickers; the school must author the managed home/policy pages (fallbacks render until then); DB-level public-document visibility is any-AAL2-staff policy while the UI gates `content.publish`; page `content.scheduled_published` events have no recipients and may retire in the worker; staging application of `000061–000083` and browser acceptance remain the provider/release gate.

Content plan close-out — server/UI authorization parity and worker honesty (10 September 2026, forward migration `000084`). (1) **Document public visibility now has a server command matching its UI.** `000084_document_visibility_command.sql` adds `app.documents_set_public_visibility(document, public, reason)`: SECURITY DEFINER, requires AAL2 plus `content_publisher`, locks the row, refuses quarantined/failed/unfinalized or retained documents, sets `public_approved`/`private`, and records an audit row with the reason; the domain wrapper calls the RPC instead of a direct table update, and a new database suite (`document-visibility-command.test.sql`) proves publisher allowed, finance-officer denied, quarantined refused, and the definer/grant surface; `validate-db-local.sh` runs it. (2) **Page scheduled-publish events are a clean no-op.** The worker now acknowledges `content.*` events targeting `content_item` with no resolved recipients as `delivered` with a `skipped:` provider id, so a scheduled page publication never retires as a no-recipient failure and never needs provider config; notice events keep the unchanged audience path and still fail permanently when no audience resolves (regression test added). Evidence: `validate-db-local.sh` passed with migrations through `000084` (`DOCUMENT VISIBILITY COMMAND PASSED`); `typecheck`, `lint`, `test` (743 web tests, 85 files; 73 contract tests), `build`, `git diff --check` all passed. The `CONTENT-NOTICES-ALERTS-PLAN.md` slices C1–C7 and both follow-ups are now implemented and locally verified; `000069–000084` remain unapplied SQL in `supabase/migrations/` for the owner, and staging/browser acceptance is the remaining provider/release gate.

Email/outbox hardening and browser journey acceptance (11 September 2026, forward migration `000085` + worker/service fixes + local browser suite; no remote mutation). (1) **Critical authorization hole closed.** `000085_outbox_authorization_hardening.sql` makes `app.claim_outbox`, `app.mark_outbox_delivered`, and `app.fail_outbox` executable by `service_role` only (revoked from `authenticated`/`anon`), and stops the publish command from enqueueing email for audience-less public notices (notice email now requires a role/guardian/student/grade-section/academic-year audience; public-only notices are in-app/sitewide only). `scripts/validate-db-local.sh` now stubs `auth.jwt()` from `request.jwt.claims` so the privilege suite can assert the anon/authenticated denial and service-role grant. (2) **Worker honesty and retry semantics** (`outbox-worker.ts`, `resend.ts`, `/api/health`): recipient-resolution query errors now fail transiently instead of permanently; only `@`-bearing contacts are treated as email addresses (phone-only contacts no longer poison a batch); zero resolved recipients now settles as a delivered skip for **all** email events (previously a permanent failure, including public notices); suppression and delivery-write errors throw transient instead of being swallowed; missing email configuration returns early from `processOutboxBatch` without claiming rows, so attempts are not burned by an unconfigured deployment; staff-invitation events no longer email the inviter; a transient provider bounce from the webhook records rank-15 retry with `deliveryFailureClass` instead of retiring terminal; `resend.ts` is `server-only`; `/api/health` reports email readiness through `providerEnvReadiness().email`. Regression tests updated for the zero-recipient skip and the config gate. (3) **Full local browser journey suite is green for the first time in this phase:** `node scripts/local-feature-journeys/index.cjs http://127.0.0.1:3007` → **105 passed, 0 failed** across identity, linking, admissions, finance, results/timetable, content/documents/support, and facility modules. Journey drift fixed: OTP helper now fills the six `Digit N` inputs and the "Verify and continue" action; access-denied needle; admissions "Reason shown to applicant" label; `role="tab"` selectors for results/timetable/finance filter tabs; portal document "PDF" dialog trigger; child switcher popover `menuitem`s; finance unpaid-filter assertion rewritten to check the selected tab and the paid invoice's removal (the page's filter heading is intentionally not rendered when rows exist, and "No invoices match this filter" is the real empty state). (4) **Real product defect fixed:** `TimetableWorkspace`'s day panel rendered the demo "today" schedule and override count for every selected weekday (`selectedPeriods`/`selectedOverrides` were computed but unused), so clicking Tuesday showed Monday's timetable under a "Tuesday" heading; the selected weekday now drives the table, the "Today" badge, the override count note ("applies today" / "on Tuesday"), and the override chips. Evidence: `npm run typecheck`, `npm run lint`, `npm test` (**753 web tests, 85 files; 73 contract tests**), `npm run build`, `git diff --check`, and `sh scripts/validate-db-local.sh` (migrations `000001–000085`; `DOCUMENT VISIBILITY COMMAND PASSED`; `ALL LOCAL DATABASE CHECKS PASSED`) all passed. **Environment note:** the demo acceptance server must run with both `FASS_DATA_ADAPTER=demo` and `NEXT_PUBLIC_FASS_DATA_ADAPTER=demo` and a fresh `apps/web/.next-dev`; a stale Turbopack cache served Supabase-mode client bundles and produced 503s from `/api/adapter` during one run. `000085` (like `000069–000084`) is local-only and unapplied; remote remains frozen, and real Resend delivery still requires `RESEND_API_KEY`/`RESEND_WEBHOOK_SECRET`.

Production-readiness night (11 September 2026, forward migrations `000086–000088` + app hardening + full local verification; no remote mutation; finance workflows parked by explicit owner instruction). (1) **Imports and exports completed end-to-end (migration `000086_import_export_completion.sql`).** The live pipeline was unable to upload or parse before this pass: the upload intent rejected the `data_import_batch` owner domain and `.csv` keys, `document_actor_allowed` had no import branch, validation never moved a batch past `uploaded`, issue resolution never cleared `resolved_at`/row status so a batch with any error could never commit, the commit read raw header keys while the UI sent camelCase (imported people were created as "Imported Student"), and the export worker allowlist did not match the catalog columns (selected columns rendered blank). Fixed with import-aware upload intent + finalize MIME boundary, `data_import_apply_validation`/`data_import_resolve_issue`/`data_import_finish_validation`/`data_import_get_batch`/`data_export_retry` commands, a bounded browser-safe CSV parser + pure row validator + header aliasing, a full demo pipeline (parse/scan/validate/resolve/commit with idempotency and version conflicts), worker catalog aliases + canonical row shaping + 1000-row pagination, and rebuilt staff workspaces (drag/drop, auto-refresh scan, retry upload, detected-header mapping, Accept/Fix/Skip, commit conflict reload, report recovery, reason-required cancel on imports; catalog-driven field selection, XLSX visibly disabled, generation polling, cancel + audited retry, expiry labelling, honest demo-disabled state on exports). (2) **Guardian portal robustness sweep (finance excluded).** Removed silent error→empty/fabricated reads in the academics and timetable services (failed reads now reject to the error state; no `new Date().toISOString()` or `new Date(0)` stand-ins; live "today" no longer comes from the demo clock); fixed permanent spinners and wrong-child mixing in the overview band, child rows, released-report view, timetable projection, documents, profile, and security surfaces; security no longer claims a hardcoded SMS factor; support uses the canonical contact; recovery copy names the real email path; a portal-level error boundary keeps the family shell; the notifications/documents read failure no longer redirects the whole portal to `/access-denied`; child switcher gains Escape/focus return. (3) **Security audit fixes (migration `000087_security_authorization_repairs.sql` + app).** Suspended accounts lose guardian scope at the database layer (`is_guardian`, `guardian_has_capability` require `user_accounts.status='active'`); public support intake is service-role only and its route now carries IP/contact rate limits and the admin client; admission idempotency records are no longer client-readable/writable; `admission_public_configuration` exposes only `planned`/`open` windows and the `effective` settings version; anonymous document reads go through the new `documents_public_register()` SECURITY DEFINER projection and `anon` loses table SELECT (no more storage keys/internal ids via PostgREST); suspension/reactivation also applies a provider-level ban (`setAccountProviderBan`); production security headers + CSP are set in `next.config.mjs`; auth cookies are Secure outside local development (HttpOnly remains an explicit deviation pending a server-mediated refresh design); `isSameOrigin` now treats browser fetch metadata as authoritative at all 12 mutating routes; the scheduler/scanner bearer comparisons use `timingSafeEqual`; the dead `serverAdapterCall` helper (latent SSRF/credential-forwarding risk) was removed; the committed test-account password was removed from `docs/TEST-ACCOUNTS.md`. (4) **Performance audit and quick wins (migration `000088_hot_path_indexes.sql`).** Request-scoped `cache()` collapses the homepage's five full public-content scans to one, dedupes the twice-read vacancies and result batch, and caches the public download register; the patient-facing results page no longer issues three `results.listReleases` reads per mount (server publications are consumed directly and terms derive from them); missing indexes added for the confirmed hot foreign keys and status/sort filters; the two guardian scope helpers are now `stable`. The audit recorded remaining unbounded list reads and RLS helper volatility as follow-ups rather than changing behavior overnight (results entry sheet list, users list, staff-home count reads; finance registers stay parked). (5) **Verification (all on the combined tree).** `npm run typecheck`, `npm run lint`, `npm test` (**829 web tests, 97 files; 73 contract tests**), `npm run build`, `git diff --check`, `npm run check:icons` (69), and `sh scripts/validate-db-local.sh` (migrations `000001–000088`, including the new import/export command suite and the outbox due-time scheduling suite) all pass. Browser evidence on the demo server (`:3007`): `scripts/critical-journeys.cjs` → **ALL JOURNEYS PASSED** (the earlier `/staff` drift and legacy selectors were repaired: popover child switcher, `.q-row5` result queue rows with inline Publish/Return confirms, "Showing published v2" needle, six-digit OTP); `scripts/local-feature-journeys/index.cjs` → **105 passed, 0 failed**; `scripts/accessibility-check.cjs` → **53 routes, 0 violations** (contrast token fixes, landmark context bar, heading-order repairs, sr-only action headers); `scripts/responsive-check.cjs` → **168 viewport×route pairs, 0 problems** (SVG sizing guard, shared scroll-wrapper containing-block fix, link-request row wrapping); focus smoke PASS; link crawl 65 routes / 114 hrefs, zero dead links; a production `next start` smoke returned 200 with the security headers on public routes. (6) **Open items:** finance workflows remain parked per owner instruction (the `/portal/fees` table measures clean as a side effect of the shared wrapper fix but was not re-verified); real Resend delivery, staging apply of `000061–000088`, and production deployment remain owner/provider-gated; `x-forwarded-for` rate-limit keys trust the platform proxy; the staging dump under `backups/` should move to encrypted off-repo storage; RLS helper volatility and the remaining unbounded list projections need a follow-up migration.

Remote apply, full synthetic-data reset, test identities, and live UI acceptance (11 September 2026, migrations `000089–000093` applied to staging, owner-authorized remote operations). (1) **Remote ledger verified:** `npx supabase migration list` shows `000001–000093` applied (000051 remains the repaired→reverted entry; `000089–000093` were pushed this session via the guarded CLI link to `jxegiamjcawdywqyutdz`). (2) **Full operational reset of the synthetic staging data (owner-approved):** every `public` table was truncated and `auth.users` emptied; structural configuration was reseeded by `scripts/seed-remote.mjs`, which surfaced and fixed four seeder defects — the `assessment_components` upsert could not target the case-insensitive expression index (now reads existing keys then inserts), `notice_audiences` re-inserted on every run (now checked first), and the migration-inserted `staff_access_profiles` / `staff_access_profile_roles` / `result_entry_officer` / `data_export_catalogs` reference rows were missing entirely. `scripts/seed-remote.mjs` now seeds the profile catalogue and profile-role bundles and deliberately seeds **no notices or pages** (school-authored content only); migration `000091` re-inserts the export catalogs idempotently after any reset. Seeded demo notices were purged from staging (only the acceptance test notice remains). A minimal school dataset was created for testing: two students in 8-A/9-C, a teacher with seven teaching assignments wired into the seeded Class 8-A timetable draft, and the three test identities — **Administrator `test.administrator@faizaam.example`** and **Principal `test.principal@faizaam.example`** (profile accounts created by `scripts/seed-test-accounts.mjs`; password supplied by the operator out-of-band, deliberately not committed) and **Guardian `p@faizaam.example`** (local development one-click account with two linked children). (3) **Live defects found by role-by-role acceptance and fixed:** `TimetableWorkspace`/manager (draft labelled "published", `Publish vN` promised the wrong version, unsorted version list, `timetable.saveDraft` resolver confused teaching vs legacy refs and UUIDs, PGRST201 ambiguous `timetable_overrides` embeds breaking `listOverrides`, guardian RLS hiding teacher names and leaking raw assignment UUIDs — fixed with SECURITY DEFINER projections `000089_timetable_teacher_names.sql` + `000090_teaching_assignment_teacher_names.sql` and hardened mappers); `settings` page permanently invalid on a fresh database ("Not configured" in an email input, `0` under a min-1 number input) so **Save silently never submitted** — now empty-when-unconfigured plus an invalid-event message; `audit` rows stored the raw account UUID as the target and "authenticated" as the actor — `000093_audit_labels.sql` resolves both going forward (history stays as recorded; `audit_events` is append-only); the `data_export_request` and `guardian_claim_accept` commands had **duplicate overloads** whose defaults made short-argument calls fail with `42725 function is not unique` (export requests returned 503) — `000092_retire_stale_overloads.sql` drops the stale signatures; portal overview said "No active notices" while listing notices existed (pinned-only filter) — now lists latest notices, urgent/pinned first; fees unpaid-filter empty copy claimed everything was settled when no invoices existed; support quoted conflicting response times; careers header showed a dangling "nearest deadline —"; the content register showed raw lowercase status values; the documents scope note credited "Admissions officer" while the Principal was signed in; the notice register offered "Unpublish" for a scheduled notice (now "Cancel schedule"); and the users/link-request copy had two confusing instructions fixed. (4) **Manual UI acceptance via the Aside browser (real Chrome, admin/principal/guardian):** 40 routes walked with full-page screenshots, DOM overflow/clipping/overlap checks, raw-UUID sweeps, non-destructive control testing, and text review; every confirmed defect above was fixed and re-verified live (greeting now follows Asia/Kolkata, e.g. "Good evening"; portal overview lists the published notice; content status Title case; documents shows the active profile; users copy correct). (5) **Live service acceptance results:** notice draft→review→publish→guardian/public visibility passes; guardian support submit→principal respond+resolve→guardian tracker reply passes; timetable draft→publish→guardian v4 rows passes with real teacher names and no UUIDs; export request creates `EXP-2026-…` (worker remains provider-gated); import upload creates `IMP-2026-…` with document finalize `pending_scan` (document scanner provider unset, honest state); users suspend blocks sign-in and reactivate restores; settings save creates draft `SET-2026-…`. (6) **Gates:** `npm run typecheck`, `npm run lint`, `npm test` (**830 web tests, 97 files; 73 contract tests**), `npm run build`, `npm run check:icons`, `sh scripts/validate-db-local.sh` (`000001–000093`, all suites) pass; the 59-route live route scan on `:3000` reports **0 flagged routes**. (7) **Open items:** finance workflows remain parked (the fees unpaid-copy fix was copy-only); **no result-batch creation surface exists in the UI** — `results_entry_sheet_create` is SQL-only, so a fresh deployment cannot produce results without an import/seeding step; the notices/content registers scroll horizontally at 1440px with eleven columns (usable but worth a column-priority pass); `DOCUMENT_SCANNER_URL/SECRET`, `CRON_SECRET`, and `RESEND_WEBHOOK_SECRET` remain unset so scanning, scheduled outbox work, and webhook verification are provider-gated; production deployment is not authorized.

Real-data operations, invited principal, and import/link pipeline repair (11 September 2026, migrations `000096–000099` applied to staging). (1) **Clean sign-in surfaces.** The local-development banner and the one-click “LOCAL DEVELOPMENT ACCOUNTS” switcher were removed from `/sign-in`, `/sign-in/staff`, and `/access-denied`; `DevelopmentAccountSwitcher` was deleted and the family password-mode status text neutralised. The development sign-in API remains available for automation only. (2) **A second Principal account was created through the real invitation flow.** Administrator invited the Resend account owner's address (redacted) as Principal; Supabase Auth SMTP delivered the invitation, the invitee accepted at `/sign-in/invite`, and the new account signs in to `/principal`. Two provider-path defects were found and recorded: the emailed link’s `redirect_to` fell back to `SITE_URL` (the app root) so the callback never carried the invitation (`APP_URL` is now the run origin for further testing), and the invite token read `otp_expired` on the second open — a fresh-invite retest is pending. A same-password `updateUser` edge on the accept page was also worked around during completion. (3) **Supabase-mode imports were completely stalled and are now end-to-end.** Migration `000096` enqueues the `data_import_parse` job for service-driven `scanning` transitions (the document-scan trigger ran without a user session, so the old `auth.uid()` guard never queued the job); migration `000097` lets `data_import_record_scan` be called by the provider worker (it required an interactive AAL2 administrator and failed with "authenticated actor required"); migration `000098` orders the group-atomic commit by dependency (students/guardians before relationships/enrollments) instead of source-key order, which had failed relationship rows whose keys sort before student keys. A local scanner callback (private store → `documents/…/scan` with the scanner bearer) plus the guarded `/api/outbox` worker completed the pipeline: the “Family register CSV” import created students Imran Khan (`STU-2026-8020FC`, 8-A) and Ayesha Khan (`STU-2026-96019A`, 9-C) with active enrollments and student numbers, and a second batch committed guardian-student links for Rukhsana Khan (Mother, `imported_record`). (4) **Guardian link requests were broken for unlinked students and are fixed.** The adapter resolved the office-issued student reference under guardian RLS, which cannot see an unlinked student, so every first request returned 404. Migration `000099` adds `app.student_reference_lookup` (SECURITY DEFINER, active guardian or AAL2 staff only) and the registry resolves `links.request` through it. Live proof: Test Guardian requested `LINK-2026-611BC1` for Imran Khan → Administrator approved → the portal shows **Your children (3)**. (5) **School data added through the UI:** teacher “Mohammad Wani” + Class 8-A Mathematics assignment via the teaching-records workspace; Class 8-A timetable v5 published with Mohammad Wani (guardian portal shows the new teacher and version); student identity details (DOB, gender, school student numbers) filled for both families; leftover debugging batches cancelled through the import UI. (6) **Job applications no longer have a portal (owner instruction).** `/apply/job/[slug]/status` and its view were deleted, the dev job-applicant account was removed, and `JobForm` now ends in an inline “application submitted · updates arrive by email · no portal to check” confirmation; the status-change and offer email events remain in the outbox. Journeys and the link crawl were updated. (7) **Gates:** `npm run typecheck`, `npm run lint`, `npm test` (**842 web tests, 100 files; 73 contract tests**), `npm run build`, `npm run check:icons` (69), and `sh scripts/validate-db-local.sh` (`000001–000099`, all suites) pass; the 59-route live scan reports **0 flagged routes**. (8) **Open items:** the invited-principal email flow needs one fresh end-to-end retest after the `APP_URL` change; the Resend key shared in this session should be rotated; the local scanner is a manual callback stub (no real provider), and machine/worker secrets are set locally only; finance stays parked; production deployment is not authorized.

Issue-plan execution and robustness pass (11 September 2026, migrations `000094–000095` applied to staging, owner-authorized). (1) **P1 — results lifecycle completed (the largest functional gap).** A Principal can now create a result batch from the staff results workspace: migration `000094_results_exam_definition_list.sql` adds a SECURITY DEFINER exam-definition projection (AAL2 + result role + role-grant year/section/subject scope; only subjects with configured assessment components), the adapter gains `results.examDefinitions` and `results.createBatch` (wrapping the existing `results_entry_sheet_create` — server authorization, exact scope, idempotency, roster/component assembly unchanged), the service gains `listExamDefinitions`/`createBatch` (Supabase fails closed, demo reopens an existing open batch), and `ResultsBatches` gains a `results.enter`-only "New batch" panel (term/exam → class section → subject, busy/error/retry states, duplicate-submit guard, success announcement with the batch reference). **Full live chain proven on staging:** Principal created `RES-2026-28E330` (Class 8 · A · English, midterm) → entered marks (45) → submitted for moderation → Administrator approved → published `PUB-2026-F4ADBC` → released the student report `RPR-2026-204CE7` → Guardian `/portal/results` shows "English · 100 max · 45 obtained · 45.0%". Fixed a mapping defect this exposed: the released-report mapper preferred the component name over the subject, so the guardian saw "Midterm" instead of "English" (now the subject is the row, with the component appended only when a subject carries several components). (2) **P2 — provider-state honesty.** `/api/health` now probes the migration surface independently of missing provider variables, so a deployment with no scanner/cron/webhook secrets reports `readiness.migration: true` instead of conflating the two; imports are now a server shell around the workspace (`scannerConfigured` from `providerEnvReadiness()`), and when the scanner is unset the workspace stops its 4-second polling loop and states the operator action ("Document scanning is not configured… set DOCUMENT_SCANNER_URL and DOCUMENT_SCANNER_SECRET, then Refresh scan status"). (3) **P3 — register column priority.** The notices register and the content notice table now use container queries instead of viewport width: Owner/Review note/Review due collapse at ≤900px of the panel and Category/Pinned/Version at ≤700px, with the table min-width reduced to 620px, so the shared composer row no longer forces horizontal scrolling at 1440px while phones keep the scroll wrapper. (4) **P4 — data-layer stability (migration `000095_stable_authorization_helpers.sql`).** `has_any_role` (preserving the system-administrator stripping), `is_staff_aal2`, `is_pure_teacher`, `guardian_publication_allowed`, and `active_publication_ids` are re-declared `stable` with unchanged bodies so per-row RLS evaluation can be planned once per statement. (5) **P5 — housekeeping.** The staging dumps were moved out of the repository tree to `~/Desktop/FASS-backups/`; `docs/VERCEL-READINESS.md` now records the four missing provider variables with their exact degradation behaviour and the no-secret honesty contract. (6) **Verification:** `npm run typecheck`, `npm run lint`, `npm test` (**844 web tests, 100 files; 73 contract tests**), `npm run build`, `npm run check:icons` (69), and `sh scripts/validate-db-local.sh` (migrations `000001–000095`, including the new exam-definition scope assertions and the stable-helper `has_any_role` boundary) all pass; the 59-route live route scan reports 0 sustained issues (a single `/disclosure` dev-rebuild timeout re-verified as 200 with zero console errors). (7) **Remaining open items:** finance workflows stay parked; provider secrets (`DOCUMENT_SCANNER_URL/SECRET`, `CRON_SECRET`, `RESEND_WEBHOOK_SECRET`) still gate live scanning, scheduled outbox work, and webhook verification; the broad unbounded list projections (results sheet list, users list, staff-home counts) and the direct `exam_definitions` RLS policy for profile Principals remain follow-ups; production deployment is not authorized.


Remote migration application (11 September 2026, explicit owner instruction via Supabase MCP on project `jxegiamjcawdywqyutdz`). **All pending forward migrations `000066–000088` are now applied on staging.** Each file was executed in version order through the MCP `execute_sql` boundary (self-transactional files ran under their own `begin`/`commit`; the rest ran as atomic batches), and a `supabase_migrations.schema_migrations` row with the exact file version and name was recorded per migration, so the remote ledger is identical to a `supabase db push` result: `list_migrations` now reports `000001–000088` (the intentionally reverted `000051` remains absent, matching local). Post-apply verification confirmed the new surface exists on remote: 20 functions (`links_restore`, `data_import_get_batch`, `data_import_apply_validation`, `data_import_resolve_issue`, `data_import_finish_validation`, `data_export_retry`, `documents_set_public_visibility`, `documents_public_register`, `admission_reviewer_directory`, `admission_enrollment_reference`, `results_report_release_candidates`, `results_report_release_publish_batch`, `admissions_review_advance_v2`, `admissions_request_changes_v2`, `admissions_decide_v2`, `content_unpublish_v3`, `guardian_link_capabilities_default`, `enrollment_conversion_enrich_student`, `import_start_after_clean_scan`, `documents_link_attachment`), columns `notices.pinned`, `content_items.scheduled_at`, `students.date_of_birth`/`gender`, `guardian_claim_invitations.token_hash`, and the hot-path indexes including `guardian_claim_invitations_token_hash_uidx`, `guardian_student_links_status_created_idx`, `result_publications_status_published_idx`. The migration freeze applied to the exposed CLI database password; the owner granted MCP access explicitly for this application — no CLI credential was used. Security advisors re-run after apply: `rls_enabled_no_policy` on internal tables is the intended deny-all posture; `admission_public_configuration`/`documents_public_register`/`public_notice_ids` are intentionally anon-executable SECURITY DEFINER projections; the 221 `authenticated` SECURITY DEFINER findings are the designed command surface guarded internally by role/AAL2 checks. **Remaining health blockers (unchanged):** `CRON_SECRET`, `RESEND_WEBHOOK_SECRET`, `DOCUMENT_SCANNER_URL`, `DOCUMENT_SCANNER_SECRET` are still unset, so `/api/health` still reports degraded, and the outbox worker has not run on staging. Prior statements below that `000066–000088` were "unapplied" or that the ledger ends at `000060`/`000065` are superseded by this entry.

`git diff --check` passed in the prior run. Review groups at inspection: 212 UI/other tracked paths, 10 backend paths, 33 test/tooling paths, and 9 documentation paths. No changes were staged, discarded, or committed.

Node resolves to 24.5.0 on this host; the required Node 22 gate remains unverified. No Docker or remote provider test is used in this recovery checkpoint. Historical test counts below must not be reused as current results. Dev-server hygiene: three hung dev servers were replaced by one fresh demo-mode server (`FASS_DATA_ADAPTER=demo` env override; `.env.local` untouched) for deterministic UI acceptance.

---

## Historical status and implementation notes

Last updated: 6 September 2026
Current phase: **Phase 11 — complete the three-portal platform end to end.** Phase 10 recovery migrations `000056–000060` are live on staging, but the "Phase 10 complete" claim is **retracted**: green local gates do not prove end-to-end completion. Browser suites still use `/staff` and Teacher personas; imports do not upload or parse; guardian activation has no public flow; timetable still uses legacy `staff_assignments`; export UI cannot select fields, download, retry, or recover; child switching does not refresh all server projections; providers are unverified with real staging sessions.
Release state: staging backend migrated (ledger `000001–000060` live on remote project `jxegiamjcawdywqyutdz`); privileged test accounts suspended; journey residue cleaned. **Remote mutations frozen** pending credential rotation. Production does not exist. No Vercel/production action authorized.

### V14 design system adoption (6 September 2026) — SUPERSEDED by V15 (8 September 2026)

The canonical UI design prototype was **`V14 Faiz E Aam School Platform.html`** from 6 September until 8 September 2026, when the owner approved **V15** as canonical. The V15 adoption note and the current route-level V15 checklist are in the authoritative checkpoint above. The historical V14 documentation updates were:

- `design/UX-BLUEPRINT.md` — comprehensive V14 design system reference added: colour tokens (§2), component specifications (§12.1), responsive breakpoints (§16), screen catalog for all 67 screens (§17), role/route matrix (§18), navigation structure (§19), and implementation priority (§20).
- `AGENTS.md` — UI guardrails section updated with V14 token summary, component catalog reference, and responsive breakpoint reference.
- `design/README.md` — V14 prototype documented as canonical design artifact.
- `PROJECT-BLUEPRINT.md` — §11.1 visual direction updated with V14 tokens, typography, radii, layout, and component catalog reference.
- `UI-COMPLETION-PLAN.md`, `plan.md`, `FEATURE-INTEGRATION-SPEC.md`, `README.md` — V14 adoption notes added.

**Implementation rule (historical):** V14 was a visual/UX layer change only. The same rule applies to V15: do not change the database, domain services, or backend logic. Only update UI components, CSS modules, layouts, and page rendering to match the design system.

### P0 — Credential exposure (owner action required)

The Supabase CLI login role database password was printed in cleartext in a terminal session during Phase 10.8 dry-run operations. It must be treated as **compromised**:
1. Rotate or revoke the exposed database/login credential in the Supabase dashboard.
2. Revoke associated temporary CLI access.
3. Prove the old credential fails.
4. Ensure it is absent from Git, logs, plans, screenshots, and artifacts.
Until rotation is confirmed, **no further remote mutations are authorized**.

## Phase 11 progress (1 September 2026)

| Phase | State | Evidence |
|---|---|---|
| 11.0 truth reset | `IN PROGRESS` | Docs updated to retract "Phase 10 complete"; credential exposure flagged; containment script replaced with manifest-hash executor; all new corrections start at `000061`. |
| 11.1 canonical portals | `NOT STARTED` | Replace `/staff` URLs with `canonicalStaffUrl(profileCode, suffix)`; preserve URL/query through auth flows; remove Teacher branches; recoverable errors; strengthen `check:cutover`; rewrite browser/accessibility/link/responsive suites. |
| 11.2 teaching/timetable/results | `NOT STARTED` | `000061`: backfill `teaching_assignment_id`; make `teaching_assignments` authoritative for timetable; restrict result entry to Principal `result_entry_officer` + AAL2; independent Administrator moderation/publication; immutable report releases. |
| 11.3 CSV imports + uploads | `NOT STARTED` | `000062`: private source document required; strict state transitions; `data_import_parse` outbox handler; signed browser upload; Upload→Map→Validate→Resolve→Commit→Report UI; group-atomic commit. |
| 11.4 guardian activation + child sync | `VERIFIED` locally | `000063`: token-hash-only claims; eliminate student-reference activation (LinkChildForm now collects name+DOB); versioned contact-change; atomic child switching with dirty-form guard; `router.refresh()` + remount + reload all projections. All gates pass. |
| 11.5 exports/documents/operational | `VERIFIED` locally | `000064`–`000065`: export catalogs; cursor pagination; opaque artifact keys; signed download; data-health workspace (6 checks); recoverable error log; health readiness. All gates pass. |
| 11.6 staging acceptance | `IN PROGRESS` | Migrations `000061`–`000065` applied to staging; types regenerated from remote; advisor review completed (INFO/WARN only, no blocking ERROR); Teacher grants `ROLE-2026-5CB9B7` and `ROLE-2026-846E6C` retired (0 active). Remaining: TOTP config, Resend/SMTP, Storage buckets, real journeys with cleanup, restore rehearsal. |
| 11.7 Vercel/production | `NOT STARTED` | Vercel Preview from reviewed commit; Node 22.x; staging acceptance; Mumbai production project; migrations from zero; production build; post-deploy checks. |

## Phase 10 recovery progress (1 September 2026) — RETRACTED as "complete"

| Phase | State | Evidence |
|---|---|---|
| 10.0 containment | `VERIFIED` locally | No committed fallback password remains (repo-wide scan); `TEST_ACCOUNT_PASSWORD` required (min 16 chars, refused defaults); hard target guards on remote scripts (approved ref + `FASS_STAGING_CONFIRMED=true`); read-only `scripts/staging-residue-inventory.mjs` (IDs only). **Remote rotation/suspension and cleanup await explicit owner confirmation with exact IDs.** |
| 10.1 authorization repair | `VERIFIED` locally | `000056`: internal AAL2/role checks on `teaching_staff_list`, `legacy_teacher_access_report`, `data_import_preview/report`, `staff_profiles_list`; export completion service-only with non-null document; profile bundle-lock (all individual grants denied); `staff_profile_adopt` version-checked adoption; `pg_advisory_xact_lock` serializes last-admin checks in `staff_profile_change` + `accounts_suspend`; server-side `context_staff_select` denial for profile accounts. Actor-matrix tests in the consolidation suite. |
| 10.2 teaching/results | `VERIFIED` locally | `000057`: teaching staff are People without accounts (Person backfill, display names persisted, `person_id NOT NULL` restored); assignments restricted to non-login records; complete list projection; legacy Teacher write branch removed from result entry — only `result_entry_officer` + AAL2 + scope writes; publish enforces publisher ≠ entry actor. Entry-officer RLS read policies added for exam_definitions/assessment_components/rosters/marks (000025 had dropped the broad `is_staff_aal2` policies). |
| 10.3 import pipeline | `VERIFIED` locally | `000058`: strict state-transition matrix; group-atomic idempotent commit (family-key groups, per-group rollback, stored immutable result); digest check on confirmed counts; shared-contact flagging; `school_student_number` (scoped unique); teaching-assignment import implemented; preview/report converted to plpgsql (SECURITY DEFINER inlining fix). |
| 10.4 guardian claims | `VERIFIED` locally | `000059`: claim acceptance REQUIRES and verifies the one-time secret hash; provider binding is one-time (rebinding denied, same-subject retry idempotent); account reuse requires provider-subject match; per-channel verified contact; contact-change RPC fixed (invalid RETURNING removed); enrollment conversion reconciles guardian + grant on BOTH branches. |
| 10.5 portal sync | `VERIFIED` locally | FamilyContextProvider: generation counter (remount key), shared dirty-form registry (blocks switches), document metadata cleared on switch; results publications reload on generation bump; "Your children (N)" accessible overview rows. |
| 10.6 exports | `VERIFIED` locally | `000060`: per-domain filter/column allowlists enforced server-side; request idempotency; service-only generation lease; **worker handler implemented** (claim → bounded per-domain reads → formula-safe CSV → private document → mark_ready with document ID + checksum); `/api/health` proves the current consolidation surface (export/import/claim tables), not just `provider_jobs`. |
| 10.7 harness | `VERIFIED` locally | `scripts/validate-upgrade-paths.sh`: both upgrade baselines pass — `000001–000039 → latest` and `000001–000055 → latest` (smoke: no null `staff_members.person_id`, all consolidation tables present, 143 RLS tables). `scripts/check-node.cjs` runtime guard enforces Node 22 (currently **WARNING** mode: Node 24.5.0 active, Node 22 not installed on this machine — recorded as an environment limitation, not silently claimed). |
| 10.8 staging | `VERIFIED` (backend) | **Backup gate:** `pg_dump --data-only` of public + auth schemas (1.6 MB, 5059 lines, 187 auth users, 76 public tables) saved to `backups/staging-pre-phase10-20260901-225202.sql` before any mutation. **Containment:** 2 privileged test accounts suspended (banned ~100 years); 19 journey Auth users + 3 user_accounts + 3 people + 3 guardians + 3 links + 3 claims + 3 contacts deleted; 12 pending claim invitations deleted; post-cleanup inventory confirms 0 journey residue, 0 pending claims, 168 auth users total. **Migration apply:** `supabase db push --linked` applied `000056–000060` successfully; remote ledger now `000001–000060` (000051 skipped/repaired as 000052). **Remaining:** type regeneration from live DB, advisor object-by-object review, first-time TOTP dashboard fix, real AAL2 + email-claim journeys. |

**Local gate evidence (this session, exact outputs):** typecheck ✓ · lint 0 issues ✓ · 513 web + 73 contract tests ✓ · 84-page production build ✓ · cutover guard ✓ · 8/8 scratch DB suites ✓ · 2/2 upgrade-path baselines ✓.

**Remaining Phase 11 blockers (owner-gated):**
1. **Credential rotation** (P0): rotate the exposed database password in the Supabase dashboard; prove the old credential fails; ensure it is absent from all artifacts.
2. **Restore rehearsal**: restore the logical dump into an isolated database built from migrations `000001–000060`; compare table counts and critical references.
3. **Storage inventory**: inventory Storage metadata and objects separately; back up object bytes or record that buckets are empty.
4. First-time TOTP enrollment (`mfa_allow_low_aal=false` dashboard setting).
5. Real email OTP dispatch (approved SMTP/sender) for the public claim journey.
6. Node 22 installation for the strict runtime gate (Node 24 currently active with a recorded warning).
7. ~~Legacy Teacher grant retirement (`ROLE-2026-5CB9B7`, `ROLE-2026-846E6C`)~~ — **COMPLETED** 2 September 2026: both grants revoked (v2), 0 active teacher grants remain.
8. ~~Type regeneration from the live remote database (post-migration)~~ — **COMPLETED** 2 September 2026: types regenerated from staging after applying 000061–000065.
9. ~~Advisor object-by-object review of SECURITY DEFINER functions~~ — **COMPLETED** 2 September 2026: advisor review shows INFO (RLS no policy on service-only tables — intentional), WARN (search_path mutable on 3 import/export helper functions — low risk), 1 ERROR (SECURITY DEFINER view `app.timetable_period_teachers` — expected pattern for app schema). No blocking findings.
10. Real AAL2 staff/guardian journeys on the updated staging backend.

## Phase 10 recovery audit (31 August 2026)

The prior "staging verified" claim was **retracted**. Verified facts:

- **Staging exposure (P0):** two privileged test accounts (`test.administrator@faizaam.example`, `test.principal@faizaam.example`) use the committed fallback password `FaizAam-Test-2026`; 19 `journey.*` Auth users, 15 claim rows (12 pending), and associated synthetic records accumulated with no run ledger or cleanup.
- **Authorization gaps (P0):** `app.teaching_staff_list`, `app.legacy_teacher_access_report`, `app.data_import_preview`, `app.data_import_report` are SECURITY DEFINER with **no internal actor check** — any authenticated account can call them.
- **Guardian claim security (P0):** acceptance never verifies the stored `secret_hash`; the one-time token is dead security data. Contact-change RPC has an invalid `RETURNING reference` into a mismatched row type.
- **Unwired pipelines (P1):** import UI never uploads the selected file (batch created with `source_document_id = null`); no `data_import_parse`/`data_export_generate` worker handlers exist; `generateCsv` is an unused isolated utility; export "ready" was tested with a null artifact.
- **Teaching/results (P1):** teacher display names are discarded; timetable still reads/writes legacy `staff_assignments`; result entry still permits legacy Teacher RPC writes; publish rejects the same independent Administrator moderating+publishing, contradicting the approved model.
- **Guardian portal (P1):** child switch clears one context object but does not remount/reload server-rendered modules; results publications and document metadata can remain from the prior child; no dirty-form guard; `/portal/link-child` still resolves student-reference existence.
- **Verification honesty (P1):** staging journeys proved only AAL1 empty projections (not AAL2 success); claim evidence used service-role inserts and password sessions, not the public claim API; gates ran on Node 24, not required Node 22; no `000039→latest` upgrade harness; no remote backup evidence was recorded before applying migrations.
- **Docs conflict (P1):** status previously said both "staging verified" and "no remote action"; `AGENTS.md` still describes the pre-consolidation ledger; blueprint/spec retain stale checkpoints.

## Phase 9 staging evidence (31 August 2026) — historical, partially retracted

Applied to linked project `jxegiamjcawdywqyutdz` (FAIZ E AAM, eu-west-1) after explicit owner authorization. **Region exception:** the blueprint requires Mumbai (`ap-south-1`); the owner explicitly selected this Ireland project for staging only — it is not production-approved.

- **Migration ledger**: remote was exactly `000001–000039`; dry-run reviewed the exact `000040–000047` order; applied once; ledger now aligned local↔remote through `000055`. `000045b` was renamed `000047` (CLI filename pattern). `000051` was repaired→reverted per the CLI procedure after an in-place edit; the corrected function shipped as `000052`.
- **Hardening migrations shipped during advisor review**: `000048`/`000049` (18→0 unindexed FKs on consolidation tables; anon EXECUTE revoked on staff-facing helpers), `000050` (service-role EXECUTE for the provider boundary), `000052` (claim-dispatch authorization order + opaque-secret-key recognition), `000054` (service-worker audit/outbox allowance with explicit `service_worker` actor label).
- **Auth config**: `mfa_allow_low_aal=false` blocks FIRST-TIME TOTP enrollment (chicken-and-egg) — `BLOCKED` on a dashboard setting the API cannot change; owner must enable enrollment at AAL1 in Dashboard → Authentication → MFA. Leaked-password protection (HIBP) is `BLOCKED` on plan tier (HTTP 402).
- **Linked types regenerated** from the live database: all consolidation tables present, phantom `staff_invitation_assignments` gone, `role_definitions.is_assignable` present; typecheck green.
- **Advisor disposition — SUPERSEDED:** the blanket acceptance of 193 authenticated SECURITY DEFINER notices was invalid; several new read functions have no actor guard (see Phase 10 audit). Object-by-object review required.
- **Masked legacy teacher inventory (read-only)**: 2 active grants (`ROLE-2026-5CB9B7`, `ROLE-2026-846E6C`), neither account holds a guardian grant or other staff grants, 1 legacy assignment, 0 timetable/override references. Retirement is low-impact and remains gated on separate owner confirmation (`scripts/legacy-teacher-inventory.mjs`; retirement command `app.legacy_teacher_access_retire` is NOT invoked).
- **Real-session journeys**: 13/13 checks passed at the time, but the staff checks proved only AAL1 empty projections and the guardian claim used service-role fixtures + a password session — **not** the public claim API. Re-verification required in Phase 10.8.
- **No remote database backup/checkpoint was recorded** before applying migrations to a project with substantial records — a Phase 10 gate before any further migration.

## Current state

The three-portal consolidation is implemented and locally verified end to end through migration `000046`:

- **R0 repair** — two-profile contracts (`staff-profile-code`, `staff-access` with `STAFF_PROFILE_ROLES`), migration `000042` (profile catalog, profile invitations with immutable role snapshots, profile change with last-administrator guard, hardened `roles_grant`/`roles_revoke` refusing deprecated roles and bundle-breaking grants, Administrator-only guardian-link activation), canonical `/administrator|/principal` routing with legacy-redirect and query preservation, aggregate profile authorization in shell/home/guards, and removal of every Teacher login/dev entry point.
- **Phase 3** — `000043` non-login `teaching_assignments` (backfilled from legacy teacher assignments, provenance + source refs preserved), timetable `teaching_assignment_id` references (backfilled), result entry moved to `result_entry_officer` + `staff_scope_allowed` with legacy-teacher compatibility, one-independent-Administrator moderation+publication with actor-level no-self-approval, teaching-staff commands/workspace, masked legacy-access inventory and a version-checked retirement command (NOT invoked).
- **Phase 4** — `000044` import batches/rows/issues/mappings, `external_record_keys` provenance, `guardian_contacts` with delivery state separate from links, import state machine with idempotent group commit, bounded server-side CSV parser (XLSX blocked pending a vetted parser), Administrator import workspace (Upload → Validate → Commit → Report).
- **Phase 5** — `000045` guardian campaigns/claims (hashed single-use secrets, exact approved-link sets, provider binding, delivery attempts), transactional claim acceptance creating-or-reusing ONE account with one Guardian grant, contact-change requests with shared-contact review; `000045b` enrollment conversion now binds the guardian record, one Guardian grant, and the verified link in the same transaction with idempotent retry.
- **Phase 6** — Guardian Portal wording consolidation ("Parent portal" removed), family-level "Your children (N)" context, fail-closed child switching (stale context cleared immediately, previous authorized context restored on failure).
- **Phase 7** — `000046` purpose-bound export requests with allowlisted domains/filters/columns, provider-job generation (`data_export_generate`), 24-hour expiry, formula-safe bounded CSV generator (OWASP triggers neutralized + RFC 4180), Administrator exports workspace.

**Local verification (this working tree, demo adapter + scratch PostgreSQL 17):** typecheck ✓ · lint 0 issues ✓ · **513 web + 73 contract tests** ✓ · production build (84 static pages, 100 routes) ✓ · protected-path cutover guard ✓ · scratch validator applies `000001–000046` from zero and passes the RLS, RPC, staff-profile, slice-4 results/timetable, slice-5 operational, and slice-6 provider-job suites ("ALL LOCAL DATABASE CHECKS PASSED") ✓.

The demo adapter remains the default runtime. No remote Supabase, Storage, Resend, payment, or Vercel action was performed. Staging (Phase 9) and production are not verified.

| Area | Demo UI | Database/RPC | Application cutover | Live verification |
|---|---|---|---|---|
| Identity, invitations, contexts, staff profiles | `VERIFIED` | `VERIFIED` through `000042` (live) | `VERIFIED` locally | Journeys `VERIFIED` at AAL1 gate; AAL2 enrollment `BLOCKED` (dashboard setting) |
| Teaching records, results entry, timetable | `VERIFIED` | `VERIFIED` through `000043` (live) | `VERIFIED` locally | `NOT STARTED` (needs AAL2 sessions) |
| School-data imports and provenance | `VERIFIED` | `VERIFIED` through `000044` (live) | `VERIFIED` locally (CSV; XLSX blocked) | `NOT STARTED` |
| Guardian claims and enrollment binding | `VERIFIED` | `VERIFIED` through `000045`/`000047` (live) | `VERIFIED` locally | Claim journey `VERIFIED` (email path); real SMS `BLOCKED` (TRAI/DLT) |
| Protected exports | `VERIFIED` | `VERIFIED` through `000046` (live) | `VERIFIED` locally (CSV; XLSX blocked) | `NOT STARTED` |
| Admissions, careers, enrollment, uploads | `VERIFIED` | `VERIFIED` through `000041` (live) | `VERIFIED` locally; provider calls use fakes | `NOT STARTED` |
| Storage, PDF, Resend, outbox, cron, health | Demo/fake contracts `VERIFIED` | `VERIFIED` through `000030` (live) | Provider-ready; credentials/configuration `BLOCKED` | `NOT STARTED` |
| Vercel and production | N/A | N/A | Readiness config only | `NOT STARTED` |

## Source-control and rollback baseline

- Archive: `/Users/fin./Desktop/FASS-archive/fass-pre-change-20260806-142412.tar.gz` (source tree; `node_modules`, `.next`, and `package-lock.json` excluded).
- SHA-256: `3a1a97b064851c53242bce3fe0cd85315190dcfbc3b20934243849ee2631e0d4` (manifest: `fass-pre-change-20260806-142412.sha256` in the same directory).
- Git is now the review/rollback baseline. `origin/master` currently ends at `86676dc`; the working tree contains the local C0–C5 implementation and must be reviewed before commit.
- The archive is never edited; it remains an additional pre-Git rollback baseline.

The system is not yet a released school platform. Local code now covers all retained workflows and provider boundaries without Supabase-mode fixture fallback. Real Storage scanning, live Resend delivery/webhooks, a payment-gateway decision, staging sessions/advisors/restore proof, Vercel preview, and production deployment remain external gates.

The authoritative product and relationship target is `FEATURE-INTEGRATION-SPEC.md`. The frontend is an archived reference in `UI-COMPLETION-PLAN.md`; the only active execution order is `plan.md` C0–C5.

## Backend foundation and current cutover

The frontend handoff gate passed. `plan.md` is the active C0–C5 Supabase cutover plan; the B0–B8 sections retained there and below are historical architecture. The local foundation includes:

- **Version-controlled source established**: `git init` + baseline commit `4edf099` (the pre-change tarball archive remains the untouched rollback baseline).
- **Secrets hygiene**: `.env.example` corrected to names and placeholders only; real Supabase keys that had been copied into it were removed. Those exposed keys MUST be rotated (plan.md §3).
- **Pinning**: Node 22 (engines + `.nvmrc`), Supabase CLI `2.111.0` as a root devDependency (up from global 2.58.5), `@supabase/ssr@0.12.4`, `@supabase/supabase-js@2.112.2`.
- **Supabase workspace**: `supabase/config.toml` (Realtime and Edge Functions disabled per the v1 locks), foundation migration `000001_foundation.sql` (conventions, `app.new_ref` reference generation, `app.touch_updated_at`, append-only blocker, optimistic-version pattern documented; tables: `audit_events`, `outbox_events`, `idempotency_records`, `webhook_receipts`, `resend_webhook_events`, `rate_limit_buckets`, `job_runs`, `role_definitions`; RLS enabled with deny-by-default; SECURITY DEFINER helpers `record_audit`, `enqueue_outbox`, `claim_outbox` (SKIP LOCKED), `mark_outbox_delivered`, `fail_outbox` with exponential backoff), deterministic `seed.sql` (17 canonical role codes), pgTAP `tests/database/foundation.test.sql` (22 assertions).
- **Application interface**: `lib/supabase/env.ts` (adapter + env validation), `client.ts` (browser), `server.ts` (SSR, Next 15 cookie convention), `admin.ts` (secret-key, RLS-bypass restricted), `database.types.ts` placeholder for generated types; `db:reset` / `db:test` / `db:types` npm scripts.
- The original foundation gates are retained as historical evidence; current gate counts are recorded only in the validation table below.

**C0–C4 local implementation exit is verified**: migrations `000001–000041` apply from zero on scratch PostgreSQL 17. The RLS/RPC suites, Slice 4 results/timetable release suite, Slice 5 operational suite, and Slice 6 provider-job suite pass. The latest application gate is 477 web tests, 47 contract tests, typecheck, zero-warning lint, a green protected-path cutover guard, and a 79-route production build. Remote migration/advisor/provider verification remains intentionally deferred.

## Historical backend foundation evidence (B1–B6 architecture slices)

B1–B6 database and auth work below is retained as historical implementation evidence. Current readiness is governed by the matrix and C2 sequence above; historical live-project claims are not current live verification.

The C2.1–C2.4 local slices are complete: identity/configuration/context reads,
server-seeded family/staff providers, request-persisted context selection, staff
invitation acceptance UI/lifecycle, admissions→finance→enrollment,
results/timetable, careers/content/support/settings/audit/notifications/document
facades, adapter mismatch checks, and local route-denial/context contracts pass.
The next gate is C5 staging-only global adapter and provider verification.

### Committed remote foundation versus local-only migrations

**Verified read-only ledger facts (31 August 2026, via the Supabase management API):** the linked `FAIZ E AAM` project `jxegiamjcawdywqyutdz` is `ACTIVE_HEALTHY` on PostgreSQL 17 in `eu-west-1`, and its applied migration ledger is exactly `000001–000039`. Local `000040` and `000041` are committed but not live remotely; local `000042_staff_access_profiles.sql` is untracked, being redesigned for the two-profile (administrator/principal) model, and not live. The staff-access-profile, import, claim, and export tables are absent remotely. The remote project contains substantial records and must NOT be assumed disposable; no remote write, migration apply, or account/grant cleanup is authorized while the three-portal consolidation is in progress.

Migrations `000001–000015` are committed and were historically pushed to the linked `FAIZ E AAM` project `jxegiamjcawdywqyutdz`. The B2–B6 notes below describe migrations `000016–000030`, which the verified ledger shows were applied to the remote project; the earlier "local-only, apply after ledger re-read" wording in this subsection is historical.

- **B2 admissions/careers**: windows, applications, immutable versions, drafts, reviews, assessments, offers, events; vacancies, immutable terms, applications, review assignments, scorecards, interviews, events. Public read = published vacancies only. Role-scoped writes (admissions officer/approver, HR reviewer/approver) via new `app.has_any_role` helper.
- **B3 enrollment**: enrollments (partial unique: one active per student/year), idempotent conversion records, restricted support records.
- **B4 finance**: fee schedule versions/items (unique per schedule+code), invoices/items (append-only), concessions, append-only ledger, attempts (unique reference), gateway events (unique provider event id), payments (unique provider txn id), allocations (unique payment+invoice), receipts (unique reference), refund requests/refunds, reconciliation runs/exceptions. Guardian read = active links only; staff read-only at RLS — every finance write goes through domain RPCs.
- **B5 results/timetable**: exam definitions (unique year+section+term), components (unique exam+subject), grade band versions, batches, frozen rosters, immutable batch versions, marks (unique batch+roster+component), publications + per-student publication items (append-only, portal reads these only), correction requests, events, timetable versions/periods/publications/overrides, exam schedules. Teacher policies now require **aal2 + exact assignment scope** (class AND subject); pure-teacher sessions are bound to their assignment scope at RLS level (`app.is_pure_teacher`), operational staff keep queue reads; timetable/exam-schedule writes are manager-only.
- **B6 content/documents/support**: content items/versions (append-only), notices/audiences, documents + per-domain attachments + processing events, support requests/messages/private notes/events, in-app notifications, notification deliveries (unique event+recipient+channel+template), email suppressions. Anonymous visibility for published public-audience content only, via a SECURITY DEFINER helper (`app.public_notice_ids`) so recipient definitions stay private; owner-scoped document reads; support threads are requester-safe (private notes are staff-only).

RLS hardening found and fixed during review: anon notice policy leaked `notice_audiences` (fixed with the helper), teacher policies did not enforce aal2 (fixed), pure teachers could read the full staff queue (fixed with `is_pure_teacher`), and timetable/content/support writes were not role-scoped (fixed with `has_any_role`).

**Local validation**: `scripts/validate-db-local.sh` rebuilds the full schema on a scratch PostgreSQL 17 instance (GUC-based `auth.uid()`/`auth.jwt()` stubs), applies seed, and runs the positive/negative RLS, transactional RPC, results/timetable release, operational, and provider-job suites with fictional actors. It passes through migration `000030`.

**pgTAP suites** added for every slice (run via `supabase db test` in CI): foundation 22, school-config 16, identity-access 31, admissions-careers 79, students-enrollment 21, finance 54, results-timetable 59, content-documents 64 assertions.

**Seed and generated types (historical):** `supabase/seed.sql` and
`scripts/seed-remote.mjs` contain the B2–B6 synthetic configuration. Prior
remote application and type generation are historical; regenerate types only
after the staging migration ledger is re-read and reviewed.

### Historical app-layer integration (outbox worker + email + server domain services)

The following records what was implemented and historically exercised against a
linked project. Because live health and the migration ledger are not currently
re-verified, these entries do not make staging or production `VERIFIED`:

- **Outbox worker** (`lib/supabase/outbox-worker.ts` + `app/api/outbox/route.ts`): claims bounded batches via `app.claim_outbox` (SKIP LOCKED), resolves recipients from authoritative records, sends through injected provider contracts, and processes PDF/Storage jobs. Transient failures return to pending with exponential backoff; permanent failures remain visibly failed and cannot be marked delivered. Vercel Cron uses the `CRON_SECRET`-protected GET route.
- **Resend adapter + templates** (`lib/email/`): fetch-based REST adapter (no SDK), idempotency keys derived from delivery records, editorial shell matching the visual system, one template per plan §9 event kind (no marks/balances in subjects or bodies).
- **Verified webhook** (`app/api/email/webhook/route.ts`): official Svix `Webhook.verify` over the raw body, retryable `svix-id` receipts, monotonic delivery projection, and hashed bounce/complaint suppression.
- **Server domain layer** (`lib/supabase/domain.ts` + `rpc.ts`): every operation returns the `ServiceResult` envelope with canonical error codes; commands call the transactional `app` RPCs through a typed schema-scoped wrapper; reads are RLS-filtered. Ops: admissions (draft/submit/queue/review/decide/respond), finance (invoices/receipts/payment), enrollment conversion, results/timetable publish + lists, family/staff context resolution.
- **Adapter endpoint** (`app/api/adapter/route.ts`): session-protected operation dispatch with zod validation; client gateway `modules/services/adapter-client.ts` behind `FASS_DATA_ADAPTER=supabase` (demo remains the isolated fallback).
- **Project config**: `supabase/config.toml` exposes the private `app` schema to PostgREST (`[api] schemas = ["public", "app"]`) and enables TOTP MFA (`[auth.mfa.totp]`); historical push status is not a current staging verification.

**Historical integration evidence** (`scripts/integration-staging.mjs`, fictional run-scoped data): the prior run exercised real sessions, RLS, AAL2, admissions/finance/enrollment, results/timetable, denial cases, and outbox retries. It must be rerun after the remote ledger and provider configuration are verified; it is not current live evidence.

Historical remainder at the C2.4 checkpoint (now superseded by the current
matrix): global staging verification, provider configuration, the payment-gateway
decision, and production acceptance were still pending.

## Historical execution notes

The dated entries below preserve implementation history. They are not current
status evidence; use the status, validation table, feature matrix, and active
cutover list above for the next task.

### UI design quality pass (21 August 2026 session)

All four surfaces audited and fixed against `design/UX-BLUEPRINT.md` (public, family portal, staff workspace, applicant journeys). Highlights:

- **Public**: notices failure state + retry added; `/notices/[slug]` loading skeleton; disabled anchor navigation blocked; category filters announce active state (`aria-current`); grievance consent error association fixed.
- **Portal**: wrong-child "latest result" hard-code removed (derives from the active child's snapshot); numeric table columns right-aligned end-to-end; honest Due/Clear badge; two-step sign-out confirmation; empty states for documents/fees/notices; loaders replaced with editorial skeletons; tab touch targets raised to 44px.
- **Staff**: careers queue gained status filter tabs with live counts; dashboard counts made honest; required-reason controls now enforce minimum length inline (`role="alert"`, `aria-required`) across admissions/careers/results/link-requests/timetable/facility; numerics right-aligned in 11 tables; marks-entry busy buttons name their operation.
- **Applicant**: autosave indicator truthful ("Saving failed — retry" path); error-summary entries are labeled "Review this answer" anchors with `aria-label` carrying the message (fixes duplicate-text test failure); job-form rail announces `aria-current="step"`; offer acceptance names exact amount + deadline pre-commit; auth error param renders expired-link recovery; OTP resend cooldown with live-region announcements.

**Loop-test evidence (fresh production build, this session):** typecheck ✓ · lint 0 issues ✓ · 256 web + 47 contract tests ✓ · build 66 pages ✓ · **22/22 critical browser journeys PASS** · responsive sweep 168 viewport×route pairs at phone-s→desktop-xl NO PROBLEMS ✓ · accessibility scan 53 routes 0 violations ✓ · focus/keyboard smoke PASS ✓ · link crawl 65 routes / 91 hrefs, zero dead links ✓.

Finance integration note: `mapServerInvoice`/`invoiceSummary` hardened per new spec tests (34 mapper tests) — receipt references pair by original allocation slot, settled invoices never show residual balance, `paidPaise` counts allocation-backed payments only.

### B7 cutover progress (21 August 2026 session)


Work completed and gate-verified on this checkout (typecheck, lint, 222 web + 47 contract tests, 66-page build all pass):

- **Server command layer complete**: every existing DB RPC now has a typed domain wrapper and a zod-validated `/api/adapter` dispatch entry — `jobs.submit`, `jobs.decide`, `links.approve`, `links.reject`, `support.respond`, `content.publishNotice`, `results.submitMarks`, `results.moderate`, `results.withdraw`, `results.correctionRequest`, `finance.issueAdmissionInvoice` join the previous 18 ops (29 total). Authorization remains entirely inside the SECURITY DEFINER RPCs.
- **Finance ledger reads enriched**: `finance.listInvoices` now embeds `ledger_entries`, `payment_allocations→payments`, and receipts; `finance.listReceipts` embeds payment method/amount and invoice identity — enough for the client to compute portal/staff parity views from the append-only ledger.
- **PDF generation adapter implemented** (`lib/pdf/render.ts`, `lib/pdf/adapter.ts`): dependency-free valid-PDF writer (Helvetica, deterministic output), receipt template (`receipt-v1`) with INR grouping and IST timestamps, report-card template ready; outbox worker now renders `pdf.generate` receipt events FROM stored records, records a private `documents` row with reserved `object_key` plus append-only processing events, and returns delivered/transient/permanent outcomes. Migration `000016_generated_documents.sql` makes `uploaded_by_account_id` nullable for server-generated documents (NOT yet pushed; regenerate types after apply). Six new renderer tests pass.
- **First client cutover wired**: `financeService` read paths (`listInvoices`, `listAllInvoices`, `getInvoice`, `listReceipts`, `getReceipt`) route through `adapterCall` when `FASS_DATA_ADAPTER=supabase`, mapping server rows into the unchanged domain shapes via `finance-server-map.ts`. Demo mode is byte-identical; no dual-write. Write paths (attempt lifecycle, `confirmSuccess`) still need server order-create/refresh ops before they can leave the demo path.
- **UI fix**: the application form's destructive "Start over" replaced `window.confirm()` with an accessible inline confirmation panel (safe default focused, Escape cancels).

Remaining for B7 (honest gaps): client wiring for admissions/careers/results/timetable/family-context/staff-context/content/support facades; server ops for payment-attempt create/refresh and single-invoice/receipt detail; school-config label→UUID resolution for draft creation; staff invite + TOTP enrollment flow; removal of operational sessionStorage once facades cut over.

### Staff identity lifecycle (21 August 2026 session, later)

The staff chain invite → account → role grant (+scopes) → assignment is now complete server-side:

- **Migration `000017_staff_identity_commands.sql`** (authored, NOT yet pushed — execute via `supabase db push` or manually): six SECURITY DEFINER commands in exact house style — `roles_grant` (system_administrator+aal2, duplicate-active-grant guard, scope rows validated against FKs, audit + `security.role_granted` outbox), `roles_revoke` (version-checked, revalidation trigger ends access next request), `assignments_create` (grant must belong to the staff member's account; grade section must belong to the academic year; duplicate-scope guard), `assignments_end`, `invites_create` (stores only sha256 of a 192-bit one-time reference; plaintext returned once to the inviting admin), `invites_revoke`. Reads stay RLS-based (`users.list`) — no read RPCs.
- **Domain + dispatch**: typed wrappers (`rolesGrant/Revoke`, `assignmentsCreate/End`, `invitesCreate/Revoke`, `usersListAdmin`) and eight zod-validated adapter ops (`users.*`, `assignments.*`, `invites.*`). Typecheck/lint green.
- **TOTP gate live**: new `/sign-in/totp` route (public-frame layout) + `TotpForm` — first visit enrolls a factor (QR or manual key), later visits challenge+verify, then opens `/staff`. In supabase mode `SignInForm` routes accounts that hold a staff context to `/sign-in/totp` after OTP verification; everyone else lands in the portal. Demo mode untouched.
- **Tests**: 5 new spec tests (directory mapping, RLS-denial envelope, invite RPC contract). Full gates: **261 web + 47 contract tests**, build 66 pages, **22/22 journeys** on a fresh prod build.

Remaining (honest): migration 000017 not applied to any database (no Docker/live project this session); users page UI still drives demo stores in supabase mode until wired to `users.list`/grant ops; staff invite delivery (email with the one-time ref) awaits Resend sender domain.

### Payment lifecycle + users admin live wiring (21 August 2026 session, final)

- **Migration `000019_finance_attempt_lifecycle.sql`** (authored, unapplied): `finance_create_attempt` (owner-guarded — applicant owner / active guardian / finance staff aal2; amount must equal current balance; sandbox order ref) and `finance_refresh_attempt` (created → processing → succeeded, idempotent terminal reads). The ledger post remains `finance_post_sandbox_payment` — the browser never marks an invoice paid.
- **Facade cutover complete for the payment journey**: `createPaymentAttempt`/`refreshAttempt`/`confirmSuccess` now drive the real server machine in supabase mode (`finance.createAttempt` → `finance.refreshAttempt` → `finance.postPayment`, receipt resolved from the server list). Demo path byte-identical; 22/22 journeys still pass.
- **Users admin fully wired** (this session): directory read, invite (one-time ref via `invites.create`), grant/revoke with optimistic versions, suspension as audited multi-revoke; reactivation honestly requires a fresh grant decision.
- Gates: typecheck ✓ lint ✓ 276 web + 47 contract tests ✓ build ✓ 22/22 journeys ✓.

### Careers facade cutover (21 August 2026 session, continued)

- **New domain reads/draft creation**: `jobsListPublishedVacancies` (public read = published only, latest version resolved), `jobsCreateDraftApplication` (owner RLS insert), `jobsListMine` / `jobsListStaffQueue` (versions + applicant-safe events). Four new dispatch ops: `jobs.vacancies`, `jobs.createDraft`, `jobs.listMine`, `jobs.staffQueue`.
- **Facade wired** (supabase mode): submit runs the live pipeline (vacancy resolve → draft row → `jobs_submit` immutable version); `getApplication`/`listStaffRecords` map server rows into the unchanged domain shapes; all four HR decisions route through the version-free `jobs_decide` RPC via a ref→id map. Demo path guarded and byte-identical.
- Gates: typecheck ✓ lint ✓ 276 web + 47 contract ✓ build ✓ 22/22 journeys ✓.

### Historical remaining command set (000015)





`links_approve`/`links_reject` (support staff + aal2, version-checked, revalidation bump, audit + outbox), `support_respond` (requester-safe thread; private notes staff-only, never rendered to the requester), `content_publish_notice` (publisher release of draft/scheduled notices), `jobs_submit` (idempotent append-only) and `jobs_decide` (HR shortlist/interview/offer/not_selected with self-decision denial), `results_submit_marks` (teacher exact year/class/subject assignment scope, per-component maxima validation, full-roster coverage, batch → submitted + immutable version row), `results_moderate` (exam reviewer approve/return), `results_withdraw` (publisher, reason required, append-only) and `results_correction_request`. This is the historical command surface; local coverage is current where recorded in the validation table, while remote application and live integration must be reverified.

### Historical B2–B6 transactional RPC layer

Migration `000009_domain_rpcs.sql` implements the historical plan.md transaction contract as SECURITY DEFINER commands (all `search_path = ''`, every call verifies `auth.uid()` + role + aal2, every command appends audit + outbox rows in its own transaction). Treat remote push status as historical until the staging ledger is re-read:

- `admissions_submit` — append-only version submit, optimistic base-version lock, idempotent retry returns the SAME version id.
- `admissions_review_advance` / `admissions_request_changes` — officer maker steps.
- `admissions_decide` — approver checker step (offer/waitlist/decline); self-approval denied.
- `admissions_respond_offer` — applicant accept/decline; accepting issues the unique admission invoice in the same transaction.
- `finance_issue_admission_invoice` — idempotent per application; requires an approved fee schedule (policy-pending schedules block issuance).
- `finance_post_sandbox_payment` — full-payment post: attempt + payment (unique provider txn) + allocation + sequential receipt + ledger entry + invoice status + audit + outbox; retries on attempt reference OR provider txn id return the same receipt and never duplicate. Sandbox only — the real gateway adapter is a future boundary.
- `enrollment_convert` — idempotent conversion: permanent student + active enrollment + verified guardian link + invoice adoption onto the student ledger + application status; readiness gates (accepted offer + paid invoice) enforced; retry returns the stored result.
- `results_publish_batch` — publisher release: freezes the roster from active enrollments, writes immutable per-student publication snapshots, marks the batch published, emits events; teacher/non-publisher denial proven.
- `timetable_publish_version` — manager publication of a validated draft (immutable publication row).

`invoices.student_id` was relaxed to NULL for pre-conversion admission invoices (forward-only `alter table`), and the guardian finance/result RLS policies were fixed (forward-only `000010_rls_fixes.sql`): applicant-owned admission invoices/items/receipts are visible to their owner, and the two guardian result policies no longer recurse (`app.active_publication_ids` helper).

The full local chain is proven by `scripts/validate-rpcs.sql` (runs inside
`validate-db-local.sh` after the RLS suite): applicant submit → officer advance
→ approver offer → accept + unique invoice → sandbox payment with idempotent
retries → conversion (retry-safe) → result publication → timetable publication,
plus denial cases and outbox/audit evidence. All three suites pass on the
scratch instance. Any remote push status in the historical record must be
rechecked against the staging migration ledger before it is relied upon.

## Current blockers

1. **C5.1 read-only ledger check attempted 28 Aug 2026 — BLOCKED on the pooler connection.** The Supabase management API is reachable (the CLI lists the linked `FAIZ E AAM` project `jxegiamjcawdywqyutdz`), but the direct database pooler connection times out (`Failed to create login role: Connection terminated due to connection timeout`), so `supabase migration list --linked` cannot read the remote `schema_migrations` ledger. No local-only migration was applied. **Region finding:** the linked project lives in **West EU (Ireland)**, while plan.md §1 requires a dedicated synthetic-data staging project in `ap-south-1` (Mumbai) — the staging project decision must be revisited (create/relocate the Mumbai staging project, or record an approved exception) before any migration apply.
2. The machine runs Node 24 while the project pins Node 22; use Node 22 in CI and staging verification.
3. Resend SMTP, verified sender domain, webhook secret, and cron secret are not configured for staging.
4. A payment gateway/merchant account and school finance policy are still unapproved.
5. Supabase-generated types must be regenerated after the local-only migrations are applied to staging.
6. Known transitive npm audit findings (postcss/sharp via Next 15) remain to be revisited when a compatible patched release is available.
7. A real document-scanner service and retention/legal-hold policy are not configured for staging.
8. Anonymous public support must remain fail-closed until a real CAPTCHA provider is selected and verified.
9. Vercel team/project ownership, preview environment, domain, and release owner are not yet recorded.

## Next phase — C5 staging activation

The next action is **C5.0 source-control review**, followed by the staging
migration ledger. Do not begin with provider configuration or Vercel.

| Stage | State | Required next evidence |
|---|---|---|
| C5.0 — source checkpoint | `IN PROGRESS` | Review/group the dirty tree, secret/lockfile scan, local gates, and commit IDs for the staging candidate |
| C5.1 — staging ownership and ledger | `NOT STARTED` | Confirm synthetic staging project/owner/region and compare local/remote migration history |
| C5.2 — migrations, types, advisors | `NOT STARTED` | Dry-run and apply `000016–000030`, re-read ledger, generated-type diff, RLS/RPC/provider suites, advisor disposition |
| C5.3 — Auth/session proof | `NOT STARTED` | Applicant/guardian/staff invitation, OTP, TOTP, recovery, revocation, and wrong-scope real-session evidence |
| C5.4 — Storage/PDF | `BLOCKED` on provider configuration | Private buckets/policies, scanner, upload/finalisation/quarantine, signed delivery, receipt/report-card generation |
| C5.5 — Resend/outbox/cron | `BLOCKED` on provider configuration | Sender domain, Auth SMTP, API/webhook/cron secrets, delivery/retry/suppression and worker freshness |
| C5.6 — finance sandbox | `NOT STARTED` in staging | DB sandbox payment, adjustments/refunds/reconciliation; real gateway remains separately blocked |
| C5.7 — global staging switch | `NOT STARTED` | Both adapter variables set together and all positive/negative vertical journeys pass |
| C5.8 — restore rehearsal | `NOT STARTED` | Isolated database/Storage restore record with RPO/RTO and integrity checks |
| C5.9 — Vercel preview | `NOT STARTED` | Preview built from reviewed commit against staging and full post-deploy verification |
| C5.10 — production | `NOT STARTED` | Separate production project/build, approved school data/providers, release and rollback evidence |

Detailed commands, stop conditions, ownership expectations, and exit criteria
are defined only in `plan.md` C5.0–C5.10.

## Current validation evidence

Evidence was rerun against the current checkout on 24 August 2026. Database
validation used the local scratch PostgreSQL validator. No remote
Supabase/provider call was used.

| Gate | Status | Evidence |
|---|---|---|
| TypeScript | `VERIFIED` | `npm run typecheck` runs `next typegen` first and passes both workspaces |
| Lint | `VERIFIED` | `npm run lint` passes with `--max-warnings=0` |
| Unit/component tests | `VERIFIED` | `npm test`: 394 web tests; contracts: 47 tests |
| Production build | `VERIFIED` | Next.js 15.5.22 compiled and generated 76 routes, including health, MFA, provider, document, webhook, and cron boundaries |
| Local migrations/RLS/RPC | `VERIFIED` | Scratch validator applies `000001–000030` from zero; RLS/RPC, result-release/timetable, operational, and provider-job suites pass |
| Route/link crawl | `VERIFIED` | 66 route cases, 91 unique internal hrefs, no dead links or route failures |
| Critical browser journeys | `VERIFIED` | 22/22: job application, student application, grievance, payment, guardian sign-in, staff admission decision (maker/checker across identities), two-child portal switching, staff link-request approval, admission → fee → enrollment, staff role denial + identity switching, teacher assignment scope, content editor publish denial, teacher subject denial, timetable read-only for non-managers, wrong-child resource scope + child switch, link approval then revocation, results maker/checker split, duplicate-safe payment retry, correction versioning, teacher entry → moderator return → approve → publish → portal publication, timetable manager publish → portal v2, family payment → staff ledger parity |
| Accessibility scan | `VERIFIED` | 54 retained routes scanned with no automated axe violations |
| Focus/keyboard smoke | `VERIFIED` | mobile drawer, skip link, document dialog, and fee-statement focus behaviors passed |
| Responsive sweep | `VERIFIED` | 176 viewport/route pairs at 320–1920 px, no reported overflow or console problems |
| Supabase cutover guard | `VERIFIED` | `npm run check:cutover` passes; protected route/components have no raw browser persistence and every session-store use proves an adapter branch |
| Local production browser review | `VERIFIED` | 22/22 critical journeys pass on local port 3002, including admissions→enrollment, result/timetable chains, role denial, stale/retry, and payment parity |
| Supabase live health/migration ledger | `BLOCKED` | Current environment returns DNS/connection failure; no local-only migration is claimed live |

Limitations of this evidence:

- Browser journey tests prove deterministic demo behavior, not server authorization or permanent persistence.
- Automated accessibility results do not replace manual screen-reader, zoom, contrast, language, and content review.
- The working tree is intentionally dirty during this implementation; review and commit the C0–C5 changes before applying them to staging.

## Feature and integration matrix

| Area | Demo UI | Database/RPC | Application cutover | Live verification |
|---|---|---|---|---|
| Public website and visual system | `VERIFIED` | Public content schema exists | Public careers/notice reads and content projections have local Supabase branches; staging not rechecked | Demo browser gates pass; staging content not rechecked |
| Student admissions | `VERIFIED` | Admissions RPCs/schema + `000022` draft/readiness projection | `VERIFIED locally` — owned server drafts, submit/status/decisions/offer response and authoritative mapping; staging not cut over | Local contracts/RPCs only |
| Enrollment conversion | `VERIFIED` | Idempotent create-or-match conversion/readiness RPCs in `000022` | `VERIFIED locally` — invoice/payment/readiness/conversion/link projection | Local contracts/RPCs only |
| Careers | `VERIFIED` | Careers schema/RPCs + `000024` drafts/withdrawal/HR commands | `VERIFIED locally` — vacancy, cross-device draft, submit/status, withdrawal, version-safe HR decision branches | Local contracts/RPCs only |
| Family finance/payment | `VERIFIED` | Provider-neutral attempts, ledger, adjustments, refunds, reconciliation `VERIFIED` locally | `VERIFIED` locally with DB sandbox | Real gateway not selected |
| Results | `VERIFIED` | Results RPCs/schema + `000023` draft/correction workflow | `VERIFIED locally` — batch/detail/version/publication/snapshot mapper and command branches | Local contracts/RPCs only |
| Timetable | `VERIFIED` | Timetable RPCs/schema + `000023` revision/conflict/override/date-sheet commands | `VERIFIED locally` — async effective/draft/publish facade and loaders | Local contracts/RPCs only |
| Guardian/student linking | `VERIFIED` | Versioned link/capability lifecycle `VERIFIED` locally | `VERIFIED` locally | Real-session proof pending |
| Family context | `VERIFIED` | Relationship/capability RLS `VERIFIED` locally | Server hydration and persisted selection `VERIFIED` locally | Staging pending |
| Staff context/roles | `VERIFIED` | Grants/assignments/AAL2 `VERIFIED` locally | Server hydration and workspace selection `VERIFIED` locally | Staging pending |
| Users administration | `VERIFIED` | Invitation/account/grant/MFA lifecycle `VERIFIED` locally | `VERIFIED` locally | Auth email/provider evidence pending |
| Identity/authentication | `VERIFIED` demo and local route guards | Auth/account/link schema and RLS `VERIFIED` locally | Provider-ready and fail-closed | Live session/AAL/TOTP proof pending |
| Documents/PDF | `VERIFIED` | Upload, scan, generation, retention jobs `VERIFIED` locally | Provider-ready with private signed delivery | Storage/scanner staging pending |
| Notices/content | `VERIFIED` | Content/notices schema + `000024` immutable draft/review/publish commands | `VERIFIED locally` — public/family/staff projections and fail-closed writes | Local contracts/RPCs only |
| Notifications/outbox | `VERIFIED` | Retry-safe outbox/delivery/webhook/job schema `VERIFIED` locally | Official Svix + Resend/provider-ready | Resend not configured |
| Support | `VERIFIED` | Support RPC/schema + `000024` requester/private/public-intake commands | `VERIFIED locally` — requester-safe/staff projections, public route, reopen/assign branches | Local contracts/RPCs only |
| Settings/audit | `VERIFIED` | Versioned settings/audit schema + `000024` commands | `VERIFIED locally` — optimistic settings save and read-only PostgreSQL audit projection | Local contracts/RPCs only |
| Backend foundation | `VERIFIED` locally | Migrations `000001–000030` pass all scratch suites | Local cutover guard passes | `000001–000015` historical only; `000016–000030` local-only |
| Production/Vercel | `NOT STARTED` | No production schema | No deployment | No release evidence |
| Facility demonstrator | `VERIFIED` demo-only | No database/RPC by design | Isolated | Deferred |

## Highest-priority audit findings

1. The next work is environment verification, not another feature or screen.
2. C2–C4/provider readiness is locally verified. Do not call staging or global cutover verified until real sessions and providers run every retained workflow.
3. The demo adapter remains the default. Supabase branches fail closed; staging must switch both adapter variables together only after its migration ledger is reconciled.
4. The server adapter boundary is split by runtime: Server Components use direct server loaders/request-aware calls; Client Components use `/api/adapter`. A relative unauthenticated server fetch is a release blocker.
5. Migrations `000016–000030` pass locally but remain unverified remotely. The next environment action is migration-ledger verification, not a blind push.
6. Storage, scanning, PDF, retention, Resend, webhook, cron, and health code is provider-ready locally; real credentials and staging evidence remain blocked.
7. Resend SMTP, sender-domain, webhook, cron, bounce, complaint, and suppression evidence is still blocked on provider configuration.
8. The frontend and local cutover gates are green; this does not make the Supabase adapter globally, staging, or production `VERIFIED`.

## Scope decision

Core launch scope remains the public site, identity/relationships, student admissions, careers, family fees/results/timetable/notices/documents/support, and the minimum staff workspaces needed to operate them.

Do not add attendance, LMS/homework, transport, payroll, library circulation, hostel/cafeteria, chat/social feeds, native apps, advanced analytics, recurring mandates, or a generic workflow/page builder. The environment/facility module stays isolated and deferred.

## Blocking school decisions

These decisions are `BLOCKED` on school/provider confirmation, not on engineering effort:

1. Official school name/spelling, logo/crest, affiliation, school code, grades/sections, contacts, languages, and approved public media.
2. Guardian verification evidence, relationship restrictions, student-account policy, and who owns identity/provider accounts.
3. Admission calendar, eligibility/capacity, withdrawal, offer expiry, admission-fee prerequisite, and enrollment approval authority.
4. Fee schedule, concessions, partial/late/refund/write-off rules, gateway merchant/provider, receipt numbering, and reconciliation ownership.
5. Result subjects/components/maxima, grading, absent/result status, aggregate/remarks/report layout, moderation, publication, and correction authority. Attendance is outside scope unless explicitly approved.
6. Timetable working days, periods, rooms, teacher assignments/load, substitute/override, and exam date-sheet process.
7. Vacancy approval, reviewer panels, scoring/interview/reference checks, applicant-facing wording, and retention.
8. Notice approval/audiences/channels, document retention, support SLA/escalation, privacy rights/retention, disclosures, hosting/domain, and provider ownership.

The UI must keep unconfirmed values fictional/demo or policy-pending.

## Active phase summary (not an execution order)

Read `plan.md` for the sole C0–C5 execution sequence. This status summary
records state only:

| Phase | State | Current boundary |
|---|---|---|
| C0 | `VERIFIED` locally | Browser dependencies, deterministic typegen, lint, demo/browser/accessibility/focus/responsive/link gates, and scratch DB validation pass. |
| C1 | `VERIFIED` locally | Role-boundary hardening, invitation lifecycle, atomic suspension/reactivation, and private bucket/policy migration pass locally. |
| C2 | `VERIFIED` locally | Every retained facade has a fail-closed Supabase branch/loader and the cutover guard passes; staging sessions remain unverified. |
| C3 | `VERIFIED` locally | Storage finalisation/scanning, signed delivery, PDF generation, and retention pass fake/provider-job contracts. |
| C4 | `BLOCKED` on provider configuration | Code and official Svix verification are locally verified; real Resend SMTP/API/domain/webhook/cron evidence remains. |
| C5 | `BLOCKED` on live environment verification | Remote ledger, generated types, advisors, real sessions/providers, restore rehearsal, Vercel preview, and production remain. |

### Historical frontend phases

1. `I0` — **implemented and verified**: shared people, relationships, roles/assignments, enrollments, and context contracts; family/staff context services; service-driven shells; context survives navigation within the demo session.
2. `I1` — **spine implemented and verified**: two-child portal switching, per-page child context, pending-link approval/rejection path.
3. `I2` — **implemented and verified**: finance ledger parameterized by student with staff/portal parity, per-student result snapshots, one timetable facade, and typed content/documents/notifications/users/settings/audit services wired into their pages.
4. `I3` — **implemented and verified**: admission offer → finance payment → enrollment/link conversion (invoice-once, shared checkout, readiness gates, idempotent create-or-match conversion, ledger adoption, applicant acknowledgement).
5. `I4` — **spine implemented and verified, then rebaselined to the canonical Phase-1 grant model**: role/action authorization module, role-aware navigation, route guards with workspace-switch denial, demo identity picker, teacher assignment-scoped marks entry, action gating; the `plan.md` Phase-1 grant table (maker/checker splits, `links.verify` scope, Rania Mir approver identity) is the authoritative action model.
6. `I5` — **in progress**: deterministic demo domain events + append-only audit + demo outbox now emit one idempotent event per consequential action (payment post, conversion, publish, withdraw, content publish, link decision); in-app notification projection from outbox events and PDF generation remain backend work.
7. `I6` — **frontend contract freeze applied (historical)**: `packages/contracts` core domain/context/error/outbox/audit types, `design/BACKEND-HANDOFF-MATRIX.md`, applicant draft privacy policy, and the archived frontend gate evidence; the active backend cutover is governed by `plan.md` C2, not this historical handoff gate.

The frontend and local backend/provider gates have passed. Do not claim staging verification or production release until the C5 environment gates pass.

## Change log

Every entry below is historical context only. It cannot override the current
matrix, validation table, blockers, or C0–C5 execution order above.

- **2026-08-31 (Three-portal consolidation audit and rebaseline):** The signed-in product is being consolidated into exactly three portal experiences — Administrator (`/administrator/*`), Principal (`/principal/*`), and Guardian (`/portal/*`); the Teacher and Student portals are removed (teachers become non-login school records with `teaching_assignments`; students remain guardian-linked records).

  **Drafted this session:** two-profile access contracts (administrator/principal provisioning presets with granular role expansion) and the `000042_staff_access_profiles.sql` redesign for the two-profile model (the old three-profile version is not authoritative).

  **Audit findings:** canonical routing still falls back to `/staff/*` instead of the canonical `/administrator/*` + `/principal/*` map; profile homes depend on the active workspace rather than the profile; invitation title/version defects; stale browser journeys; and Phases 3–9 of the consolidation remain. Verified read-only remote facts: ledger exactly `000001–000039`, project `ACTIVE_HEALTHY` PostgreSQL 17 `eu-west-1`, local `000040–000041` not live, `000042` untracked/not live, profile/import/claim/export tables absent remotely, and the project contains substantial records that must not be assumed disposable.

  **Status:** the consolidation is `IN PROGRESS`, not `VERIFIED`. The C5 staging sequence remains paused until the consolidation lands and local gates pass; no Vercel/production work is authorized.

- **2026-08-31 (C5 local feature completion — admissions withdrawal, review state, careers scorecards, CMS pages, finance reconciliation, timetable date input, and demo-leakage removal):** Completed the remaining local features and ran the full local verification gate through migration `000041`:

  **New database work (all verified from zero):** `000040_timetable_override_and_date_sheet_reads.sql` (override/date-sheet read projections for the timetable facade) and `000041_admissions_withdrawal.sql` (version-checked, idempotent `admissions_withdraw` RPC + idempotency records). Scratch PostgreSQL 17 applies `000001–000041` from zero; RLS/RPC, results-release, slice-5 operational, and slice-6 provider-job suites all pass ("ALL LOCAL DATABASE CHECKS PASSED").

  **Functional completion:**
  1. **Admissions withdrawal** — end-to-end applicant withdrawal in Supabase mode: RPC + domain function + `admissions.withdraw` adapter op + service method + mode-aware UI. `withdrawn` status now maps correctly (was "Declined").
  2. **Admissions staff review state** — new `staffStartReview` (submitted → under review) and canonical transitions (Submitted → Under review → Assessment → decision); decisions are no longer offered from Submitted. Maker/checker reviewer attribution preserved.
  3. **Careers detail** — staff queue projection now includes `job_review_assignments` + `job_scorecards`; `assignReviewer` and `saveScorecard` service methods (Supabase + demo); JobReview renders real attributed scorecards with a save form, real reviewer account, and snapshot-derived qualifications instead of fictional rows.
  4. **Private-document boundary** — upload-intent route now resolves allowed MIME types/max bytes from the admission configuration projection server-side instead of trusting the browser allowlist; declared type/size is validated against the resolved requirement.
  5. **Finance** — payment-register merge now dedupes by attempt id (provider order refs never align with txn ids); `finance.listAllAttempts` adapter op; reconciliation start now imports sandbox evidence (`finance_reconciliation_import`) so runs carry authoritative matched/pending/exception counts instead of an empty shell.
  6. **Timetable** — override form uses a calendar date input (date-specific overrides on any date) instead of a weekday select; portal/date-sheet reads stay versioned.
  7. **CMS** — staff content page gained a public-page editor (create draft with slug, edit draft/archived/expired rows, review/publish note) backed by `createPublicPage`/`editPublicPage`/`getPublicPageBody` in both adapters; homepage latest-notice strip reads the latest authoritative published public notice in Supabase mode; the About route renders a published managed page when one exists (concept copy remains the honest fallback).
  8. **Demo leakage removed** — every remaining Supabase-mode demo badge, "(demo)" success message, "demo session" text, and fictional policy note across portal/staff/applicant pages and staff components now renders only in demo mode (30+ files).
  9. **Demo identity fix** — added a dedicated `content_publisher` demo identity (Naseer Lone, account `…0206`) so the notice maker/checker flow is exercisable end to end (Aisha drafts → Naseer approves/publishes); fixture and identity tests updated.
  10. **Middleware edge fix** — `@supabase/ssr` is now imported lazily inside `updateSession` so the edge bundle never evaluates its code-generation helpers in demo mode (fixes `EvalError: Code generation from strings disallowed` on every route).
  11. **Responsive fix** — `FinanceActions` grid used `minmax(0, 1fr)` which made `auto-fit` emit dozens of ~1px tracks that overlapped the panels; now `minmax(min(100%, 300px), 1fr)` (single column at 320px, side-by-side at desktop). The 176 viewport×route responsive sweep passes with no problems.

  **Journey automation:** the local feature-journey suite grew from ~90 to 107 checks (timetable override record/revoke with reason, notice maker/checker publish through the new identity, careers reviewer/scorecard), and the finance/override test steps were updated for the date input and the required revocation reason.

  **Full local gate (demo adapter, production build on localhost):** typecheck ✓ · lint 0 issues ✓ · 477 web + 47 contract tests ✓ · 79-route production build ✓ · 107/107 local feature journeys ✓ · 22/22 critical journeys ✓ · link crawl 91 hrefs, no dead links ✓ · accessibility 54 routes, no violations ✓ · focus smoke ✓ · responsive 176 viewport×route pairs, no problems ✓. The Supabase-mode contract suites (47) and the local database validation continue to pass. Remote staging/provider verification remains `NOT STARTED`/`BLOCKED` per plan.md C5.

- **2026-08-28 (C5.0 source-control checkpoint — 6 reviewable commits from the dirty tree):** Audited the entire dirty working tree (248 files: 166 modified + 82 untracked) and committed it in six reviewable groups:

  1. `feat(database)` — migrations `000016–000030` + results-release/slice-5/slice-6 pgTAP suites + seed + DB validators (22 files).
  2. `feat(facades)` — Supabase-mode service facades, `/api/adapter` registry, server loaders, identity-server auth routes, finance server mapping, contract tests (58 files).
  3. `feat(providers)` — outbox worker, Resend/Svix webhook, PDF renderer, document/support providers, provider tests (23 files).
  4. `feat(ui)` — portal/staff/applicant/public integration: identity routes, timetable overrides + date-sheet publish, role-gated actions, empty/error states, service tests, override browser journey (123 files).
  5. `docs` — PROJECT-STATUS/plan/blueprint/spec/README/UI-completion updates, VERCEL-READINESS, COMPLETE-FEATURE-PROMPT (10 files).
  6. `chore(tooling)` — Playwright/axe pins, cutover guard, vercel.json cron contract, `.env.example` names, and the safe audit fix (nanoid 3.3.18 + Next 15.5.24 patch) (12 files).

  **Security/quality audit (all clear):** no secrets in the diff or untracked files (the only base64-looking strings are package-lock SRI integrity hashes); `.env.local` confirmed git-ignored; no logs, archives, `.DS_Store`, or temp artifacts; no obsolete files found. **Dependency risk:** `npm audit` reduced from 4 high to 1 high — the remaining `postcss` advisory requires the breaking Next 16 upgrade and stays documented (blocker 6).

  **Full gate re-run from the reviewed HEAD (`50fd7d4`), all VERIFIED:** typecheck ✓ · lint 0 issues ✓ · 394 web + 47 contract tests ✓ · 76-route production build ✓ · protected-path cutover guard ✓ · 22/22 critical browser journeys ✓ · scratch PostgreSQL applies `000001–000030` from zero and passes the RLS/RPC, results-release, slice-5 operational, and slice-6 provider-job suites ✓ ("ALL LOCAL DATABASE CHECKS PASSED").

  **C5.1 read-only staging ledger check (attempted, BLOCKED):** `supabase projects list` works (management API reachable; linked project `FAIZ E AAM` `jxegiamjcawdywqyutdz`), but the pooler connection times out, so the remote migration ledger could not be read. No migration was applied. Region discrepancy recorded: linked project is West EU (Ireland) vs the plan.md Mumbai (`ap-south-1`) staging requirement.

- **2026-08-28 (feature-by-feature local verification — every workflow tested in demo mode):** Systematically exercised EVERY feature area in the browser against the local demo runtime (no database or email service — everything is the deterministic demo adapter) and fixed every defect found. 16 feature areas, ~90 browser checks, plus the existing 22 critical journeys:

  **Feature areas verified (all PASS in demo mode):** 1) Public site — 19 routes render with key content, no console errors; 2) Identity — sign-in→verify→complete, recovery issues reset refs for known AND unknown identifiers (no existence leak), wrong-password error, invite/register/totp/session-expired/access-denied routes; 3) Linking — link-child request with reference, two-child switcher, child switch changes overview, staff reject with reason; 4) Admissions applicant — autosave indicator, draft resumes after leaving, resume panel on the landing page, full 8-step submit → APP- reference + timeline, step-1 validation; 5) Staff admissions — request-changes with reason → applicant sees it → edit restores submitted data → re-upload documents → resubmit → offer decline → 9 queue filter tabs; 6+7) Careers — vacancy detail + apply CTA, job draft autosave/resume, submit → JOB- reference, applicant withdrawal with confirmation, 8 queue tabs, HR decision (offer/not-selected) with note + confirm; 8+9) Finance — portal ledger + filter tabs + invoice detail line items + receipt view, staff workspace/invoices/payments/reconciliation render + reconciliation run; 10+11) Results + timetable — portal results list, publication detail with subjects/grades, results queue status tabs, timetable override record→portal applies→revoke restores, date-sheet publish → portal "v1 · published" badge; 12-14) Content — notice draft created (editor workspace), published after switching to the Content publisher workspace (maker/checker), public + portal notice boards, documents library + preview dialog, portal grievance → GRV- ref, staff respond + resolve + reopen; 15-16) Admin + facility — users directory + invite with one-time reference, settings save persists, audit explorer + filters, facility overview/alerts ack/devices/history/reports/zones/wallboard.

  **Bugs found and fixed:**

  1. **`/sign-in/totp` threw a Supabase environment error in demo mode** — `TotpForm` unconditionally called `createSupabaseBrowserClient()`, producing a pageerror and a fatal "Sign-in state could not be read" state even though the docs say "demo-mode sign-ins never route here". Fixed: the page passes `adapter={dataAdapter()}`; in demo mode the form renders an honest "Demo sign-in — pick a demo identity instead" state with a link back to sign-in, and never touches the Supabase client. All auth routes now load with zero console errors in demo mode.
  2. **Portal results never showed the published snapshot** — the results pages passed the student's public reference (`STU-2026-0901`) to `getStudentResultSnapshot`, but the demo fixture is keyed by the internal student id (UUID), so EVERY child saw "No released snapshot for this child" even though the fixtures exist. Fixed: the demo path resolves the public reference to the internal id through the relationship graph (`demoStudents`), so the same student always resolves to its published snapshot. 2 new regression tests; the publication detail now renders subjects, obtained marks, grades, and remarks.

  **Gates:** typecheck ✓ · lint 0 issues ✓ · 394 web + 47 contract tests ✓ · 76-route build ✓ · cutover guard ✓ · 22/22 critical journeys ✓ · 16/16 feature areas (≈90 checks) ✓.

- **2026-08-28 (docs-vs-code gap audit — timetable overrides + date-sheet publish):** Read PROJECT-BLUEPRINT.md, FEATURE-INTEGRATION-SPEC.md, plan.md, UI-COMPLETION-PLAN.md, PROJECT-STATUS.md, README.md, and AGENTS.md, then compared every documented feature contract against the actual code. All previously claimed gates verified true (392 tests now passing, 76-route build, all facades wired, cutover guard green). Two genuine lags found and closed:

  **Timetable overrides (blueprint §5.9, spec §6.10, plan.md C2.3):** The server RPC (`timetable_save_override`) and adapter op (`timetable.saveOverride`) existed, but the demo timetable service — the DEFAULT runtime — had no override model, the staff UI had no override panel, and the portal never displayed overrides. Implemented end-to-end: `TimetableOverride` model in `modules/services/timetable.ts` (`saveTimetableOverride` with kind-specific validation + deterministic refs + audit recording, `listTimetableOverrides` active-first, `revokeTimetableOverride` append-only revocation, `effectivePeriodsForDate` that applies substitute/room/cancellation/special overrides on their date only — the base timetable is never mutated); staff `TimetableManager` gained an "Overrides" panel (add form with day/period/kind/substitute-teacher/room/reason, active list, revoke) gated by `timetable.manage`; portal `TimetableWorkspace` now applies active overrides on the day/week views with a "Date override" badge and an override-count notice, and derives the exam date range from the date-sheet fixture instead of hardcoded "1–9 September".

  **Exam date-sheet publish was fake:** the staff publish button only flipped local React state. `publishDateSheet`/`getDateSheetState` are now session-backed, versioned service writes (audited), and the portal shows a "v{n} · published" badge for the published state.

  **Tests + verification:** 9 new tests in `timetable-facade.test.ts` (demo-week date mapping, deterministic refs, apply-on-date-only, active-first ordering, kind-specific validation rejections, cancellation/room overrides without base mutation, revoke restores base + rejects unknown/double revoke, date-sheet versioning). New `scripts/timetable-override-check.cjs` browser journey: staff records → portal shows substitute on Tuesday only → staff revokes → portal restores base → exam range derived — 7/7 PASS.

  **Gates:** typecheck ✓ · lint 0 issues ✓ · 392 web + 47 contract tests ✓ · 76-route build ✓ · cutover guard ✓ · 22/22 critical journeys ✓ · 7/7 override browser checks ✓.

- **2026-08-23 (C2.2–C2.4 local cutover):** Added `000022_c2_admissions_enrollment_facade.sql`, `000023_c2_results_timetable_facade.sql`, and `000024_c2_operational_facades.sql`; wired local Supabase branches for admissions/finance/enrollment, results/timetable, careers, content, support, settings, audit, notifications, and document metadata; added public support intake, server loaders, async timetable reads, mapper/denial/retry contracts, and the protected-path cutover guard. Local evidence: 350 web tests + 47 contract tests, zero-warning lint, 70-page build, scratch migrations/RLS/RPC from zero, 22/22 critical journeys, 54-route accessibility, focus smoke, 176 responsive pairs, 66-route/287-href crawl. No remote Supabase/provider action occurred; C2.5 staging proof remains pending.

- **2026-08-23 (C2.1 local implementation slice; superseded by the C2.2–C2.4 slice below):** Added strict server/public adapter matching; request-scoped family-child and staff-workspace selection persisted with secure cookies; server-seeded family/staff context loaders and provider hydration; typed local school-configuration reads; pending staff invitation acceptance UI/lifecycle; and Supabase-mode context/denial tests. The subsequent local C2.2–C2.4 gate is the current evidence above.

- **2026-08-22 (C0–C3 implementation slice):** Pinned Playwright `1.62.1` and axe-core `4.13.0`; added deterministic `next typegen` and zero-warning lint; corrected the teacher-finance RLS expectation; added `000020_role_boundary_hardening.sql` (system-administrator functional-role denial, staff invitation role/scope acceptance, atomic account suspension/reactivation) and `000021_private_storage_buckets.sql`; added verified-claims/AAL route guards for portal, applicant, and staff layouts; added Supabase family/staff context reads, finance/careers server loaders, adapter parity configuration, private document signed delivery/upload intent, generated-PDF Storage upload, Resend timestamp/replay/error hardening, and correlation/no-store headers. Local gates: 329 web + 47 contract tests, typecheck, lint, 68-page build, scratch migrations/RLS/RPC, 22/22 browser journeys, 53-route accessibility, focus smoke, 168 responsive pairs, and 65-route crawl — all pass. Staging migration ledger, Supabase-mode all-facade journeys, Resend configuration, Storage scan/finalisation, and production remain pending.

- **2026-08-22 (Loop 6 — AGENTS.md compliance audit):** A final compliance audit against the AGENTS.md guardrails found and fixed one HIGH-severity issue:

  **Facility authorization gap (HIGH — FIXED):** The `/staff/facility` routes were explicitly excluded from `StaffRouteGuard`, bypassing authorization entirely. The alerts page had write actions (acknowledge/resolve) with no role gate. Fixed by: (1) adding `facility.view` and `facility.manage` actions to `staff-authorization.ts`; (2) granting `facility.view` to `support_officer` and `system_administrator`, and `facility.manage` to `system_administrator`; (3) removing the facility exclusion from `StaffRouteGuard` and adding facility routes to the `ROUTE_ACTIONS` mapping; (4) gating the acknowledge/resolve buttons in `AlertsWorkspace` with `canRole(..., "facility.manage")`.

  **Audit findings documented as known limitations:**
  - Predictable demo refs (PUB-2026-001, INV-2026-0101, APP-2026-0417): These are human-readable demo fixture identifiers. Access control is enforced through authorization checks (family context, staff role gates), not security-through-obscurity. The backend must generate non-sequential identifiers.
  - Presentation mappings in components (STATUS_TONE, FILTERS): These are UI presentation concerns (which color/tone to show for a status badge, which filter options to offer), not business rules. Actual state transitions, validation, and authorization ARE enforced in service modules.
  - TOTP secret display in enrollment: Standard TOTP enrollment flow — the user must receive the secret to set up their authenticator. Not stored persistently in client storage.

  **Gates:** typecheck ✓ · lint 0 errors (1 pre-existing warning) ✓ · 329 web + 47 contract tests ✓ · 67-page build ✓.

- **2026-08-22 (Loop 5 — publishNotice guard, session/timetable tests):** Two parallel subagents filled the last remaining test coverage gaps, and a publishNotice guard was added:

  **publishNotice guard:** Added a guard rejecting re-publishing an already-published notice with the same note (forces a different note to republish, preserving the append-only version trail).

  **Session service tests (7 new):** Created `session-service.test.ts` covering sessionGet/sessionSet round-trip, null for unset keys, overwrite behavior, complex object handling, sessionRemove clearing, sessionRemove on non-existent keys, and sessionKey namespace prefix.

  **Timetable service tests (6 new):** Added to `timetable-facade.test.ts` covering saveTimetableDraft + getTimetableDraft persistence, clearTimetableDraft, deriveEditedKeys (empty set for unchanged, keys for changed cells), detectConflicts (empty array for no conflicts, teacher conflict detection across peer classes).

  **Gates:** typecheck ✓ · lint 0 errors (1 pre-existing warning) ✓ · 329 web + 47 contract tests ✓ · 67-page build ✓.

- **2026-08-22 (Loop 4 — comprehensive error handling and test coverage):** Three parallel subagents completed the remaining error-handling and test-coverage work identified in Loop 3's audit:

  **Error handling (15 files):** Added `.catch()` handlers to every remaining unhandled promise chain across staff and portal components: ResultsBatches, AdmissionsQueue, TimetableManager, MarksEntry (3 sites), OverviewFinanceBand (2 sites), portal results page (2 sites), portal results detail (2 sites), FeeLedger, ReceiptView (2 sites), InvoiceDetail, GrievanceInbox, NotificationBell (2 sites), TimetableWorkspace, staff results detail, and FinanceWorkspace. All handlers use the existing cancellation flags to avoid setting state after unmount, with safe fallback values (empty arrays, null, or `setLoading(false)` to prevent stuck loading states).

  **Content service tests (9 new):** Added tests for `publishNotice` (draft→published, version increment, unknown slug rejection), `unpublishNotice` (published→draft, draft rejection), `listForAudience` (public vs family filtering), and `getNotice` (found and not-found). Flagged a discrepancy: `publishNotice` does not guard against re-publishing an already-published notice (it bumps the version instead) — may need a guard to match the `editNotice` pattern.

  **Identity service tests (9 new):** Added tests for `signIn` validation (empty phone, empty password, short password, wrong credentials, successful sign-in), `verifyCode` validation (empty code, wrong code), `startRecovery` (returns recovery reference), and `requestLink` (returns link reference).

  **Gates:** typecheck ✓ · lint 0 issues ✓ · 316 web + 47 contract tests ✓ · 67-page build ✓.

- **2026-08-22 (Loop 2+3 — deeper audit: empty states, audit recording, role gates, error handling, test coverage):** Continued self-prompting audit loops beyond the initial role-by-role pass. Four parallel subagents audited hardcoded values, missing role gates, error handling patterns, and test coverage gaps across the entire codebase. Findings and fixes:

  **Empty states (Loop 2):** The invoices, payments, and reconciliation pages rendered empty tables with headers but no "no data" message when the register was empty. Added conditional empty-state messages to all three pages.

  **Audit recording (Loop 2):** `contentService.editNotice` and `contentService.unpublishNotice` were not recording audit events — now both record "Notice published" with a descriptive reason. All four careers staff decisions (`staffShortlist`, `staffRequestInterview`, `staffOffer`, `staffNotSelected`) were not recording audit events — now all record "Application reviewed" with the decision reason. 1 new test verifying audit recording for careers staff decisions.

  **Missing role gates (Loop 3):** Four staff pages had write actions accessible without `canRole` checks: (1) Content page review button — now gated by `content.publish`; (2) Settings page save and reset buttons — now gated by `settings.manage`; (3) Users page invite/manage/grant/revoke/suspend/reactivate — now gated by `users.manage`, with the "Manage" button becoming "View" for unauthorized roles; (4) Link requests page approve/reject/revoke — now gated by `links.verify`, with read-only fallback for unauthorized roles.

  **Hardcoded values (Loop 3):** StaffHomeWorkspace had hardcoded "2 drafts · 1 scheduled" notice counts — replaced with the actual fixture count. TimetableManager had a hardcoded "Class 8-A and peer fixture (Class 9-B)" conflict intro — replaced with the dynamic `selectedClass` value.

  **Error handling (Loop 3):** Five critical service-call sites had no `.catch()` handlers, risking unhandled promise rejections: content page, users page, settings page, link-requests page, and StaffHomeWorkspace. All now have `.catch()` handlers that set safe fallback state.

  **Test coverage (Loop 3):** Three previously untested services now have dedicated test files: `audit-service.test.ts` (7 tests covering listEvents ordering, record append-only contract, deterministic id/timestamp, session persistence, non-idempotency), `settings-service.test.ts` (6 tests covering default view, session persistence, audit recording, policy-pending preservation), `outbox-service.test.ts` (9 tests covering idempotent enqueue, insertion-order listing, pending filter, delivered transition, unknown-id rejection). Total: 22 new tests.

  **Gates:** typecheck ✓ · lint 0 issues ✓ · 298 web + 47 contract tests ✓ · 67-page build ✓.

- **2026-08-22 (full-role page-by-page audit — all staff roles + portal + applicants):** Continued the systematic audit across every role's pages. The System Administrator improvements (see entry below) were followed by per-role audits of Content, Admissions, Finance, Teacher/Exam Reviewer/Result Publisher, Timetable Manager, HR/Careers, Support, Auditor, Guardian/Student portal, and Applicant journeys. Findings and fixes:

  **Content Editor/Publisher (`/staff/content`, `/staff/notices`):** The public-page review button was only modifying local React state — now persists through `contentService.setPublicPageStatus` (session-backed, audit-recorded). The NoticePublisher "Edit" button was a stub announcement — now opens an inline edit form calling `contentService.editNotice` (drafts only; published notices must be unpublished first). 9 new `content-service.test.ts` tests covering page status persistence, notice editing with published-rejection guard, and field preservation.

  **Admissions Officer/Approver (`/staff/admissions`):** The top metrics used static fixture counts (`admissionsQueueCounts`) that never reflected session decisions — now derived from live service data inside `AdmissionsQueue`. The filter tabs were missing "Changes requested", "Waitlisted", "Declined", and "Enrolled" — all 9 statuses are now filterable.

  **Finance Officer/Approver (`/staff/finance`):** The "Payments to reconcile" count in `FinanceWorkspace` was hardcoded to `2` — now derived from recent receipts. The `ReconciliationRun` component reported a hardcoded "4 matched, 1 discrepancy" — now computes actual matched/discrepancy/pending counts from the comparison data. The invoices page Print button was a non-functional placeholder — now triggers `window.print()` via a client `PrintButton` component.

  **Teacher/Exam Reviewer/Result Publisher (`/staff/results`):** The results batch queue had no status filtering — an exam reviewer seeing all batches couldn't narrow by status. Added status filter tabs (All, Draft, Submitted, Moderation, Returned, Approved, Published, Withdrawn) with live counts, matching the pattern used in the admissions and careers queues.

  **HR/Careers Reviewer/Approver (`/staff/careers`):** The recruitment queue filter tabs were missing "Offered", "Not selected", and "Withdrawn" — all 7 statuses are now filterable.

  **Timetable Manager, Support Officer, Auditor, Guardian/Student portal, Applicant journeys:** Audited and found no gaps — these pages are already comprehensive with service-backed state, role gating, accessibility, and complete lifecycle flows.

  **Gates (all roles):** typecheck ✓ · lint 0 issues ✓ · 275 web + 47 contract tests ✓ · 67-page build ✓.

- **2026-08-22 (System Administrator role — page-by-page audit and implementation):** Audited all admin-accessible pages (`/staff/users`, `/staff/settings`, `/staff/audit`, `/staff/link-requests`, `/staff` home) and implemented functional improvements:

  **Users page (`/staff/users`):** Replaced the broken invite form (wrong role list, no persistence, no grant/revoke) with a fully functional admin workspace. The `usersService` now derives rows from the mutable relationship store (not the immutable fixture) and supports five write operations: `inviteUser` (creates person + account + staff member + initial role grant, returns a one-time reference shown once), `grantRole` (with duplicate-active-grant guard), `revokeRole` (appends to history, clears the active workspace if it was the revoked grant), `suspendAccount` (revokes all active grants), and `reactivateAccount`. The page shows an expandable "Manage" panel per user with the active role grants, grant-additional-role form, revoke-with-reason, and suspend/reactivate controls. All operations require a reason (recorded in the audit trail) and produce an audit event. The `RelationshipDemoStore` was extended with mutable `userAccounts`, `staffMembers`, `roleGrants`, and `staffAssignments` collections seeded from the immutable graph fixture; `staff-context.ts` and `family-context.ts` now read from the store so admin changes are immediately visible to every consumer.

  **Settings page (`/staff/settings`):** The save button now persists editable fields (grading scheme, two-reviewer requirement, notice expiry, email sender) to sessionStorage via `settingsService.saveSettings`, records an audit event, and shows the updated "saved by/at" metadata. Policy-pending sections remain visibly flagged and disabled until the school confirms them.

  **Audit page (`/staff/audit`):** The action filter dropdown now includes all 15 `AuditAction` types (was missing `Result withdrawn`, `Payment posted`, `Link requested`, `Link approved`, `Link rejected`, `Link revoked`, `Enrollment converted`).

  **Staff home (`/staff`):** Added an "Administration" section for the system_administrator role with quick-link tiles to Manage users (with live staff count), Link requests (with pending count), Settings, and Audit trail. Previously the admin role saw an empty home page.

  **Tests:** 15 new `users-service.test.ts` tests covering invite, grant (with duplicate guard), revoke (with already-revoked guard), suspend (with already-suspended guard), reactivate (with already-active guard), and staff-context integration (revoking the active workspace grant causes the context to pick a new workspace). Fixed the pre-existing `staff-identity-domain.test.ts` typecheck error by adding a typed `AdminDirectoryRow` return type to `usersListAdmin`.

  **Gates:** typecheck ✓ · lint 0 issues ✓ · 276 web + 47 contract tests ✓ · 67-page build ✓ · 65-route link crawl zero dead links ✓.

- **2026-08-06 (remaining matrix commands live — links/support/content/careers/marks/moderate/withdraw):** Migration `000015_remaining_commands.sql` closes the BACKEND-HANDOFF-MATRIX command surface: `links_approve`/`links_reject` (revalidation bump, version checks), `support_respond` (private notes staff-only), `content_publish_notice`, `jobs_submit` (idempotent) / `jobs_decide` (self-decision denied), `results_submit_marks` (exact assignment scope + maxima + roster coverage), `results_moderate`, `results_withdraw`, `results_correction_request`. Local RPC suite extended with happy paths + denials for all of them — all three suites pass; pushed live; types regenerated (4827 lines); typecheck/lint/216+47 tests/66-page build/22/22 journeys green; live integration suite still passes.
- **2026-08-06 (app layer: outbox worker + email + server domain services + live integration suite):** Built the Supabase runtime wiring: `lib/supabase/outbox-worker.ts` + `app/api/outbox/route.ts` (CRON_SECRET-protected claim/dispatch/deliver-fail with exponential backoff), `lib/email/` Resend adapter + editorial string templates (one per plan §9 event kind), `app/api/email/webhook/route.ts` (Svix HMAC verification, svix-id dedup, suppression projection), `lib/supabase/domain.ts` + `rpc.ts` (typed app-schema RPC wrapper; ServiceResult envelopes with canonical error codes), `app/api/adapter/route.ts` (session-protected dispatch) + `modules/services/adapter-client.ts`. Project config now exposes the `app` schema to PostgREST and enables TOTP MFA (pushed live; forward migrations 000011–000014: nullable suppression author/processing document, service_role app grants). `scripts/integration-staging.mjs` proves the full chain live with real sessions: RLS draft→submit idempotency, TOTP aal2 elevation + aal1 denial, offer→invoice→payment retry-safety, idempotent conversion, result/timetable publication, denial cases, outbox contract (34 events, unique deliveries, backoff) — INTEGRATION SUITE PASSED. Gates: local DB suites, typecheck, lint, 216 web + 47 contract tests, 66-page build, 22/22 journeys — all green. Pushed to GitHub.
- **2026-08-06 (transactional domain RPCs live + RLS fixes):** Migration `000009_domain_rpcs.sql` implements the plan.md §8 command set (admissions submit/review/decide/respond, unique admission invoice, sandbox payment with attempt+txn idempotency, idempotent enrollment conversion, result publication with frozen rosters + immutable snapshots, timetable publication) — every command SECURITY DEFINER with auth.uid()/role/aal2 checks and same-transaction audit + outbox. `invoices.student_id` relaxed for pre-conversion invoices; `000010_rls_fixes.sql` pushed forward-only fixes for applicant-owned invoice visibility and the guardian result-policy recursion. `scripts/validate-rpcs.sql` proves the full chain (incl. retry deduplication and denial cases) on the scratch instance — all three local suites pass. Types regenerated; typecheck, lint, 216 web + 47 contract tests, 63-page build all green. Pushed to GitHub.
- **2026-08-06 (B2–B6 database layer live + RLS hardening):** Authored, reviewed, and pushed migrations `000004_b2_admissions_careers.sql`–`000008_b6_content_documents.sql` to `jxegiamjcawdywqyutdz` (admissions/careers, enrollment, finance, results/timetable, content/documents/support/notifications). Fixed during review: anon notice policy referencing private `notice_audiences` (now a SECURITY DEFINER `app.public_notice_ids` helper); teacher policies missing the aal2 gate; pure-teacher sessions able to read the whole staff queue (now `app.is_pure_teacher` bounds them to assignment scope at RLS level); timetable/content/support writes not role-scoped (now `app.has_any_role` gated). Added natural-key uniques (`admission_windows(academic_year_id, grade_id)`, `fee_schedule_items(schedule_version_id, code)`, `assessment_components(exam_definition_id, subject_id)`) and `app.has_any_role`/`app.is_pure_teacher` helpers. Local validator rebuilt: GUC-based auth stubs + `scripts/validate-rls.sql` positive/negative RLS matrix (guardian two-child scope, revocation, teacher aal1/aal2 + class/subject scope, anon denial) — all pass. Five new pgTAP suites (79/21/54/59/64 assertions). Seed extended (B2–B6 configuration) in `supabase/seed.sql` and mirrored in `scripts/seed-remote.mjs`; applied and verified on the live project; types regenerated (4638 lines). Gates: typecheck, lint, 216 web + 47 contract tests, 63-page build — all green.
- **2026-08-06 (B1 live schema + auth foundation):** Migrations pushed to the `jxegiamjcawdywqyutdz` project (foundation + B1 identity/school config + actor-read policy), real generated types, remote seed applied and verified via the idempotent `scripts/seed-remote.mjs` (admin client + ignore-duplicates). Auth foundation: session-refresh middleware, `/auth/callback` safe code exchange, server actor resolver (`lib/auth/actor.ts`), adapter-aware email-OTP sign-in behind `FASS_DATA_ADAPTER=supabase`. Resend key added to `.env.local` with the default `onboarding@resend.dev` development sender. Gates: 216 web + 47 contract tests, 63-page build, 22/22 journeys. Pushed to GitHub (`833d39e`).
- **2026-08-06 (B1 identity/school-config slice):** Keys moved to `.env.local` (gitignored); Supabase project `jxegiamjcawdywqyutdz` verified live (GoTrue v2.195.0, PostgREST v14.15). Added migration `000002_b1_identity_school_config.sql` (school configuration + identity/access tables, SECURITY DEFINER authorization helpers `is_staff_aal2`/`has_role`/`is_guardian`/`bump_access_revalidation`, revalidation triggers, RLS policies with explicit base grants for the always-revoked default), B1 seeds (years/grades/sections/subjects/rooms/periods/settings/flags), pgTAP suites for both slices (47 assertions), and `scripts/validate-db-local.sh` which validates migrations + seed + RLS/append-only/outbox behavior on a scratch PostgreSQL 17 instance — all checks pass. Git push to `github.com/scnz313/FAIZ-A-AAM-` is blocked: the repo does not exist and the authenticated `gh` account (HASHIM-HAMEEM) cannot create it.
- **2026-08-06 (B0 backend foundation):** `plan.md` rebaselined to the Supabase Backend Blueprint (B0–B8). B0 completed: version-controlled source established (`git init`, baseline commit `4edf099`); `.env.example` scrubbed to names/placeholders (exposed Supabase keys flagged for rotation) and Node 22 pinned; Supabase CLI 2.111.0 + `@supabase/ssr` + `@supabase/supabase-js` added; `supabase/config.toml` with Realtime/Edge Functions disabled; foundation migration (conventions, `app.new_ref`, version/append-only helpers, audit/outbox/idempotency/webhook-receipts/rate-limit/job-runs tables, `role_definitions`, RLS deny-by-default, SECURITY DEFINER outbox/audit helpers with SKIP LOCKED claim and exponential retry); deterministic role-code seed; 22-assertion pgTAP foundation suite; Supabase client factories (browser/server/admin) + env validation + types placeholder + `db:*` scripts. Typecheck, lint, 216 web + 47 contract tests, and the 62-page build all re-passed. Remaining B0 items are blocked on environment inputs (staging project + keys, Docker, Node 22, Resend).
- **2026-08-06 (plan.md Phases 1–6):** Completed the canonical grant model (Phase 1): maker/checker splits across content/admissions/finance/HR/results, `links.verify` scoped to support_officer + system_administrator, and the fourth demo identity Rania Mir (approvals/timetables); admissions self-approval is rejected with the same actor account. Workflow repairs (Phase 2): result batches carry a subject, teacher marks entry requires class AND subject, moderators return sheets with a reason from the queue, `withdrawPublication` is versioned and never deletes published history, timetable known classes 8-A/9-C with a manager-only selector and honest empty state, date-sheet publish gated to managers. Family context (Phase 3): one link-request store — guardian requests flow to `/staff/link-requests` (LR refs), approval creates the active link exactly once, active links are revocable; `classifyStudentAccess` (current/other/none) wired into invoice and receipt pages with a child-switch panel and neutral denial. Propagation (Phase 4): append-only `auditService.record` + session-backed idempotent demo outbox (`modules/services/outbox.ts`) — one event per payment post, enrollment conversion, result publish/withdraw, content publish, and link decision; retries never duplicate (proven by tests); all `Math.random()` latencies replaced with fixed 200 ms. Contracts + privacy (Phase 5): `packages/contracts` core types with 47 tests, `design/BACKEND-HANDOFF-MATRIX.md`, applicant drafts moved to sessionStorage with sensitive-field stripping. Phase 6 gate: typecheck, lint, historical frontend gate counts and build output, 22/22 critical journeys (incl. the full teacher entry → moderator return → approve → publish → portal chain, timetable manager publish reaching the portal, and family payment → staff ledger parity via the client-refreshed finance workspace), 53-route axe scan, focus smoke, 65-route link crawl, 168-pair responsive sweep — all passed. Docs updated: `UI-COMPLETION-PLAN.md` (phase status + execution record) and `PROJECT-STATUS.md` (evidence, matrix, order).
- **2026-08-05 (I4 + visual refinement):** Implemented staff role/assignment scope: `staff-authorization` module (role/action matrix, active-workspace checks, assignment-class matching); role-aware staff navigation; `StaffRouteGuard` with workspace-switch denial on every staff route (marks-entry sub-route requires the teacher role); demo identity picker in the staff shell (Sana Wani — finance/results/admissions, Firdous Ahmad — teacher, Aisha Lone — content/support/audit/admin; session-persistent); teacher marks entry denied for batches outside assigned classes; results approve/publish/correction gated to publishers and entry links to teachers; timetable editor read-only for non-managers; support response gated to support officers; staff home dashboard filtered by role. Refined the design tokens toward a cleaner, more minimal professional system (finer hairlines, pure-white raised surfaces, softly rounded controls, slightly lighter paper) while preserving the editorial serif, saffron accent, and Urdu wordmark identity. Verified by 23 test files (163 tests), a 62-page build, 11/11 critical journeys (incl. role denial and teacher assignment scope), a 53-route axe scan, a 168-pair responsive sweep, and a clean route/link crawl — all re-run after the visual refinement.
- **2026-08-05 (I3):** Implemented admission → finance → enrollment. Accepting a seat issues one admission invoice through `financeService` (idempotent per applicant; `createAdmissionInvoice` / `assignInvoiceToStudent`); `AdmissionFeeStep` now pays through the shared checkout (attempt/confirm/receipt, duplicate-safe) instead of a timer; `enrollmentConversionService` derives readiness (offered + accepted + fee paid + documents + capacity + approval) and converts idempotently — creating the permanent person/student/enrollment (and activating the guardian link) or matching the already-enrolled child, adopting the paid invoice into the new student's ledger, and marking the application Enrolled with its permanent references; the applicant sees an acknowledgement with the student reference and the portal link. Verified by 22 test files (157 tests), a 62-page build, 9/9 critical journeys (new admission-to-enrollment journey), a 53-route axe scan, a 168-pair responsive sweep, and a clean route/link crawl.
- **2026-08-05 (I2):** Consolidated service boundaries. Finance: ledgers parameterized by student (Aarif + Mariam), portal fees/receipts/overview read `financeService` for the active child, staff finance pages read the same service across both students with parity tests (totals agree). Results: portal marks come from a per-student published snapshot (`academicsService.getStudentResultSnapshot`) with distinct fictional snapshots per child. Timetable: one `timetableService` facade (periods, edits, conflicts, versions, history) now serves portal and staff; the academics timetable stub was removed. Added typed demo adapters `contentService` (notice status/audience/version), `documentsService` (per-student bundles), `notificationsService` (per-account read state, demo clock), `usersService`, `settingsService`, and `auditService`, wired into public/portal/staff pages; the portal overview now reads the active child's ledger and family-audience notices through services. Verified by 21 test files (150 tests), a 62-page build, 8/8 critical journeys, a 53-route axe scan, a 168-pair responsive sweep, and a clean route/link crawl; in-browser parity confirmed (child switch changes the overview band and ledger; staff finance aggregates both students).
- **2026-08-05 (I0/I1):** Implemented the shared relationship/context spine: stable demo session identifiers (account/person/guardian); expired sessions now sign out; sign-out clears identity and relationship context; family-context service gained student-context, account-summary, and link-request summaries; staff-context gained workspace summaries. Added `FamilyContextProvider`/`StaffContextProvider`, service-driven `PortalShell` (two-child switcher, context strip, service sign-out) and `StaffShell` (workspace switcher, role/year/assignment context), `ActiveChildLine` on every child-scoped portal page, and the `/staff/link-requests` approval/rejection path. Verified by 15 test files (112 tests), a 62-page build, 8/8 critical journeys (incl. two-child switching and link approval), a 53-route axe scan, a 168-pair responsive sweep, and a clean route/link crawl.
- **2026-08-05:** Re-audited the complete current route/service/fixture/test structure and reran all frontend validation gates. Confirmed the polished self-contained demo baseline and identified the remaining integration boundary: static one-child context, direct fixture consumers, missing admissions-to-finance/enrollment conversion, non-snapshot result rows, split timetable services, and absent role/assignment authorization. Added `FEATURE-INTEGRATION-SPEC.md`, rebaselined the active frontend plan, and separated UI verification from integrated/production completion.
- **2026-08-04:** Completed P0/P1/P2 frontend work: repaired student-form interaction and error/mobile shell behavior; added deterministic demo services and 87 tests; completed key applicant/payment/results/timetable/support/staff-decision UI flows; removed the optional facility demonstrator from core navigation and launch checks; and established warning-free build/browser gates.
- **2026-08-03 to 2026-08-04:** Built the broad editorial route tree, design system, fictional content/data, applicant/portal/staff prototypes, optional environment demonstrator, concept imagery, and responsive design passes.
